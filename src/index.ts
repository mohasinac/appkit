// Single unified barrel
// Runtime tags: [CLIENT-SSR] [CLIENT-ONLY] [SERVER-ONLY] [DB] [UTIL] [TYPE] [SCHEMA] [CONFIG]

// ./tokens/index
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// COLORS - Constant used across modules.
export { COLORS } from "./tokens/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// FLUID_GRID_MIN_WIDTHS - Constant used across modules.
export { FLUID_GRID_MIN_WIDTHS } from "./tokens/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// LOCALE_CONFIG - Constant used across modules.
export { LOCALE_CONFIG } from "./tokens/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// RADIUS - Constant used across modules.
export { RADIUS } from "./tokens/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// SHADOWS - Constant used across modules.
export { SHADOWS } from "./tokens/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// THEME_CONSTANTS - Constant used across modules.
export { THEME_CONSTANTS } from "./tokens/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// TOKENS - Constant used across modules.
export { TOKENS } from "./tokens/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// Z_INDEX - Constant used across modules.
export { Z_INDEX } from "./tokens/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// token - Shared export for token.
export { token } from "./tokens/index";

// ./ui/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Accordion - Shared export for accordion.
export { Accordion } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AccordionItem - Model for accordion item.
export { AccordionItem } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ActiveFilterChips - Shared export for active filter chips.
export { ActiveFilterChips } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Alert - Shared export for alert.
export { Alert } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Article - Shared export for article.
export { Article } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Aside - Shared export for aside.
export { Aside } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Avatar - Shared export for avatar.
export { Avatar } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AvatarDisplay - Shared export for avatar display.
export { AvatarDisplay } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AvatarGroup - Shared export for avatar group.
export { AvatarGroup } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BackgroundRenderer - Shared export for background renderer.
export { BackgroundRenderer } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Badge - Component for badge.
export { Badge } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BaseListingCard - Component for base listing card.
export { BaseListingCard } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BlockFooter - Shared export for block footer.
export { BlockFooter } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BlockHeader - Shared export for block header.
export { BlockHeader } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Breadcrumb - Shared export for breadcrumb.
export { Breadcrumb } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BulkActionBar - Shared export for bulk action bar.
export { BulkActionBar } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Button - Component for button.
export { Button } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Caption - Shared export for caption.
export { Caption } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Card - Component for card.
export { Card } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CardBody - Shared export for card body.
export { CardBody } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CardFooter - Shared export for card footer.
export { CardFooter } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CardHeader - Shared export for card header.
export { CardHeader } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Checkbox - Shared export for checkbox.
export { Checkbox } from "./ui/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// ConfirmDeleteModal - Component for confirm delete modal.
export { ConfirmDeleteModal } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Container - Shared export for container.
export { Container } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CountdownDisplay - Shared export for countdown display.
export { CountdownDisplay } from "./ui/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_PAGINATION_CONFIG - Constant used across modules.
export { DEFAULT_PAGINATION_CONFIG } from "./ui/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_STICKY_CONFIG - Constant used across modules.
export { DEFAULT_STICKY_CONFIG } from "./ui/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_TABLE_CONFIG - Constant used across modules.
export { DEFAULT_TABLE_CONFIG } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DashboardStatsCard - Component for dashboard stats card.
export { DashboardStatsCard } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DataTable - Component for data table.
export { DataTable } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DescriptionField - Shared export for description field.
export { DescriptionField } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DetailViewShell - Component for detail view shell.
export { DetailViewShell } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Div - Shared export for div.
export { Div } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Divider - Shared export for divider.
export { Divider } from "./ui/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// Drawer - Component for drawer.
export { Drawer } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Dropdown - Shared export for dropdown.
export { Dropdown } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DropdownItem - Model for dropdown item.
export { DropdownItem } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DropdownMenu - Shared export for dropdown menu.
export { DropdownMenu } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DropdownSeparator - Shared export for dropdown separator.
export { DropdownSeparator } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DropdownTrigger - Shared export for dropdown trigger.
export { DropdownTrigger } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DynamicSelect - Component for dynamic select.
export { DynamicSelect } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EmptyState - Model for empty state.
export { EmptyState } from "./ui/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// FilterDrawer - Component for filter drawer.
export { FilterDrawer } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FlowDiagram - Shared export for flow diagram.
export { FlowDiagram } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Footer - Shared export for footer.
export { Footer } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Form - Component for form.
export { Form } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FormActions - Shared export for form actions.
export { FormActions } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FormField - Shared export for form field.
export { FormField } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FormFieldSpan - Shared export for form field span.
export { FormFieldSpan } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FormGrid - Component for form grid.
export { FormGrid } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FormGridField - Shared export for form grid field.
export { FormGridField } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FormGroup - Shared export for form group.
export { FormGroup } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// GRID_MAP - Constant used across modules.
export { GRID_MAP } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Grid - Component for grid.
export { Grid } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Header - Shared export for header.
export { Header } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Heading - Shared export for heading.
export { Heading } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HorizontalScroller - Shared export for horizontal scroller.
export { HorizontalScroller } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// IconButton - Component for icon button.
export { IconButton } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ImageGallery - Shared export for image gallery.
export { ImageGallery } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ImageLightbox - Shared export for image lightbox.
export { ImageLightbox } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// IndeterminateProgress - Shared export for indeterminate progress.
export { IndeterminateProgress } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Input - Component for input.
export { Input } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ItemRow - Shared export for item row.
export { ItemRow } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Label - Shared export for label.
export { Label } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Li - Shared export for li.
export { Li } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ListingLayout - Component for listing layout.
export { ListingLayout } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ListingViewShell - Component for listing view shell.
export { ListingViewShell } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Main - Shared export for main.
export { Main } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Menu - Shared export for menu.
export { Menu } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MenuContent - Shared export for menu content.
export { MenuContent } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MenuItem - Model for menu item.
export { MenuItem } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MenuSeparator - Shared export for menu separator.
export { MenuSeparator } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MenuTrigger - Shared export for menu trigger.
export { MenuTrigger } from "./ui/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// Modal - Component for modal.
export { Modal } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ModalFooter - Shared export for modal footer.
export { ModalFooter } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Nav - Shared export for nav.
export { Nav } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Ol - Shared export for ol.
export { Ol } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Pagination - Shared export for pagination.
export { Pagination } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PasswordStrengthIndicator - Shared export for password strength indicator.
export { PasswordStrengthIndicator } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PriceDisplay - Shared export for price display.
export { PriceDisplay } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Progress - Shared export for progress.
export { Progress } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// RadioGroup - Shared export for radio group.
export { RadioGroup } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// RatingDisplay - Shared export for rating display.
export { RatingDisplay } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ResponsiveView - Component for responsive view.
export { ResponsiveView } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// RichText - Shared export for rich text.
export { RichText } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// RoleBadge - Component for role badge.
export { RoleBadge } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Row - Shared export for row.
export { Row } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// RowActionMenu - Shared export for row action menu.
export { RowActionMenu } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Section - Shared export for section.
export { Section } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SectionTabs - Component for section tabs.
export { SectionTabs } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Select - Component for select.
export { Select } from "./ui/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// SideDrawer - Component for side drawer.
export { SideDrawer } from "./ui/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// SideModal - Component for side modal.
export { SideModal } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Skeleton - Shared export for skeleton.
export { Skeleton } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SkipToMain - Shared export for skip to main.
export { SkipToMain } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Slider - Shared export for slider.
export { Slider } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SlottedListingView - Component for slotted listing view.
export { SlottedListingView } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SortDropdown - Shared export for sort dropdown.
export { SortDropdown } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Span - Shared export for span.
export { Span } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Spinner - Shared export for spinner.
export { Spinner } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Stack - Shared export for stack.
export { Stack } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StackedViewShell - Component for stacked view shell.
export { StackedViewShell } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StarRating - Shared export for star rating.
export { StarRating } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StatsGrid - Component for stats grid.
export { StatsGrid } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StatusBadge - Component for status badge.
export { StatusBadge } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StepperNav - Shared export for stepper nav.
export { StepperNav } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SummaryCard - Component for summary card.
export { SummaryCard } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TabStrip - Shared export for tab strip.
export { TabStrip } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TablePagination - Shared export for table pagination.
export { TablePagination } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Tabs - Component for tabs.
export { Tabs } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TabsContent - Shared export for tabs content.
export { TabsContent } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TabsList - Shared export for tabs list.
export { TabsList } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TabsTrigger - Shared export for tabs trigger.
export { TabsTrigger } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TagInput - Component for tag input.
export { TagInput } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Text - Shared export for text.
export { Text } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TextLink - Shared export for text link.
export { TextLink } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Textarea - Shared export for textarea.
export { Textarea } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ToastProvider - Component for toast provider.
export { ToastProvider } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Toggle - Shared export for toggle.
export { Toggle } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Tooltip - Shared export for tooltip.
export { Tooltip } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Ul - Shared export for ul.
export { Ul } from "./ui/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// UnsavedChangesModal - Component for unsaved changes modal.
export { UnsavedChangesModal } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ViewToggle - Shared export for view toggle.
export { ViewToggle } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildColumns - Helper for build columns.
export { buildColumns } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// classNames - Shared export for class names.
export { classNames } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createColumnBuilder - Helper for create column builder.
export { createColumnBuilder } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// mergeTableConfig - Helper for merge table config.
export { mergeTableConfig } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// mergeTailwindClasses - Helper for merge tailwind classes.
export { mergeTailwindClasses } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// renderBoolean - Shared export for render boolean.
export { renderBoolean } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// renderCount - Shared export for render count.
export { renderCount } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// renderCurrency - Shared export for render currency.
export { renderCurrency } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// renderCurrencyCompact - Shared export for render currency compact.
export { renderCurrencyCompact } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// renderNullable - Shared export for render nullable.
export { renderNullable } from "./ui/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// renderRating - Shared export for render rating.
export { renderRating } from "./ui/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useToast - React hook for use toast.
export { useToast } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AccordionItemProps - Type contract for accordion item props.
export type { AccordionItemProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AccordionProps - Type contract for accordion props.
export type { AccordionProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ActiveFilter - Type contract for active filter.
export type { ActiveFilter } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ActiveFilterChipsProps - Type contract for active filter chips props.
export type { ActiveFilterChipsProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AlertProps - Type contract for alert props.
export type { AlertProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ArticleProps - Type contract for article props.
export type { ArticleProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AsideProps - Type contract for aside props.
export type { AsideProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AsyncPage - Type contract for async page.
export type { AsyncPage } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AvatarDisplayProps - Type contract for avatar display props.
export type { AvatarDisplayProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AvatarGroupProps - Type contract for avatar group props.
export type { AvatarGroupProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AvatarProps - Type contract for avatar props.
export type { AvatarProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BackgroundConfig - Type contract for background config.
export type { BackgroundConfig } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BackgroundRendererProps - Type contract for background renderer props.
export type { BackgroundRendererProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BadgeOrderStatus - Type contract for badge order status.
export type { BadgeOrderStatus } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BadgePaymentStatus - Type contract for badge payment status.
export type { BadgePaymentStatus } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BadgeProps - Type contract for badge props.
export type { BadgeProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BadgeReviewStatus - Type contract for badge review status.
export type { BadgeReviewStatus } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BadgeTicketStatus - Type contract for badge ticket status.
export type { BadgeTicketStatus } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BadgeVariant - Type contract for badge variant.
export type { BadgeVariant } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BaseListingCardCheckboxProps - Type contract for base listing card checkbox props.
export type { BaseListingCardCheckboxProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BaseListingCardHeroProps - Type contract for base listing card hero props.
export type { BaseListingCardHeroProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BaseListingCardInfoProps - Type contract for base listing card info props.
export type { BaseListingCardInfoProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BaseListingCardRootProps - Type contract for base listing card root props.
export type { BaseListingCardRootProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlockFooterProps - Type contract for block footer props.
export type { BlockFooterProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlockHeaderProps - Type contract for block header props.
export type { BlockHeaderProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BooleanRenderOpts - Type contract for boolean render opts.
export type { BooleanRenderOpts } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BreadcrumbItem - Type contract for breadcrumb item.
export type { BreadcrumbItem } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BreadcrumbProps - Type contract for breadcrumb props.
export type { BreadcrumbProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BulkActionBarLabels - Type contract for bulk action bar labels.
export type { BulkActionBarLabels } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BulkActionBarProps - Type contract for bulk action bar props.
export type { BulkActionBarProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BulkActionItem - Type contract for bulk action item.
export type { BulkActionItem } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ButtonProps - Type contract for button props.
export type { ButtonProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CardProps - Type contract for card props.
export type { CardProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CardSectionProps - Type contract for card section props.
export type { CardSectionProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CheckboxProps - Type contract for checkbox props.
export type { CheckboxProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ConfirmDeleteModalProps - Type contract for confirm delete modal props.
export type { ConfirmDeleteModalProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ContainerProps - Type contract for container props.
export type { ContainerProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ContainerSize - Type contract for container size.
export type { ContainerSize } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CountdownDisplayProps - Type contract for countdown display props.
export type { CountdownDisplayProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DashboardStatsCardProps - Type contract for dashboard stats card props.
export type { DashboardStatsCardProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DataTableColumn - Type contract for data table column.
export type { DataTableColumn } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DataTableProps - Type contract for data table props.
export type { DataTableProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DescriptionFieldProps - Type contract for description field props.
export type { DescriptionFieldProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DetailViewLayout - Type contract for detail view layout.
export type { DetailViewLayout } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DetailViewShellProps - Type contract for detail view shell props.
export type { DetailViewShellProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DivProps - Type contract for div props.
export type { DivProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DividerProps - Type contract for divider props.
export type { DividerProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DrawerMode - Type contract for drawer mode.
export type { DrawerMode } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DrawerProps - Type contract for drawer props.
export type { DrawerProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DropdownMenuItem - Type contract for dropdown menu item.
export type { DropdownMenuItem } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DropdownProps - Type contract for dropdown props.
export type { DropdownProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DynamicSelectOption - Type contract for dynamic select option.
export type { DynamicSelectOption } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DynamicSelectProps - Type contract for dynamic select props.
export type { DynamicSelectProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EmptyStateProps - Type contract for empty state props.
export type { EmptyStateProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FilterDrawerProps - Type contract for filter drawer props.
export type { FilterDrawerProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FlowDiagramProps - Type contract for flow diagram props.
export type { FlowDiagramProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FlowStep - Type contract for flow step.
export type { FlowStep } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FooterProps - Type contract for footer props.
export type { FooterProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FormActionsProps - Type contract for form actions props.
export type { FormActionsProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FormFieldProps - Type contract for form field props.
export type { FormFieldProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FormFieldSpanProps - Type contract for form field span props.
export type { FormFieldSpanProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FormGridProps - Type contract for form grid props.
export type { FormGridProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FormGroupProps - Type contract for form group props.
export type { FormGroupProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FormProps - Type contract for form props.
export type { FormProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GalleryImage - Type contract for gallery image.
export type { GalleryImage } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GapKey - Type contract for gap key.
export type { GapKey } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GapToken - Type contract for gap token.
export type { GapToken } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenericStatus - Type contract for generic status.
export type { GenericStatus } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GridCols - Type contract for grid cols.
export type { GridCols } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GridProps - Type contract for grid props.
export type { GridProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HeaderProps - Type contract for header props.
export type { HeaderProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HorizontalScrollerProps - Type contract for horizontal scroller props.
export type { HorizontalScrollerProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IconButtonProps - Type contract for icon button props.
export type { IconButtonProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ImageGalleryProps - Type contract for image gallery props.
export type { ImageGalleryProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ImageLightboxProps - Type contract for image lightbox props.
export type { ImageLightboxProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IndeterminateProgressProps - Type contract for indeterminate progress props.
export type { IndeterminateProgressProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// InputProps - Type contract for input props.
export type { InputProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ItemRowProps - Type contract for item row props.
export type { ItemRowProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LiProps - Type contract for li props.
export type { LiProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LightboxImage - Type contract for lightbox image.
export type { LightboxImage } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ListingLayoutLabels - Type contract for listing layout labels.
export type { ListingLayoutLabels } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ListingLayoutProps - Type contract for listing layout props.
export type { ListingLayoutProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ListingViewShellProps - Type contract for listing view shell props.
export type { ListingViewShellProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MainProps - Type contract for main props.
export type { MainProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MenuProps - Type contract for menu props.
export type { MenuProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ModalProps - Type contract for modal props.
export type { ModalProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NavProps - Type contract for nav props.
export type { NavProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OlProps - Type contract for ol props.
export type { OlProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PaginationConfig - Type contract for pagination config.
export type { PaginationConfig } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PaginationProps - Type contract for pagination props.
export type { PaginationProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PasswordStrengthIndicatorProps - Type contract for password strength indicator props.
export type { PasswordStrengthIndicatorProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PerViewConfig - Type contract for per view config.
export type { PerViewConfig } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PriceDisplayProps - Type contract for price display props.
export type { PriceDisplayProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProgressProps - Type contract for progress props.
export type { ProgressProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RadioGroupProps - Type contract for radio group props.
export type { RadioGroupProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RadioOption - Type contract for radio option.
export type { RadioOption } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RatingDisplayProps - Type contract for rating display props.
export type { RatingDisplayProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RatingMode - Type contract for rating mode.
export type { RatingMode } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RatingRenderOpts - Type contract for rating render opts.
export type { RatingRenderOpts } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ResponsiveViewProps - Type contract for responsive view props.
export type { ResponsiveViewProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RichTextProps - Type contract for rich text props.
export type { RichTextProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RoleBadgeProps - Type contract for role badge props.
export type { RoleBadgeProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RowAction - Type contract for row action.
export type { RowAction } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RowActionMenuProps - Type contract for row action menu props.
export type { RowActionMenuProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RowProps - Type contract for row props.
export type { RowProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SectionProps - Type contract for section props.
export type { SectionProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SectionTab - Type contract for section tab.
export type { SectionTab } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SectionTabsProps - Type contract for section tabs props.
export type { SectionTabsProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SelectOption - Type contract for select option.
export type { SelectOption } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SelectProps - Type contract for select props.
export type { SelectProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SideDrawerProps - Type contract for side drawer props.
export type { SideDrawerProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SideModalProps - Type contract for side modal props.
export type { SideModalProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SkeletonProps - Type contract for skeleton props.
export type { SkeletonProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SkipToMainProps - Type contract for skip to main props.
export type { SkipToMainProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SliderProps - Type contract for slider props.
export type { SliderProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SlottedListingViewLabels - Type contract for slotted listing view labels.
export type { SlottedListingViewLabels } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SlottedListingViewProps - Type contract for slotted listing view props.
export type { SlottedListingViewProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SmartFormFieldProps - Type contract for smart form field props.
export type { SmartFormFieldProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SortDropdownProps - Type contract for sort dropdown props.
export type { SortDropdownProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SortOption - Type contract for sort option.
export type { SortOption } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SpinnerProps - Type contract for spinner props.
export type { SpinnerProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StackProps - Type contract for stack props.
export type { StackProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StackedViewShellLabels - Type contract for stacked view shell labels.
export type { StackedViewShellLabels } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StackedViewShellProps - Type contract for stacked view shell props.
export type { StackedViewShellProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StarRatingProps - Type contract for star rating props.
export type { StarRatingProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StatItem - Type contract for stat item.
export type { StatItem } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StatsGridProps - Type contract for stats grid props.
export type { StatsGridProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StatusBadgeProps - Type contract for status badge props.
export type { StatusBadgeProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StatusBadgeStatus - Type contract for status badge status.
export type { StatusBadgeStatus } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StepperNavProps - Type contract for stepper nav props.
export type { StepperNavProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StepperNavStep - Type contract for stepper nav step.
export type { StepperNavStep } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StickyConfig - Type contract for sticky config.
export type { StickyConfig } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SummaryCardProps - Type contract for summary card props.
export type { SummaryCardProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SummaryLine - Type contract for summary line.
export type { SummaryLine } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TabStripProps - Type contract for tab strip props.
export type { TabStripProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TabStripTab - Type contract for tab strip tab.
export type { TabStripTab } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TableConfig - Type contract for table config.
export type { TableConfig } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TablePaginationLabels - Type contract for table pagination labels.
export type { TablePaginationLabels } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TablePaginationProps - Type contract for table pagination props.
export type { TablePaginationProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TableViewMode - Type contract for table view mode.
export type { TableViewMode } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TabsContentProps - Type contract for tabs content props.
export type { TabsContentProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TabsListProps - Type contract for tabs list props.
export type { TabsListProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TabsProps - Type contract for tabs props.
export type { TabsProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TabsTriggerProps - Type contract for tabs trigger props.
export type { TabsTriggerProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TagInputProps - Type contract for tag input props.
export type { TagInputProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TextLinkProps - Type contract for text link props.
export type { TextLinkProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TextareaProps - Type contract for textarea props.
export type { TextareaProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ToastPosition - Type contract for toast position.
export type { ToastPosition } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ToastProviderProps - Type contract for toast provider props.
export type { ToastProviderProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ToastVariant - Type contract for toast variant.
export type { ToastVariant } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ToggleProps - Type contract for toggle props.
export type { ToggleProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TooltipProps - Type contract for tooltip props.
export type { TooltipProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UlProps - Type contract for ul props.
export type { UlProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UnsavedChangesLabels - Type contract for unsaved changes labels.
export type { UnsavedChangesLabels } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UnsavedChangesModalProps - Type contract for unsaved changes modal props.
export type { UnsavedChangesModalProps } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ViewMode - Type contract for view mode.
export type { ViewMode } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ViewPortal - Type contract for view portal.
export type { ViewPortal } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ViewToggleLabels - Type contract for view toggle labels.
export type { ViewToggleLabels } from "./ui/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ViewToggleProps - Type contract for view toggle props.
export type { ViewToggleProps } from "./ui/index";

// ./react/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ErrorBoundary - Component for error boundary.
export { ErrorBoundary } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// RTDBPayloadStatus - Model for rtdbpayload status.
export { RTDBPayloadStatus } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// RealtimeEventStatus - Model for realtime event status.
export { RealtimeEventStatus } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// RealtimeEventType - Model for realtime event type.
export { RealtimeEventType } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SessionProvider - Component for session provider.
export { SessionProvider } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ThemeProvider - Component for theme provider.
export { ThemeProvider } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// UNSAVED_CHANGES_EVENT - Constant used across modules.
export { UNSAVED_CHANGES_EVENT } from "./react/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useAuth - React hook for use auth.
export { useAuth } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useBreakpoint - React hook for use breakpoint.
export { useBreakpoint } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useBulkAction - React hook for use bulk action.
export { useBulkAction } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useBulkSelection - React hook for use bulk selection.
export { useBulkSelection } from "./react/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useCamera - React hook for use camera.
export { useCamera } from "./react/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useClickOutside - React hook for use click outside.
export { useClickOutside } from "./react/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useContainerGrid - React hook for use container grid.
export { useContainerGrid } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCountdown - React hook for use countdown.
export { useCountdown } from "./react/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useGesture - React hook for use gesture.
export { useGesture } from "./react/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useKeyPress - React hook for use key press.
export { useKeyPress } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useLongPress - React hook for use long press.
export { useLongPress } from "./react/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useMediaQuery - React hook for use media query.
export { useMediaQuery } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useMessage - React hook for use message.
export { useMessage } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useModalStack - React hook for use modal stack.
export { useModalStack } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// usePendingFilters - React hook for use pending filters.
export { usePendingFilters } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// usePendingTable - React hook for use pending table.
export { usePendingTable } from "./react/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// usePullToRefresh - React hook for use pull to refresh.
export { usePullToRefresh } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useRealtimeEvent - React hook for use realtime event.
export { useRealtimeEvent } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useSession - React hook for use session.
export { useSession } from "./react/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useSwipe - React hook for use swipe.
export { useSwipe } from "./react/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useTheme - React hook for use theme.
export { useTheme } from "./react/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useUnsavedChanges - React hook for use unsaved changes.
export { useUnsavedChanges } from "./react/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useUrlTable - React hook for use url table.
export { useUrlTable } from "./react/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useVisibleItems - React hook for use visible items.
export { useVisibleItems } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AvatarMetadataShape - Type contract for avatar metadata shape.
export type { AvatarMetadataShape } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BulkActionItemFailure - Type contract for bulk action item failure.
export type { BulkActionItemFailure } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BulkActionPayload - Type contract for bulk action payload.
export type { BulkActionPayload } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BulkActionResult - Type contract for bulk action result.
export type { BulkActionResult } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BulkActionSummary - Type contract for bulk action summary.
export type { BulkActionSummary } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CountdownRemaining - Type contract for countdown remaining.
export type { CountdownRemaining } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ErrorBoundaryProps - Type contract for error boundary props.
export type { ErrorBoundaryProps } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GestureType - Type contract for gesture type.
export type { GestureType } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// InvalidateQueriesFn - Type contract for invalidate queries fn.
export type { InvalidateQueriesFn } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// KeyModifiers - Type contract for key modifiers.
export type { KeyModifiers } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ModalEntry - Type contract for modal entry.
export type { ModalEntry } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PendingTable - Type contract for pending table.
export type { PendingTable } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RTDBEventPayload - Type contract for rtdbevent payload.
export type { RTDBEventPayload } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RealtimeEventMessages - Type contract for realtime event messages.
export type { RealtimeEventMessages } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RealtimeEventStatusValue - Type contract for realtime event status value.
export type { RealtimeEventStatusValue } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RealtimeEventTypeValue - Type contract for realtime event type value.
export type { RealtimeEventTypeValue } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SessionContextValue - Type contract for session context value.
export type { SessionContextValue } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SessionEndpoints - Type contract for session endpoints.
export type { SessionEndpoints } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SessionProviderProps - Type contract for session provider props.
export type { SessionProviderProps } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SessionUser - Type contract for session user.
export type { SessionUser } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SwipeDirection - Type contract for swipe direction.
export type { SwipeDirection } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ThemeMode - Type contract for theme mode.
export type { ThemeMode } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseBulkActionOptions - Type contract for use bulk action options.
export type { UseBulkActionOptions } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseBulkActionReturn - Type contract for use bulk action return.
export type { UseBulkActionReturn } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseBulkSelectionOptions - Type contract for use bulk selection options.
export type { UseBulkSelectionOptions } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseBulkSelectionReturn - Type contract for use bulk selection return.
export type { UseBulkSelectionReturn } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseCameraOptions - Type contract for use camera options.
export type { UseCameraOptions } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseCameraReturn - Type contract for use camera return.
export type { UseCameraReturn } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseClickOutsideOptions - Type contract for use click outside options.
export type { UseClickOutsideOptions } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseContainerGridOptions - Type contract for use container grid options.
export type { UseContainerGridOptions } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseContainerGridResult - Type contract for use container grid result.
export type { UseContainerGridResult } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseGestureOptions - Type contract for use gesture options.
export type { UseGestureOptions } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseKeyPressOptions - Type contract for use key press options.
export type { UseKeyPressOptions } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UsePendingFiltersOptions - Type contract for use pending filters options.
export type { UsePendingFiltersOptions } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UsePendingFiltersReturn - Type contract for use pending filters return.
export type { UsePendingFiltersReturn } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UsePendingTableReturn - Type contract for use pending table return.
export type { UsePendingTableReturn } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UsePullToRefreshOptions - Type contract for use pull to refresh options.
export type { UsePullToRefreshOptions } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UsePullToRefreshReturn - Type contract for use pull to refresh return.
export type { UsePullToRefreshReturn } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseRealtimeEventConfig - Type contract for use realtime event config.
export type { UseRealtimeEventConfig } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseRealtimeEventReturn - Type contract for use realtime event return.
export type { UseRealtimeEventReturn } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseSwipeOptions - Type contract for use swipe options.
export type { UseSwipeOptions } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseUnsavedChangesOptions - Type contract for use unsaved changes options.
export type { UseUnsavedChangesOptions } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseUnsavedChangesReturn - Type contract for use unsaved changes return.
export type { UseUnsavedChangesReturn } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseUrlTableOptions - Type contract for use url table options.
export type { UseUrlTableOptions } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseVisibleItemsOptions - Type contract for use visible items options.
export type { UseVisibleItemsOptions } from "./react/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseVisibleItemsResult - Type contract for use visible items result.
export type { UseVisibleItemsResult } from "./react/index";

// ./values/index
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// CONFIRMATION_MESSAGES - Constant used across modules.
export { CONFIRMATION_MESSAGES } from "./values/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// INFO_MESSAGES - Constant used across modules.
export { INFO_MESSAGES } from "./values/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// SUCCESS_MESSAGES - Constant used across modules.
export { SUCCESS_MESSAGES } from "./values/index";

// ./utils/index
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// BUSINESS_DAY_HOUR_IST - Constant used across modules.
export { BUSINESS_DAY_HOUR_IST } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// arrayToObject - Shared export for array to object.
export { arrayToObject } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// booleanToString - Shared export for boolean to string.
export { booleanToString } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// buildSieveFilters - Helper for build sieve filters.
export { buildSieveFilters } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// calculatePagination - Helper for calculate pagination.
export { calculatePagination } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// capitalize - Shared export for capitalize.
export { capitalize } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// capitalizeWords - Shared export for capitalize words.
export { capitalizeWords } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// chunk - Shared export for chunk.
export { chunk } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// cleanObject - Shared export for clean object.
export { cleanObject } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// coerceMediaField - Shared export for coerce media field.
export { coerceMediaField } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// coerceMediaFieldArray - Shared export for coerce media field array.
export { coerceMediaFieldArray } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// currentYear - Shared export for current year.
export { currentYear } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// dateToISOString - Shared export for date to isostring.
export { dateToISOString } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// deepClone - Shared export for deep clone.
export { deepClone } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// deepMerge - Shared export for deep merge.
export { deepMerge } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// deleteCookie - Helper for delete cookie.
export { deleteCookie } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// deriveFormFields - Model for derive form fields.
export { deriveFormFields } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// easings - Shared export for easings.
export { easings } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// escapeHtml - Shared export for escape html.
export { escapeHtml } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// firestoreTimestampToDate - Shared export for firestore timestamp to date.
export { firestoreTimestampToDate } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatCompactNumber - Helper for format compact number.
export { formatCompactNumber } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatCurrency - Helper for format currency.
export { formatCurrency } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatCustomDate - Helper for format custom date.
export { formatCustomDate } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatDate - Helper for format date.
export { formatDate } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatDateRange - Helper for format date range.
export { formatDateRange } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatDateTime - Helper for format date time.
export { formatDateTime } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatDecimal - Helper for format decimal.
export { formatDecimal } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatFileSize - Helper for format file size.
export { formatFileSize } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatMonthYear - Helper for format month year.
export { formatMonthYear } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatNumber - Helper for format number.
export { formatNumber } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatOrdinal - Helper for format ordinal.
export { formatOrdinal } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatPercentage - Helper for format percentage.
export { formatPercentage } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatRelativeTime - Helper for format relative time.
export { formatRelativeTime } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// formatTime - Helper for format time.
export { formatTime } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateAuctionId - Helper for generate auction id.
export { generateAuctionId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateAuctionImageFilename - Helper for generate auction image filename.
export { generateAuctionImageFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateBarcodeFromId - Helper for generate barcode from id.
export { generateBarcodeFromId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateBidId - Helper for generate bid id.
export { generateBidId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateBlogImageFilename - Helper for generate blog image filename.
export { generateBlogImageFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateBlogPostId - Helper for generate blog post id.
export { generateBlogPostId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateCarouselId - Helper for generate carousel id.
export { generateCarouselId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateCarouselImageFilename - Helper for generate carousel image filename.
export { generateCarouselImageFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateCategoryId - Helper for generate category id.
export { generateCategoryId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateCategoryImageFilename - Helper for generate category image filename.
export { generateCategoryImageFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateCouponId - Helper for generate coupon id.
export { generateCouponId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateCroppedImageFilename - Helper for generate cropped image filename.
export { generateCroppedImageFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateEventImageFilename - Helper for generate event image filename.
export { generateEventImageFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateFAQId - Helper for generate faqid.
export { generateFAQId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateHomepageSectionId - Helper for generate homepage section id.
export { generateHomepageSectionId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateInvoiceFilename - Helper for generate invoice filename.
export { generateInvoiceFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateMediaFilename - Helper for generate media filename.
export { generateMediaFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateOfferId - Helper for generate offer id.
export { generateOfferId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateOrderId - Helper for generate order id.
export { generateOrderId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generatePayoutDocFilename - Helper for generate payout doc filename.
export { generatePayoutDocFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generatePayoutId - Helper for generate payout id.
export { generatePayoutId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generatePreOrderId - Helper for generate pre order id.
export { generatePreOrderId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generatePreOrderImageFilename - Helper for generate pre order image filename.
export { generatePreOrderImageFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateProductId - Helper for generate product id.
export { generateProductId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateProductImageFilename - Helper for generate product image filename.
export { generateProductImageFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateProductVideoFilename - Helper for generate product video filename.
export { generateProductVideoFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateQRCodeData - Helper for generate qrcode data.
export { generateQRCodeData } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateReviewId - Helper for generate review id.
export { generateReviewId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateReviewImageFilename - Helper for generate review image filename.
export { generateReviewImageFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateReviewVideoFilename - Helper for generate review video filename.
export { generateReviewVideoFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateRichTextImageFilename - Helper for generate rich text image filename.
export { generateRichTextImageFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateStoreBannerFilename - Helper for generate store banner filename.
export { generateStoreBannerFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateStoreLogoFilename - Helper for generate store logo filename.
export { generateStoreLogoFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateTrimmedVideoFilename - Helper for generate trimmed video filename.
export { generateTrimmedVideoFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateUniqueId - Helper for generate unique id.
export { generateUniqueId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateUserAvatarFilename - Helper for generate user avatar filename.
export { generateUserAvatarFilename } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// generateUserId - Helper for generate user id.
export { generateUserId } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// getBusinessDayEligibilityDate - Helper for get business day eligibility date.
export { getBusinessDayEligibilityDate } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// getBusinessDayStart - Helper for get business day start.
export { getBusinessDayStart } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// getBusinessDaysElapsed - Helper for get business days elapsed.
export { getBusinessDaysElapsed } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// getBusinessDaysRemaining - Helper for get business days remaining.
export { getBusinessDaysRemaining } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// getContrastColor - Helper for get contrast color.
export { getContrastColor } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// getCookie - Helper for get cookie.
export { getCookie } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// getMediaUrl - Helper for get media url.
export { getMediaUrl } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// groupBy - Shared export for group by.
export { groupBy } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// hasCookie - Shared export for has cookie.
export { hasCookie } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// hexToRgb - Shared export for hex to rgb.
export { hexToRgb } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// idExists - Shared export for id exists.
export { idExists } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// inferMediaTypeFromMime - Helper for infer media type from mime.
export { inferMediaTypeFromMime } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// isEmptyObject - Shared export for is empty object.
export { isEmptyObject } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// isEmptyString - Shared export for is empty string.
export { isEmptyString } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// isEqual - Shared export for is equal.
export { isEqual } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// isFuture - Shared export for is future.
export { isFuture } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// isPast - Shared export for is past.
export { isPast } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// isSameMonth - Shared export for is same month.
export { isSameMonth } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// isToday - Shared export for is today.
export { isToday } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// maskString - Helper for mask string.
export { maskString } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// mediaFieldSchema - Model for media field schema.
export { mediaFieldSchema } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// nowISO - Shared export for now iso.
export { nowISO } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// nowMs - Shared export for now ms.
export { nowMs } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// objectToArray - Shared export for object to array.
export { objectToArray } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// objectToQueryString - Shared export for object to query string.
export { objectToQueryString } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// omit - Shared export for omit.
export { omit } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// paginate - Shared export for paginate.
export { paginate } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// parseCookies - Helper for parse cookies.
export { parseCookies } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// parseFormattedNumber - Helper for parse formatted number.
export { parseFormattedNumber } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// pick - Shared export for pick.
export { pick } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// proseMirrorToHtml - Shared export for prose mirror to html.
export { proseMirrorToHtml } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// queryStringToObject - Shared export for query string to object.
export { queryStringToObject } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// randomString - Shared export for random string.
export { randomString } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// resolveDate - Helper for resolve date.
export { resolveDate } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// rgbToHex - Shared export for rgb to hex.
export { rgbToHex } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// slugify - Shared export for slugify.
export { slugify } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// sort - Shared export for sort.
export { sort } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// sortBy - Shared export for sort by.
export { sortBy } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// splitCartIntoOrderGroups - Shared export for split cart into order groups.
export { splitCartIntoOrderGroups } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// stringToBoolean - Shared export for string to boolean.
export { stringToBoolean } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// stripHtml - Shared export for strip html.
export { stripHtml } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// truncate - Shared export for truncate.
export { truncate } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// truncateWords - Shared export for truncate words.
export { truncateWords } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// unique - Shared export for unique.
export { unique } from "./utils/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// uniqueBy - Shared export for unique by.
export { uniqueBy } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DeriveFormFieldsOptions - Type contract for derive form fields options.
export type { DeriveFormFieldsOptions } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DerivedField - Type contract for derived field.
export type { DerivedField } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DerivedFieldOption - Type contract for derived field option.
export type { DerivedFieldOption } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DerivedFieldType - Type contract for derived field type.
export type { DerivedFieldType } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateAuctionIdInput - Type contract for generate auction id input.
export type { GenerateAuctionIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateAuctionImageFilenameInput - Type contract for generate auction image filename input.
export type { GenerateAuctionImageFilenameInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateBidIdInput - Type contract for generate bid id input.
export type { GenerateBidIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateBlogImageFilenameInput - Type contract for generate blog image filename input.
export type { GenerateBlogImageFilenameInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateBlogPostIdInput - Type contract for generate blog post id input.
export type { GenerateBlogPostIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateCarouselIdInput - Type contract for generate carousel id input.
export type { GenerateCarouselIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateCategoryIdInput - Type contract for generate category id input.
export type { GenerateCategoryIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateEventImageFilenameInput - Type contract for generate event image filename input.
export type { GenerateEventImageFilenameInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateFAQIdInput - Type contract for generate faqid input.
export type { GenerateFAQIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateHomepageSectionIdInput - Type contract for generate homepage section id input.
export type { GenerateHomepageSectionIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateOfferIdInput - Type contract for generate offer id input.
export type { GenerateOfferIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateOrderIdInput - Type contract for generate order id input.
export type { GenerateOrderIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GeneratePayoutIdInput - Type contract for generate payout id input.
export type { GeneratePayoutIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GeneratePreOrderIdInput - Type contract for generate pre order id input.
export type { GeneratePreOrderIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GeneratePreOrderImageFilenameInput - Type contract for generate pre order image filename input.
export type { GeneratePreOrderImageFilenameInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateProductIdInput - Type contract for generate product id input.
export type { GenerateProductIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateProductImageFilenameInput - Type contract for generate product image filename input.
export type { GenerateProductImageFilenameInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateProductVideoFilenameInput - Type contract for generate product video filename input.
export type { GenerateProductVideoFilenameInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateReviewIdInput - Type contract for generate review id input.
export type { GenerateReviewIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateReviewImageFilenameInput - Type contract for generate review image filename input.
export type { GenerateReviewImageFilenameInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateReviewVideoFilenameInput - Type contract for generate review video filename input.
export type { GenerateReviewVideoFilenameInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateRichTextImageFilenameInput - Type contract for generate rich text image filename input.
export type { GenerateRichTextImageFilenameInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenerateUserIdInput - Type contract for generate user id input.
export type { GenerateUserIdInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaField - Type contract for media field.
export type { MediaField } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaFieldInput - Type contract for media field input.
export type { MediaFieldInput } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaFieldType - Type contract for media field type.
export type { MediaFieldType } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaFilenameContext - Type contract for media filename context.
export type { MediaFilenameContext } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderGroup - Type contract for order group.
export type { OrderGroup } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderType - Type contract for order type.
export type { OrderType } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PaginationOptions - Type contract for pagination options.
export type { PaginationOptions } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PaginationResult - Type contract for pagination result.
export type { PaginationResult } from "./utils/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SortOrder - Type contract for sort order.
export type { SortOrder } from "./utils/index";

// ./constants/index
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// ACCOUNT_ENDPOINTS - Constant used across modules.
export { ACCOUNT_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// ADMIN_ENDPOINTS - Constant used across modules.
export { ADMIN_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// API_ENDPOINTS - Constant used across modules.
export { API_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// AUCTION_ENDPOINTS - Constant used across modules.
export { AUCTION_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// AUTH_ENDPOINTS - Constant used across modules.
export { AUTH_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// AUTH_ROUTES - Constant used across modules.
export { AUTH_ROUTES } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// BID_ENDPOINTS - Constant used across modules.
export { BID_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// BLOG_ENDPOINTS - Constant used across modules.
export { BLOG_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// CART_ENDPOINTS - Constant used across modules.
export { CART_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// CATEGORY_ENDPOINTS - Constant used across modules.
export { CATEGORY_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// CHAT_ENDPOINTS - Constant used across modules.
export { CHAT_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// CHECKOUT_ENDPOINTS - Constant used across modules.
export { CHECKOUT_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// COLLECTION_CACHE_PATHS - Constant used across modules.
export { COLLECTION_CACHE_PATHS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// COLLECTION_ENDPOINTS - Constant used across modules.
export { COLLECTION_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// CONSULTATION_ENDPOINTS - Constant used across modules.
export { CONSULTATION_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// COPILOT_ENDPOINTS - Constant used across modules.
export { COPILOT_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// CORPORATE_ENDPOINTS - Constant used across modules.
export { CORPORATE_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// DEMO_ENDPOINTS - Constant used across modules.
export { DEMO_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// EVENT_ENDPOINTS - Constant used across modules.
export { EVENT_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// FAQ_ENDPOINTS - Constant used across modules.
export { FAQ_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// HOMEPAGE_ENDPOINTS - Constant used across modules.
export { HOMEPAGE_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// LOGS_ENDPOINTS - Constant used across modules.
export { LOGS_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// LOYALTY_ENDPOINTS - Constant used across modules.
export { LOYALTY_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// MEDIA_ENDPOINTS - Constant used across modules.
export { MEDIA_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// NOTIFICATIONS_ENDPOINTS - Constant used across modules.
export { NOTIFICATIONS_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// ORDER_ENDPOINTS - Constant used across modules.
export { ORDER_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// PAYMENT_ENDPOINTS - Constant used across modules.
export { PAYMENT_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// PREORDER_ENDPOINTS - Constant used across modules.
export { PREORDER_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// PRODUCT_ENDPOINTS - Constant used across modules.
export { PRODUCT_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// PROFILE_STATS_ENDPOINTS - Constant used across modules.
export { PROFILE_STATS_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// PROMOTION_ENDPOINTS - Constant used across modules.
export { PROMOTION_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// PROTECTED_ROUTES - Constant used across modules.
export { PROTECTED_ROUTES } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// PUBLIC_ROUTES - Constant used across modules.
export { PUBLIC_ROUTES } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// REVIEW_ENDPOINTS - Constant used across modules.
export { REVIEW_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// ROUTES - Constant used across modules.
export { ROUTES } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// SEARCH_ENDPOINTS - Constant used across modules.
export { SEARCH_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// SELLER_ENDPOINTS - Constant used across modules.
export { SELLER_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// SITE_SETTINGS_ENDPOINTS - Constant used across modules.
export { SITE_SETTINGS_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// STORE_ENDPOINTS - Constant used across modules.
export { STORE_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// WISHLIST_ENDPOINTS - Constant used across modules.
export { WISHLIST_ENDPOINTS } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// resolveEndpoint - Helper for resolve endpoint.
export { resolveEndpoint } from "./constants/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// resolveEndpointFn - Helper for resolve endpoint fn.
export { resolveEndpointFn } from "./constants/index";

// ./contracts/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// _resetCollectionHooks - Shared export for reset collection hooks.
export { _resetCollectionHooks } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// _resetFormFields - Model for reset form fields.
export { _resetFormFields } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// _resetProviders - Shared export for reset providers.
export { _resetProviders } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// arrayRemove - Shared export for array remove.
export { arrayRemove } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// arrayUnion - Shared export for array union.
export { arrayUnion } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// deleteField - Helper for delete field.
export { deleteField } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getClientAuthProvider - Helper for get client auth provider.
export { getClientAuthProvider } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getClientPaymentGateway - Helper for get client payment gateway.
export { getClientPaymentGateway } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getClientRealtimeProvider - Helper for get client realtime provider.
export { getClientRealtimeProvider } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getClientSessionAdapter - Helper for get client session adapter.
export { getClientSessionAdapter } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getCollectionHooks - Helper for get collection hooks.
export { getCollectionHooks } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getProviders - Helper for get providers.
export { getProviders } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getRegisteredPaymentGateways - Helper for get registered payment gateways.
export { getRegisteredPaymentGateways } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// increment - Shared export for increment.
export { increment } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// mergeFilterDefinitions - Helper for merge filter definitions.
export { mergeFilterDefinitions } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// mergeSortDefinitions - Helper for merge sort definitions.
export { mergeSortDefinitions } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// registerClientAuthProvider - Helper for register client auth provider.
export { registerClientAuthProvider } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// registerClientPaymentGateway - Helper for register client payment gateway.
export { registerClientPaymentGateway } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// registerClientRealtimeProvider - Helper for register client realtime provider.
export { registerClientRealtimeProvider } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// registerClientSessionAdapter - Helper for register client session adapter.
export { registerClientSessionAdapter } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// registerFieldOps - Helper for register field ops.
export { registerFieldOps } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// registerFormFields - Helper for register form fields.
export { registerFormFields } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// registerProviders - Helper for register providers.
export { registerProviders } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// removeCollectionHooks - Helper for remove collection hooks.
export { removeCollectionHooks } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// removeFormFields - Helper for remove form fields.
export { removeFormFields } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// resolveFormFields - Helper for resolve form fields.
export { resolveFormFields } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// serverTimestamp - Shared export for server timestamp.
export { serverTimestamp } from "./contracts/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// setCollectionHooks - Helper for set collection hooks.
export { setCollectionHooks } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdapterAuthUser - Type contract for adapter auth user.
export type { AdapterAuthUser } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ApiRouteStub - Type contract for api route stub.
export type { ApiRouteStub } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AuthPayload - Type contract for auth payload.
export type { AuthPayload } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AuthUnsubscribe - Type contract for auth unsubscribe.
export type { AuthUnsubscribe } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AuthUser - Type contract for auth user.
export type { AuthUser } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ColumnExtensionOpts - Type contract for column extension opts.
export type { ColumnExtensionOpts } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateShipmentInput - Type contract for create shipment input.
export type { CreateShipmentInput } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateUserInput - Type contract for create user input.
export type { CreateUserInput } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EmailAttachment - Type contract for email attachment.
export type { EmailAttachment } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EmailOptions - Type contract for email options.
export type { EmailOptions } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EmailResult - Type contract for email result.
export type { EmailResult } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventHandler - Type contract for event handler.
export type { EventHandler } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ExtraFieldRenderArgs - Type contract for extra field render args.
export type { ExtraFieldRenderArgs } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ExtraFormField - Type contract for extra form field.
export type { ExtraFormField } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FeatureExtension - Type contract for feature extension.
export type { FeatureExtension } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FeatureManifest - Type contract for feature manifest.
export type { FeatureManifest } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FeaturesConfig - Type contract for features config.
export type { FeaturesConfig } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FieldSentinel - Type contract for field sentinel.
export type { FieldSentinel } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FilterDefinition - Type contract for filter definition.
export type { FilterDefinition } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FilterOption - Type contract for filter option.
export type { FilterOption } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FilterType - Type contract for filter type.
export type { FilterType } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GatewayPaymentResponse - Type contract for gateway payment response.
export type { GatewayPaymentResponse } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GenericListResponse - Type contract for generic list response.
export type { GenericListResponse } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IAuthProvider - Type contract for iauth provider.
export type { IAuthProvider } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ICacheProvider - Type contract for icache provider.
export type { ICacheProvider } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IClientAuthProvider - Type contract for iclient auth provider.
export type { IClientAuthProvider } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IClientPaymentGateway - Type contract for iclient payment gateway.
export type { IClientPaymentGateway } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IClientRealtimeProvider - Type contract for iclient realtime provider.
export type { IClientRealtimeProvider } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IClientSessionAdapter - Type contract for iclient session adapter.
export type { IClientSessionAdapter } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IDbProvider - Type contract for idb provider.
export type { IDbProvider } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IEmailProvider - Type contract for iemail provider.
export type { IEmailProvider } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IEventBus - Type contract for ievent bus.
export type { IEventBus } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IFieldOps - Type contract for ifield ops.
export type { IFieldOps } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IPaymentProvider - Type contract for ipayment provider.
export type { IPaymentProvider } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IQueueProvider - Type contract for iqueue provider.
export type { IQueueProvider } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IReadRepository - Type contract for iread repository.
export type { IReadRepository } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IRealtimeRepository - Type contract for irealtime repository.
export type { IRealtimeRepository } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IRepository - Type contract for irepository.
export type { IRepository } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ISearchProvider - Type contract for isearch provider.
export type { ISearchProvider } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ISessionProvider - Type contract for isession provider.
export type { ISessionProvider } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IShippingProvider - Type contract for ishipping provider.
export type { IShippingProvider } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IStorageProvider - Type contract for istorage provider.
export type { IStorageProvider } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IStyleAdapter - Type contract for istyle adapter.
export type { IStyleAdapter } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IWriteRepository - Type contract for iwrite repository.
export type { IWriteRepository } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LayoutSlots - Type contract for layout slots.
export type { LayoutSlots } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NavItem - Type contract for nav item.
export type { NavItem } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OpenGatewayOptions - Type contract for open gateway options.
export type { OpenGatewayOptions } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PagedResult - Type contract for paged result.
export type { PagedResult } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PaymentCapture - Type contract for payment capture.
export type { PaymentCapture } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PaymentOrder - Type contract for payment order.
export type { PaymentOrder } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProviderRegistry - Type contract for provider registry.
export type { ProviderRegistry } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// QueueJob - Type contract for queue job.
export type { QueueJob } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RealtimeSnapshot - Type contract for realtime snapshot.
export type { RealtimeSnapshot } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// Refund - Type contract for refund.
export type { Refund } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RepositoryLifecycleHooks - Type contract for repository lifecycle hooks.
export type { RepositoryLifecycleHooks } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RouteStub - Type contract for route stub.
export type { RouteStub } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchHit - Type contract for search hit.
export type { SearchHit } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchOptions - Type contract for search options.
export type { SearchOptions } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchResult - Type contract for search result.
export type { SearchResult } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ServiceabilityResult - Type contract for serviceability result.
export type { ServiceabilityResult } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// Shipment - Type contract for shipment.
export type { Shipment } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShippingAddress - Type contract for shipping address.
export type { ShippingAddress } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SieveQuery - Type contract for sieve query.
export type { SieveQuery } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SiteConfig - Type contract for site config.
export type { SiteConfig } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SortDefinition - Type contract for sort definition.
export type { SortDefinition } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StorageFile - Type contract for storage file.
export type { StorageFile } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SuggestOptions - Type contract for suggest options.
export type { SuggestOptions } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TableColumn - Type contract for table column.
export type { TableColumn } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TrackingEvent - Type contract for tracking event.
export type { TrackingEvent } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TrackingInfo - Type contract for tracking info.
export type { TrackingInfo } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// Unsubscribe - Type contract for unsubscribe.
export type { Unsubscribe } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UploadOptions - Type contract for upload options.
export type { UploadOptions } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// WhereOp - Type contract for where op.
export type { WhereOp } from "./contracts/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// WithTransformOpts - Type contract for with transform opts.
export type { WithTransformOpts } from "./contracts/index";

// ./validation/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ZodSetup - Shared export for zod setup.
export { ZodSetup } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// addPhoneSchema - Helper for add phone schema.
export { addPhoneSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// addressSchema - Model for address schema.
export { addressSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// calculatePasswordStrength - Helper for calculate password strength.
export { calculatePasswordStrength } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// changePasswordSchema - Model for change password schema.
export { changePasswordSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// cropDataSchema - Model for crop data schema.
export { cropDataSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// dateStringSchema - Model for date string schema.
export { dateStringSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// deleteAccountSchema - Helper for delete account schema.
export { deleteAccountSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// emailSchema - Model for email schema.
export { emailSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// exactLength - Shared export for exact length.
export { exactLength } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// extractCountryCode - Shared export for extract country code.
export { extractCountryCode } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// formatPhone - Helper for format phone.
export { formatPhone } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// formatZodErrors - Helper for format zod errors.
export { formatZodErrors } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// inRange - Shared export for in range.
export { inRange } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isCommonPassword - Shared export for is common password.
export { isCommonPassword } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isDisposableEmail - Shared export for is disposable email.
export { isDisposableEmail } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isExternalUrl - Shared export for is external url.
export { isExternalUrl } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isInList - Shared export for is in list.
export { isInList } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isNumeric - Shared export for is numeric.
export { isNumeric } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isRequired - Shared export for is required.
export { isRequired } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isValidEmail - Shared export for is valid email.
export { isValidEmail } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isValidEmailDomain - Shared export for is valid email domain.
export { isValidEmailDomain } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isValidIndianPincode - Shared export for is valid indian pincode.
export { isValidIndianPincode } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isValidMobile - Shared export for is valid mobile.
export { isValidMobile } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isValidPhone - Shared export for is valid phone.
export { isValidPhone } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isValidUrl - Shared export for is valid url.
export { isValidUrl } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isValidUrlWithProtocol - Shared export for is valid url with protocol.
export { isValidUrlWithProtocol } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// matchesPattern - Shared export for matches pattern.
export { matchesPattern } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// maxLength - Shared export for max length.
export { maxLength } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// mediaUrlSchema - Model for media url schema.
export { mediaUrlSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// meetsPasswordRequirements - Shared export for meets password requirements.
export { meetsPasswordRequirements } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// minLength - Shared export for min length.
export { minLength } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// normalizeEmail - Helper for normalize email.
export { normalizeEmail } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// normalizePhone - Helper for normalize phone.
export { normalizePhone } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// objectIdSchema - Model for object id schema.
export { objectIdSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// paginationQuerySchema - Model for pagination query schema.
export { paginationQuerySchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// passwordSchema - Model for password schema.
export { passwordSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// phoneSchema - Model for phone schema.
export { phoneSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// resetPasswordSchema - Model for reset password schema.
export { resetPasswordSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// sanitizeUrl - Shared export for sanitize url.
export { sanitizeUrl } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// sendVerificationSchema - Model for send verification schema.
export { sendVerificationSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// setupZodErrorMap - Model for setup zod error map.
export { setupZodErrorMap } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// trimDataSchema - Model for trim data schema.
export { trimDataSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// urlSchema - Model for url schema.
export { urlSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// validateRequestBody - Helper for validate request body.
export { validateRequestBody } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// verifyPhoneSchema - Model for verify phone schema.
export { verifyPhoneSchema } from "./validation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// zodErrorMap - Model for zod error map.
export { zodErrorMap } from "./validation/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PasswordRequirements - Type contract for password requirements.
export type { PasswordRequirements } from "./validation/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PasswordStrength - Type contract for password strength.
export type { PasswordStrength } from "./validation/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ZodSetupProps - Type contract for zod setup props.
export type { ZodSetupProps } from "./validation/index";

// ./errors/index
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// ApiError - Model for api error.
export { ApiError } from "./errors/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// AppError - Model for app error.
export { AppError } from "./errors/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// AuthenticationError - Model for authentication error.
export { AuthenticationError } from "./errors/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// AuthorizationError - Model for authorization error.
export { AuthorizationError } from "./errors/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// DatabaseError - Model for database error.
export { DatabaseError } from "./errors/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// ERROR_CODES - Constant used across modules.
export { ERROR_CODES } from "./errors/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// ERROR_MESSAGES - Constant used across modules.
export { ERROR_MESSAGES } from "./errors/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// NotFoundError - Model for not found error.
export { NotFoundError } from "./errors/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// ValidationError - Model for validation error.
export { ValidationError } from "./errors/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// handleApiError - Model for handle api error.
export { handleApiError } from "./errors/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// isAppError - Model for is app error.
export { isAppError } from "./errors/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logError - Helper for log error.
export { logError } from "./errors/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ErrorCode - Type contract for error code.
export type { ErrorCode } from "./errors/index";

// ./seo/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SEO_CONFIG - Constant used across modules.
export { SEO_CONFIG } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// aggregateRatingJsonLd - Shared export for aggregate rating json ld.
export { aggregateRatingJsonLd } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// auctionJsonLd - Shared export for auction json ld.
export { auctionJsonLd } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// blogPostJsonLd - Shared export for blog post json ld.
export { blogPostJsonLd } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// breadcrumbJsonLd - Shared export for breadcrumb json ld.
export { breadcrumbJsonLd } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createSeoConfig - Helper for create seo config.
export { createSeoConfig } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// faqJsonLd - Shared export for faq json ld.
export { faqJsonLd } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// generateAuctionMetadata - Helper for generate auction metadata.
export { generateAuctionMetadata } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// generateBlogMetadata - Helper for generate blog metadata.
export { generateBlogMetadata } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// generateCategoryMetadata - Helper for generate category metadata.
export { generateCategoryMetadata } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// generateMetadata - Helper for generate metadata.
export { generateMetadata } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// generateProductMetadata - Helper for generate product metadata.
export { generateProductMetadata } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// generateProfileMetadata - Helper for generate profile metadata.
export { generateProfileMetadata } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// generateSearchMetadata - Helper for generate search metadata.
export { generateSearchMetadata } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// organizationJsonLd - Shared export for organization json ld.
export { organizationJsonLd } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// productJsonLd - Shared export for product json ld.
export { productJsonLd } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// reviewJsonLd - Shared export for review json ld.
export { reviewJsonLd } from "./seo/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// searchBoxJsonLd - Shared export for search box json ld.
export { searchBoxJsonLd } from "./seo/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AuctionSeoInput - Type contract for auction seo input.
export type { AuctionSeoInput } from "./seo/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogPostJsonLdInput - Type contract for blog post json ld input.
export type { BlogPostJsonLdInput } from "./seo/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogSeoInput - Type contract for blog seo input.
export type { BlogSeoInput } from "./seo/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BreadcrumbJsonLdItem - Type contract for breadcrumb json ld item.
export type { BreadcrumbJsonLdItem } from "./seo/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategorySeoInput - Type contract for category seo input.
export type { CategorySeoInput } from "./seo/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FaqJsonLdInput - Type contract for faq json ld input.
export type { FaqJsonLdInput } from "./seo/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductJsonLdInput - Type contract for product json ld input.
export type { ProductJsonLdInput } from "./seo/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductSeoInput - Type contract for product seo input.
export type { ProductSeoInput } from "./seo/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewJsonLdInput - Type contract for review json ld input.
export type { ReviewJsonLdInput } from "./seo/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeoConfig - Type contract for seo config.
export type { SeoConfig } from "./seo/index";

// ./cli/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FEATURE_SUBPATH_MAP - Constant used across modules.
export { FEATURE_SUBPATH_MAP } from "./cli/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// mergeFeatureMessages - Helper for merge feature messages.
export { mergeFeatureMessages } from "./cli/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// withFeatures - Shared export for with features.
export { withFeatures } from "./cli/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NextConfig - Type contract for next config.
export type { NextConfig } from "./cli/index";

// ./core/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// COPILOT_LOGS_COLLECTION - Constant used across modules.
export { COPILOT_LOGS_COLLECTION } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// COPILOT_LOGS_INDEXED_FIELDS - Constant used across modules.
export { COPILOT_LOGS_INDEXED_FIELDS } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CacheManager - Shared export for cache manager.
export { CacheManager } from "./core/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// CopilotLogRepository - Shared export for copilot log repository.
export { CopilotLogRepository } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EventBus - Shared export for event bus.
export { EventBus } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FEATURE_FLAGS - Constant used across modules.
export { FEATURE_FLAGS } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Logger - Shared export for logger.
export { Logger } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MUTATION_EVENTS - Constant used across modules.
export { MUTATION_EVENTS } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// NEWSLETTER_SUBSCRIBERS_COLLECTION - Constant used across modules.
export { NEWSLETTER_SUBSCRIBERS_COLLECTION } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// NEWSLETTER_SUBSCRIBER_FIELDS - Constant used across modules.
export { NEWSLETTER_SUBSCRIBER_FIELDS } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// NEWSLETTER_SUBSCRIBER_INDEXED_FIELDS - Constant used across modules.
export { NEWSLETTER_SUBSCRIBER_INDEXED_FIELDS } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Queue - Shared export for queue.
export { Queue } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SITE_CONFIG - Constant used across modules.
export { SITE_CONFIG } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StorageManager - Shared export for storage manager.
export { StorageManager } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// _resetActionMiddleware - Shared export for reset action middleware.
export { _resetActionMiddleware } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// cacheManager - Shared export for cache manager.
export { cacheManager } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// configureMarketDefaults - Shared export for configure market defaults.
export { configureMarketDefaults } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// copilotLogQueryHelpers - Shared export for copilot log query helpers.
export { copilotLogQueryHelpers } from "./core/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// copilotLogRepository - Shared export for copilot log repository.
export { copilotLogRepository } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createServerAction - Helper for create server action.
export { createServerAction } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createSiteConfig - Helper for create site config.
export { createSiteConfig } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// emitMutation - Shared export for emit mutation.
export { emitMutation } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// eventBus - Shared export for event bus.
export { eventBus } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getDefaultCountry - Helper for get default country.
export { getDefaultCountry } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getDefaultCurrency - Helper for get default currency.
export { getDefaultCurrency } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getDefaultCurrencySymbol - Helper for get default currency symbol.
export { getDefaultCurrencySymbol } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getDefaultLocale - Helper for get default locale.
export { getDefaultLocale } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getDefaultPhonePrefix - Helper for get default phone prefix.
export { getDefaultPhonePrefix } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getDefaultTimezone - Helper for get default timezone.
export { getDefaultTimezone } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getMarketProfile - Helper for get market profile.
export { getMarketProfile } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// inferMutationEvent - Helper for infer mutation event.
export { inferMutationEvent } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// invalidateIntegrationKeysCache - Shared export for invalidate integration keys cache.
export { invalidateIntegrationKeysCache } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// logger - Shared export for logger.
export { logger } from "./core/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// newsletterRepository - Shared export for newsletter repository.
export { newsletterRepository } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// onMutation - Shared export for on mutation.
export { onMutation } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// resetMarketDefaults - Shared export for reset market defaults.
export { resetMarketDefaults } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// resolveSiteConfig - Helper for resolve site config.
export { resolveSiteConfig } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// setActionMiddleware - Helper for set action middleware.
export { setActionMiddleware } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// storageManager - Shared export for storage manager.
export { storageManager } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// subscribeNewsletter - Shared export for subscribe newsletter.
export { subscribeNewsletter } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// unitOfWork - Shared export for unit of work.
export { unitOfWork } from "./core/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useSiteSettings - React hook for use site settings.
export { useSiteSettings } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ActionMiddleware - Type contract for action middleware.
export type { ActionMiddleware } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ActionMiddlewareContext - Type contract for action middleware context.
export type { ActionMiddlewareContext } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ActionResult - Type contract for action result.
export type { ActionResult } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CacheEntry - Type contract for cache entry.
export type { CacheEntry } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CacheOptions - Type contract for cache options.
export type { CacheOptions } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CopilotFeedback - Type contract for copilot feedback.
export type { CopilotFeedback } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CopilotLogCreateInput - Type contract for copilot log create input.
export type { CopilotLogCreateInput } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CopilotLogDocument - Type contract for copilot log document.
export type { CopilotLogDocument } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventSubscription - Type contract for event subscription.
export type { EventSubscription } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LogEntry - Type contract for log entry.
export type { LogEntry } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LogLevel - Type contract for log level.
export type { LogLevel } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LoggerOptions - Type contract for logger options.
export type { LoggerOptions } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MarketProfile - Type contract for market profile.
export type { MarketProfile } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MutationEventPayload - Type contract for mutation event payload.
export type { MutationEventPayload } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NewsletterSubscriberCreateInput - Type contract for newsletter subscriber create input.
export type { NewsletterSubscriberCreateInput } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NewsletterSubscriberDocument - Type contract for newsletter subscriber document.
export type { NewsletterSubscriberDocument } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NewsletterSubscriberUpdateInput - Type contract for newsletter subscriber update input.
export type { NewsletterSubscriberUpdateInput } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// QueueOptions - Type contract for queue options.
export type { QueueOptions } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ResolvedKeys - Type contract for resolved keys.
export type { ResolvedKeys } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RuntimeSiteConfig - Type contract for runtime site config.
export type { RuntimeSiteConfig } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ServerActionOptions - Type contract for server action options.
export type { ServerActionOptions } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StorageOptions - Type contract for storage options.
export type { StorageOptions } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StorageType - Type contract for storage type.
export type { StorageType } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SubscribeNewsletterActionInput - Type contract for subscribe newsletter action input.
export type { SubscribeNewsletterActionInput } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SupportedNewsletterSource - Type contract for supported newsletter source.
export type { SupportedNewsletterSource } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// Task - Type contract for task.
export type { Task } from "./core/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UnitOfWork - Type contract for unit of work.
export type { UnitOfWork } from "./core/index";

// ./security/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ADDRESS_PII_FIELDS - Constant used across modules.
export { ADDRESS_PII_FIELDS } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BID_PII_FIELDS - Constant used across modules.
export { BID_PII_FIELDS } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CHAT_PII_FIELDS - Constant used across modules.
export { CHAT_PII_FIELDS } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Can - Shared export for can.
export { Can } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DEFAULT_ROLES - Constant used across modules.
export { DEFAULT_ROLES } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ENC_PREFIX - Constant used across modules.
export { ENC_PREFIX } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EVENT_ENTRY_PII_FIELDS - Constant used across modules.
export { EVENT_ENTRY_PII_FIELDS } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HMAC_PREFIX - Constant used across modules.
export { HMAC_PREFIX } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// NEWSLETTER_PII_FIELDS - Constant used across modules.
export { NEWSLETTER_PII_FIELDS } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// NEWSLETTER_PII_INDEX_MAP - Constant used across modules.
export { NEWSLETTER_PII_INDEX_MAP } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// OFFER_PII_FIELDS - Constant used across modules.
export { OFFER_PII_FIELDS } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ORDER_PII_FIELDS - Constant used across modules.
export { ORDER_PII_FIELDS } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PAYOUT_PII_FIELDS - Constant used across modules.
export { PAYOUT_PII_FIELDS } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PAYOUT_PII_INDEX_MAP - Constant used across modules.
export { PAYOUT_PII_INDEX_MAP } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// REVIEW_PII_FIELDS - Constant used across modules.
export { REVIEW_PII_FIELDS } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// REVIEW_PII_INDEX_MAP - Constant used across modules.
export { REVIEW_PII_INDEX_MAP } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// RateLimitPresets - Shared export for rate limit presets.
export { RateLimitPresets } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TOKEN_PII_FIELDS - Constant used across modules.
export { TOKEN_PII_FIELDS } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TOKEN_PII_INDEX_MAP - Constant used across modules.
export { TOKEN_PII_INDEX_MAP } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// USER_PII_FIELDS - Constant used across modules.
export { USER_PII_FIELDS } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// USER_PII_INDEX_MAP - Constant used across modules.
export { USER_PII_INDEX_MAP } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// addPiiIndices - Helper for add pii indices.
export { addPiiIndices } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// applyRateLimit - Helper for apply rate limit.
export { applyRateLimit } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildCSP - Helper for build csp.
export { buildCSP } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// canChangeRole - Model for can change role.
export { canChangeRole } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// clearRateLimitStore - Shared export for clear rate limit store.
export { clearRateLimitStore } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createRbacHook - Helper for create rbac hook.
export { createRbacHook } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createRbacMiddleware - Helper for create rbac middleware.
export { createRbacMiddleware } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createRequirePermission - Helper for create require permission.
export { createRequirePermission } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createRequirePermissionSync - Helper for create require permission sync.
export { createRequirePermissionSync } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// decryptPayoutBankAccount - Helper for decrypt payout bank account.
export { decryptPayoutBankAccount } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// decryptPayoutDetails - Helper for decrypt payout details.
export { decryptPayoutDetails } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// decryptPii - Helper for decrypt pii.
export { decryptPii } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// decryptPiiFields - Helper for decrypt pii fields.
export { decryptPiiFields } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// decryptSecret - Helper for decrypt secret.
export { decryptSecret } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// decryptShippingAddress - Helper for decrypt shipping address.
export { decryptShippingAddress } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// decryptShippingConfig - Helper for decrypt shipping config.
export { decryptShippingConfig } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// decryptValue - Helper for decrypt value.
export { decryptValue } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// encryptPayoutBankAccount - Helper for encrypt payout bank account.
export { encryptPayoutBankAccount } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// encryptPayoutDetails - Helper for encrypt payout details.
export { encryptPayoutDetails } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// encryptPii - Helper for encrypt pii.
export { encryptPii } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// encryptPiiFields - Helper for encrypt pii fields.
export { encryptPiiFields } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// encryptSecret - Helper for encrypt secret.
export { encryptSecret } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// encryptShippingAddress - Helper for encrypt shipping address.
export { encryptShippingAddress } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// encryptShippingConfig - Helper for encrypt shipping config.
export { encryptShippingConfig } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// encryptValue - Helper for encrypt value.
export { encryptValue } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// generateNonce - Helper for generate nonce.
export { generateNonce } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getPiiConfigError - Helper for get pii config error.
export { getPiiConfigError } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getRoleLevel - Helper for get role level.
export { getRoleLevel } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// hasAllPermissions - Shared export for has all permissions.
export { hasAllPermissions } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// hasAnyPermission - Model for has any permission.
export { hasAnyPermission } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// hasPermission - Model for has permission.
export { hasPermission } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// hmacBlindIndex - Shared export for hmac blind index.
export { hmacBlindIndex } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isPiiEncrypted - Shared export for is pii encrypted.
export { isPiiEncrypted } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isSecretEncrypted - Shared export for is secret encrypted.
export { isSecretEncrypted } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// maskEmail - Helper for mask email.
export { maskEmail } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// maskName - Helper for mask name.
export { maskName } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// maskOfferForSeller - Helper for mask offer for seller.
export { maskOfferForSeller } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// maskPublicBid - Helper for mask public bid.
export { maskPublicBid } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// maskPublicEventEntry - Helper for mask public event entry.
export { maskPublicEventEntry } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// maskPublicReview - Helper for mask public review.
export { maskPublicReview } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// maskSecret - Helper for mask secret.
export { maskSecret } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// piiBlindIndex - Shared export for pii blind index.
export { piiBlindIndex } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// rateLimit - Shared export for rate limit.
export { rateLimit } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// rateLimitByIdentifier - Shared export for rate limit by identifier.
export { rateLimitByIdentifier } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// redactPii - Shared export for redact pii.
export { redactPii } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// requireActiveAccount - Shared export for require active account.
export { requireActiveAccount } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// requireAuth - Shared export for require auth.
export { requireAuth } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// requireEmailVerified - Shared export for require email verified.
export { requireEmailVerified } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// requireOwnership - Shared export for require ownership.
export { requireOwnership } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// requireRole - Model for require role.
export { requireRole } from "./security/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// resolvePermissions - Helper for resolve permissions.
export { resolvePermissions } from "./security/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// Permission - Type contract for permission.
export type { Permission } from "./security/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RateLimitConfig - Type contract for rate limit config.
export type { RateLimitConfig } from "./security/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RateLimitResult - Type contract for rate limit result.
export type { RateLimitResult } from "./security/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RbacConfig - Type contract for rbac config.
export type { RbacConfig } from "./security/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RbacHookReturn - Type contract for rbac hook return.
export type { RbacHookReturn } from "./security/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ResolvedUser - Type contract for resolved user.
export type { ResolvedUser } from "./security/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RoleDefinition - Type contract for role definition.
export type { RoleDefinition } from "./security/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserRole - Type contract for user role.
export type { UserRole } from "./security/index";

// ./repositories/index
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// AddressRepository - Shared export for address repository.
export { AddressRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// BaseRepository - Shared export for base repository.
export { BaseRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// BlogRepository - Shared export for blog repository.
export { BlogRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// CartRepository - Shared export for cart repository.
export { CartRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// CategoriesRepository - Shared export for categories repository.
export { CategoriesRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// EmailVerificationTokenRepository - Shared export for email verification token repository.
export { EmailVerificationTokenRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// EventEntriesRepository - Shared export for event entries repository.
export { EventEntriesRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// EventEntryRepository - Shared export for event entry repository.
export { EventEntryRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// EventRepository - Shared export for event repository.
export { EventRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// EventsRepository - Shared export for events repository.
export { EventsRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// FAQsRepository - Shared export for faqs repository.
export { FAQsRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// FirebaseFAQsRepository - Shared export for firebase faqs repository.
export { FirebaseFAQsRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// NewsletterRepository - Shared export for newsletter repository.
export { NewsletterRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// OfferRepository - Shared export for offer repository.
export { OfferRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// OrderRepository - Shared export for order repository.
export { OrderRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// OrdersRepository - Shared export for orders repository.
export { OrdersRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// PasswordResetTokenRepository - Shared export for password reset token repository.
export { PasswordResetTokenRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// ProductRepository - Shared export for product repository.
export { ProductRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// ProductsRepository - Shared export for products repository.
export { ProductsRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// ReviewRepository - Shared export for review repository.
export { ReviewRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// ReviewsRepository - Shared export for reviews repository.
export { ReviewsRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// SessionRepository - Shared export for session repository.
export { SessionRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// SmsCounterRepository - Shared export for sms counter repository.
export { SmsCounterRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// StoreRepository - Shared export for store repository.
export { StoreRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// UserRepository - Shared export for user repository.
export { UserRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// addressRepository - Shared export for address repository.
export { addressRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// bidRepository - Shared export for bid repository.
export { bidRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// blogRepository - Shared export for blog repository.
export { blogRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// carouselRepository - Shared export for carousel repository.
export { carouselRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// cartRepository - Shared export for cart repository.
export { cartRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// categoriesRepository - Shared export for categories repository.
export { categoriesRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// chatRepository - Shared export for chat repository.
export { chatRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// couponsRepository - Shared export for coupons repository.
export { couponsRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// emailVerificationTokenRepository - Shared export for email verification token repository.
export { emailVerificationTokenRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// eventEntryRepository - Shared export for event entry repository.
export { eventEntryRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// eventRepository - Shared export for event repository.
export { eventRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// faqsRepository - Shared export for faqs repository.
export { faqsRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// homepageSectionsRepository - Shared export for homepage sections repository.
export { homepageSectionsRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// notificationRepository - Shared export for notification repository.
export { notificationRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// offerRepository - Shared export for offer repository.
export { offerRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// orderRepository - Shared export for order repository.
export { orderRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// passwordResetTokenRepository - Shared export for password reset token repository.
export { passwordResetTokenRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// payoutRepository - Shared export for payout repository.
export { payoutRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// productRepository - Shared export for product repository.
export { productRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// reviewRepository - Shared export for review repository.
export { reviewRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// sessionRepository - Shared export for session repository.
export { sessionRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// siteSettingsRepository - Shared export for site settings repository.
export { siteSettingsRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// smsCounterRepository - Shared export for sms counter repository.
export { smsCounterRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// storeAddressRepository - Shared export for store address repository.
export { storeAddressRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// storeRepository - Shared export for store repository.
export { storeRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// tokenRepository - Shared export for token repository.
export { tokenRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// userRepository - Shared export for user repository.
export { userRepository } from "./repositories/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// wishlistRepository - Shared export for wishlist repository.
export { wishlistRepository } from "./repositories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NewsletterListModel - Type contract for newsletter list model.
export type { NewsletterListModel } from "./repositories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// WishlistItem - Type contract for wishlist item.
export type { WishlistItem } from "./repositories/index";

// ./seed/index
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// ADDRESS_FIXTURES - Constant used across modules.
export { ADDRESS_FIXTURES } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// BID_FIXTURES - Constant used across modules.
export { BID_FIXTURES } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// CART_FIXTURES - Constant used across modules.
export { CART_FIXTURES } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// COUPON_FIXTURES - Constant used across modules.
export { COUPON_FIXTURES } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// DEFAULT_CATEGORIES - Constant used across modules.
export { DEFAULT_CATEGORIES } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// DEFAULT_FAQS - Constant used across modules.
export { DEFAULT_FAQS } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// DEFAULT_HOMEPAGE_SECTIONS - Constant used across modules.
export { DEFAULT_HOMEPAGE_SECTIONS } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// NOTIFICATION_FIXTURES - Constant used across modules.
export { NOTIFICATION_FIXTURES } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// PAYOUT_FIXTURES - Constant used across modules.
export { PAYOUT_FIXTURES } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// PRODUCT_FIXTURES - Constant used across modules.
export { PRODUCT_FIXTURES } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// SESSION_COLLECTION - Constant used across modules.
export { SESSION_COLLECTION } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// SESSION_FIXTURES - Constant used across modules.
export { SESSION_FIXTURES } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// STORE_FIXTURES - Constant used across modules.
export { STORE_FIXTURES } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// USER_FIXTURES - Constant used across modules.
export { USER_FIXTURES } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// addressesSeedData - Model for addresses seed data.
export { addressesSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// assertPiiRoundTrip - Shared export for assert pii round trip.
export { assertPiiRoundTrip } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// bidsSeedData - Model for bids seed data.
export { bidsSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// blogPostsSeedData - Model for blog posts seed data.
export { blogPostsSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// carouselSlidesSeedData - Model for carousel slides seed data.
export { carouselSlidesSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// cartsSeedData - Model for carts seed data.
export { cartsSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// categoriesSeedData - Model for categories seed data.
export { categoriesSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// couponsSeedData - Model for coupons seed data.
export { couponsSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// demoSeed - Shared export for demo seed.
export { demoSeed } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// eventEntriesSeedData - Model for event entries seed data.
export { eventEntriesSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// eventsSeedData - Model for events seed data.
export { eventsSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// faqSeedData - Model for faq seed data.
export { faqSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// formatSeedPrice - Helper for format seed price.
export { formatSeedPrice } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// generateMergedFirestoreIndexFile - Helper for generate merged firestore index file.
export { generateMergedFirestoreIndexFile } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getSeedLocale - Helper for get seed locale.
export { getSeedLocale } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// homepageSectionsSeedData - Model for homepage sections seed data.
export { homepageSectionsSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeAddress - Shared export for make address.
export { makeAddress } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeBid - Shared export for make bid.
export { makeBid } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeBlogPost - Shared export for make blog post.
export { makeBlogPost } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeCarouselSlide - Shared export for make carousel slide.
export { makeCarouselSlide } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeCart - Shared export for make cart.
export { makeCart } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeCartItem - Model for make cart item.
export { makeCartItem } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeCategory - Shared export for make category.
export { makeCategory } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeCoupon - Shared export for make coupon.
export { makeCoupon } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeFaq - Shared export for make faq.
export { makeFaq } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeFullAddress - Shared export for make full address.
export { makeFullAddress } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeFullCart - Shared export for make full cart.
export { makeFullCart } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeFullCoupon - Shared export for make full coupon.
export { makeFullCoupon } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeFullNotification - Shared export for make full notification.
export { makeFullNotification } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeFullPayout - Shared export for make full payout.
export { makeFullPayout } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeFullProduct - Shared export for make full product.
export { makeFullProduct } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeFullStore - Shared export for make full store.
export { makeFullStore } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeFullUser - Shared export for make full user.
export { makeFullUser } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeHomepageSection - Shared export for make homepage section.
export { makeHomepageSection } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeNotification - Shared export for make notification.
export { makeNotification } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeOrder - Shared export for make order.
export { makeOrder } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makePayout - Shared export for make payout.
export { makePayout } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeProduct - Shared export for make product.
export { makeProduct } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeReview - Shared export for make review.
export { makeReview } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeRevokedSession - Shared export for make revoked session.
export { makeRevokedSession } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeSeedPhone - Shared export for make seed phone.
export { makeSeedPhone } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeSession - Shared export for make session.
export { makeSession } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeStore - Shared export for make store.
export { makeStore } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeUser - Shared export for make user.
export { makeUser } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeWinningBid - Shared export for make winning bid.
export { makeWinningBid } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// mergeFirestoreIndices - Helper for merge firestore indices.
export { mergeFirestoreIndices } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// notificationsSeedData - Model for notifications seed data.
export { notificationsSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// ordersSeedData - Model for orders seed data.
export { ordersSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// payoutsSeedData - Model for payouts seed data.
export { payoutsSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// productsSeedData - Model for products seed data.
export { productsSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// registerSeedLocale - Helper for register seed locale.
export { registerSeedLocale } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// reviewsSeedData - Model for reviews seed data.
export { reviewsSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// runSeed - Helper for run seed.
export { runSeed } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// seedForTest - Shared export for seed for test.
export { seedForTest } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sessionsSeedData - Model for sessions seed data.
export { sessionsSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// siteSettingsSeedData - Model for site settings seed data.
export { siteSettingsSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// storeAddressesSeedData - Model for store addresses seed data.
export { storeAddressesSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// storesSeedData - Model for stores seed data.
export { storesSeedData } from "./seed/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// usersSeedData - Model for users seed data.
export { usersSeedData } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CouponDiscountType - Type contract for coupon discount type.
export type { CouponDiscountType } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FirestoreIndexConfig - Type contract for firestore index config.
export type { FirestoreIndexConfig } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HomepageSectionType - Type contract for homepage section type.
export type { HomepageSectionType } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NotificationType - Type contract for notification type.
export type { NotificationType } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PayoutStatus - Type contract for payout status.
export type { PayoutStatus } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedAddressDocument - Type contract for seed address document.
export type { SeedAddressDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedBaseOrderDocument - Type contract for seed base order document.
export type { SeedBaseOrderDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedBaseOrderItem - Type contract for seed base order item.
export type { SeedBaseOrderItem } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedBaseProductDocument - Type contract for seed base product document.
export type { SeedBaseProductDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedBaseStoreDocument - Type contract for seed base store document.
export type { SeedBaseStoreDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedBaseUserDocument - Type contract for seed base user document.
export type { SeedBaseUserDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedBidDocument - Type contract for seed bid document.
export type { SeedBidDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedBlogPostDocument - Type contract for seed blog post document.
export type { SeedBlogPostDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedCarouselSlideDocument - Type contract for seed carousel slide document.
export type { SeedCarouselSlideDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedCartDocument - Type contract for seed cart document.
export type { SeedCartDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedCartItemDocument - Type contract for seed cart item document.
export type { SeedCartItemDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedCategoryDocument - Type contract for seed category document.
export type { SeedCategoryDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedCollection - Type contract for seed collection.
export type { SeedCollection } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedCollectionName - Type contract for seed collection name.
export type { SeedCollectionName } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedConfig - Type contract for seed config.
export type { SeedConfig } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedCouponDocument - Type contract for seed coupon document.
export type { SeedCouponDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedFaqDocument - Type contract for seed faq document.
export type { SeedFaqDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedHomepageSectionDocument - Type contract for seed homepage section document.
export type { SeedHomepageSectionDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedLocaleData - Type contract for seed locale data.
export type { SeedLocaleData } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedNotificationDocument - Type contract for seed notification document.
export type { SeedNotificationDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedOperationResult - Type contract for seed operation result.
export type { SeedOperationResult } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedPayoutDocument - Type contract for seed payout document.
export type { SeedPayoutDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedResult - Type contract for seed result.
export type { SeedResult } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedReviewDocument - Type contract for seed review document.
export type { SeedReviewDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SeedSessionDocument - Type contract for seed session document.
export type { SeedSessionDocument } from "./seed/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TestSeedHandles - Type contract for test seed handles.
export type { TestSeedHandles } from "./seed/index";

// ./monitoring/index
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// ANALYTICS_EVENTS - Constant used across modules.
export { ANALYTICS_EVENTS } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// ErrorCategory - Shared export for error category.
export { ErrorCategory } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// ErrorSeverity - Shared export for error severity.
export { ErrorSeverity } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// addTraceAttribute - Helper for add trace attribute.
export { addTraceAttribute } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// addTraceMetric - Helper for add trace metric.
export { addTraceMetric } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// extractRequestMetadata - Shared export for extract request metadata.
export { extractRequestMetadata } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// getCacheHitRate - Helper for get cache hit rate.
export { getCacheHitRate } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// getCacheMetrics - Helper for get cache metrics.
export { getCacheMetrics } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// getCacheStatistics - Helper for get cache statistics.
export { getCacheStatistics } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// initializeAnalytics - Shared export for initialize analytics.
export { initializeAnalytics } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// initializeClientLogger - Shared export for initialize client logger.
export { initializeClientLogger } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// isCacheHitRateLow - Shared export for is cache hit rate low.
export { isCacheHitRateLow } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logApiError - Helper for log api error.
export { logApiError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logApiRouteError - Helper for log api route error.
export { logApiRouteError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logApplicationError - Helper for log application error.
export { logApplicationError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logAuthError - Helper for log auth error.
export { logAuthError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logAuthorizationError - Helper for log authorization error.
export { logAuthorizationError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logClientDebug - Helper for log client debug.
export { logClientDebug } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logClientError - Helper for log client error.
export { logClientError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logClientInfo - Helper for log client info.
export { logClientInfo } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logClientWarning - Helper for log client warning.
export { logClientWarning } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logDatabaseError - Helper for log database error.
export { logDatabaseError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logEmailError - Helper for log email error.
export { logEmailError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logExternalApiError - Helper for log external api error.
export { logExternalApiError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logNavigationError - Helper for log navigation error.
export { logNavigationError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logPaymentError - Helper for log payment error.
export { logPaymentError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logSecurityEvent - Helper for log security event.
export { logSecurityEvent } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logServerAuthError - Helper for log server auth error.
export { logServerAuthError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logServerDebug - Helper for log server debug.
export { logServerDebug } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logServerError - Helper for log server error.
export { logServerError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logServerInfo - Helper for log server info.
export { logServerInfo } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logServerWarning - Helper for log server warning.
export { logServerWarning } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logSlowOperation - Helper for log slow operation.
export { logSlowOperation } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logStorageError - Helper for log storage error.
export { logStorageError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logUploadError - Helper for log upload error.
export { logUploadError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// logValidationError - Helper for log validation error.
export { logValidationError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// recordCacheHit - Shared export for record cache hit.
export { recordCacheHit } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// recordCacheMiss - Shared export for record cache miss.
export { recordCacheMiss } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// resetCacheMetrics - Shared export for reset cache metrics.
export { resetCacheMetrics } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// serverLogger - Shared export for server logger.
export { serverLogger } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// setAnalyticsUserId - Helper for set analytics user id.
export { setAnalyticsUserId } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// setAnalyticsUserProperties - Helper for set analytics user properties.
export { setAnalyticsUserProperties } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// setErrorTracker - Helper for set error tracker.
export { setErrorTracker } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// setPerformanceProvider - Helper for set performance provider.
export { setPerformanceProvider } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// setupCacheMonitoring - Shared export for setup cache monitoring.
export { setupCacheMonitoring } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// setupGlobalErrorHandler - Shared export for setup global error handler.
export { setupGlobalErrorHandler } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// startTrace - Helper for start trace.
export { startTrace } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// stopTrace - Helper for stop trace.
export { stopTrace } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// trackAdmin - Helper for track admin.
export { trackAdmin } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// trackApiError - Helper for track api error.
export { trackApiError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// trackAuth - Helper for track auth.
export { trackAuth } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// trackAuthError - Helper for track auth error.
export { trackAuthError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// trackContent - Helper for track content.
export { trackContent } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// trackDatabaseError - Helper for track database error.
export { trackDatabaseError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// trackEcommerce - Helper for track ecommerce.
export { trackEcommerce } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// trackError - Helper for track error.
export { trackError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// trackEvent - Helper for track event.
export { trackEvent } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// trackForm - Helper for track form.
export { trackForm } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// trackPageView - Helper for track page view.
export { trackPageView } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// trackPermissionError - Helper for track permission error.
export { trackPermissionError } from "./monitoring/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// trackValidationError - Helper for track validation error.
export { trackValidationError } from "./monitoring/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AnalyticsEvent - Type contract for analytics event.
export type { AnalyticsEvent } from "./monitoring/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ClientErrorContext - Type contract for client error context.
export type { ClientErrorContext } from "./monitoring/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ErrorContext - Type contract for error context.
export type { ErrorContext } from "./monitoring/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ErrorTrackerFn - Type contract for error tracker fn.
export type { ErrorTrackerFn } from "./monitoring/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PerformanceProvider - Type contract for performance provider.
export type { PerformanceProvider } from "./monitoring/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PerformanceTrace - Type contract for performance trace.
export type { PerformanceTrace } from "./monitoring/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ServerErrorContext - Type contract for server error context.
export type { ServerErrorContext } from "./monitoring/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TrackedError - Type contract for tracked error.
export type { TrackedError } from "./monitoring/index";

// ./instrumentation/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createInstrumentation - Helper for create instrumentation.
export { createInstrumentation } from "./instrumentation/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// InstrumentationConfig - Type contract for instrumentation config.
export type { InstrumentationConfig } from "./instrumentation/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// InstrumentationHook - Type contract for instrumentation hook.
export type { InstrumentationHook } from "./instrumentation/index";

// ./http/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ApiClient - Shared export for api client.
export { ApiClient } from "./http/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ApiClientError - Model for api client error.
export { ApiClientError } from "./http/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// apiClient - Shared export for api client.
export { apiClient } from "./http/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ApiClientOptions - Type contract for api client options.
export type { ApiClientOptions } from "./http/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ApiResponse - Type contract for api response.
export type { ApiResponse } from "./http/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RequestConfig - Type contract for request config.
export type { RequestConfig } from "./http/index";

// ./next/index
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// ApiErrors - Shared export for api errors.
export { ApiErrors } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// DEFAULT_ROUTE_MAP - Constant used across modules.
export { DEFAULT_ROUTE_MAP } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// ErrorView - Component for error view.
export { ErrorView } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// GlobalError - Model for global error.
export { GlobalError } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// NotFoundView - Component for not found view.
export { NotFoundView } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// UnauthorizedView - Component for unauthorized view.
export { UnauthorizedView } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// buildBaseContext - Helper for build base context.
export { buildBaseContext } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createApiErrorHandler - Helper for create api error handler.
export { createApiErrorHandler } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createApiHandler - Helper for create api handler.
export { createApiHandler } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createApiHandlerFactory - Helper for create api handler factory.
export { createApiHandlerFactory } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createApiMiddleware - Helper for create api middleware.
export { createApiMiddleware } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createPiiRedactorMiddleware - Helper for create pii redactor middleware.
export { createPiiRedactorMiddleware } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createRouteHandler - Helper for create route handler.
export { createRouteHandler } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createRouteMap - Helper for create route map.
export { createRouteMap } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// errorResponse - Model for error response.
export { errorResponse } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getBooleanParam - Helper for get boolean param.
export { getBooleanParam } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getNumberParam - Helper for get number param.
export { getNumberParam } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getOptionalSessionCookie - Helper for get optional session cookie.
export { getOptionalSessionCookie } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getRequiredSessionCookie - Helper for get required session cookie.
export { getRequiredSessionCookie } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getSearchParams - Helper for get search params.
export { getSearchParams } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getStringParam - Helper for get string param.
export { getStringParam } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// invalidateCache - Shared export for invalidate cache.
export { invalidateCache } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// piiScrubberMiddleware - Shared export for pii scrubber middleware.
export { piiScrubberMiddleware } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// runChain - Helper for run chain.
export { runChain } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// successResponse - Model for success response.
export { successResponse } from "./next/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// withCache - Shared export for with cache.
export { withCache } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ApiErrorHandlerOptions - Type contract for api error handler options.
export type { ApiErrorHandlerOptions } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ApiErrorResponse - Type contract for api error response.
export type { ApiErrorResponse } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ApiHandlerFactoryDeps - Type contract for api handler factory deps.
export type { ApiHandlerFactoryDeps } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ApiHandlerOptions - Type contract for api handler options.
export type { ApiHandlerOptions } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ApiRateLimitResult - Type contract for api rate limit result.
export type { ApiRateLimitResult } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ApiSuccessResponse - Type contract for api success response.
export type { ApiSuccessResponse } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AuthRequestContext - Type contract for auth request context.
export type { AuthRequestContext } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AuthVerifiedUser - Type contract for auth verified user.
export type { AuthVerifiedUser } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BaseRequestContext - Type contract for base request context.
export type { BaseRequestContext } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CacheConfig - Type contract for cache config.
export type { CacheConfig } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateApiMiddlewareConfig - Type contract for create api middleware config.
export type { CreateApiMiddlewareConfig } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ErrorViewProps - Type contract for error view props.
export type { ErrorViewProps } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GlobalErrorProps - Type contract for global error props.
export type { GlobalErrorProps } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IApiErrorLogger - Type contract for iapi error logger.
export type { IApiErrorLogger } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IAuthVerifier - Type contract for iauth verifier.
export type { IAuthVerifier } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// Middleware - Type contract for middleware.
export type { Middleware } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NotFoundViewProps - Type contract for not found view props.
export type { NotFoundViewProps } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PiiRedactionRule - Type contract for pii redaction rule.
export type { PiiRedactionRule } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RouteMap - Type contract for route map.
export type { RouteMap } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RoutePath - Type contract for route path.
export type { RoutePath } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RouteUser - Type contract for route user.
export type { RouteUser } from "./next/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UnauthorizedViewProps - Type contract for unauthorized view props.
export type { UnauthorizedViewProps } from "./next/index";

// ./style/tailwind/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// cn - Shared export for cn.
export { cn } from "./style/tailwind/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// tailwindAdapter - Shared export for tailwind adapter.
export { tailwindAdapter } from "./style/tailwind/index";

// ./style/vanilla/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// vanillaAdapter - Shared export for vanilla adapter.
export { vanillaAdapter } from "./style/vanilla/index";

// ./providers/auth-firebase/index
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createMiddlewareAuthChain - Helper for create middleware auth chain.
export { createMiddlewareAuthChain } from "./providers/auth-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createSessionCookie - Helper for create session cookie.
export { createSessionCookie } from "./providers/auth-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createSessionCookieFromToken - Helper for create session cookie from token.
export { createSessionCookieFromToken } from "./providers/auth-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// firebaseAuthProvider - Component for firebase auth provider.
export { firebaseAuthProvider } from "./providers/auth-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// firebaseSessionProvider - Component for firebase session provider.
export { firebaseSessionProvider } from "./providers/auth-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getUserFromRequest - Helper for get user from request.
export { getUserFromRequest } from "./providers/auth-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// requireAuthFromRequest - Model for require auth from request.
export { requireAuthFromRequest } from "./providers/auth-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// requireAuthUser - Shared export for require auth user.
export { requireAuthUser } from "./providers/auth-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// requireRoleFromRequest - Model for require role from request.
export { requireRoleFromRequest } from "./providers/auth-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// requireRoleUser - Shared export for require role user.
export { requireRoleUser } from "./providers/auth-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// revokeUserTokens - Shared export for revoke user tokens.
export { revokeUserTokens } from "./providers/auth-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// verifyIdToken - Shared export for verify id token.
export { verifyIdToken } from "./providers/auth-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// verifySessionCookie - Shared export for verify session cookie.
export { verifySessionCookie } from "./providers/auth-firebase/index";

// ./providers/db-firebase/index
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// FirebaseRealtimeRepository - Shared export for firebase realtime repository.
export { FirebaseRealtimeRepository } from "./providers/db-firebase/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// FirebaseRepository - Shared export for firebase repository.
export { FirebaseRepository } from "./providers/db-firebase/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// FirebaseSieveRepository - Shared export for firebase sieve repository.
export { FirebaseSieveRepository } from "./providers/db-firebase/index";
// [CONFIG]-Application configuration constant — route path, API endpoint, design token, or feature flag.
// RTDB_PATHS - Constant used across modules.
export { RTDB_PATHS } from "./providers/db-firebase/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// _resetAdminSingletons - Shared export for reset admin singletons.
export { _resetAdminSingletons } from "./providers/db-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// applySieveToFirestore - Helper for apply sieve to firestore.
export { applySieveToFirestore } from "./providers/db-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// buildFirebaseClientConfig - Helper for build firebase client config.
export { buildFirebaseClientConfig } from "./providers/db-firebase/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// deserializeTimestamps - Shared export for deserialize timestamps.
export { deserializeTimestamps } from "./providers/db-firebase/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// firebaseDbProvider - Component for firebase db provider.
export { firebaseDbProvider } from "./providers/db-firebase/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// firebaseFieldOps - Shared export for firebase field ops.
export { firebaseFieldOps } from "./providers/db-firebase/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// getAdminApp - Helper for get admin app.
export { getAdminApp } from "./providers/db-firebase/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// getAdminAuth - Helper for get admin auth.
export { getAdminAuth } from "./providers/db-firebase/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// getAdminDb - Helper for get admin db.
export { getAdminDb } from "./providers/db-firebase/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// getAdminRealtimeDb - Helper for get admin realtime db.
export { getAdminRealtimeDb } from "./providers/db-firebase/index";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// getAdminStorage - Helper for get admin storage.
export { getAdminStorage } from "./providers/db-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// normalizeFirebaseConfigValue - Helper for normalize firebase config value.
export { normalizeFirebaseConfigValue } from "./providers/db-firebase/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// prepareForFirestore - Shared export for prepare for firestore.
export { prepareForFirestore } from "./providers/db-firebase/index";
// [UTIL]-Pure utility — no framework or runtime dependency; safe to import from any environment.
// removeUndefined - Helper for remove undefined.
export { removeUndefined } from "./providers/db-firebase/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DocumentSnapshot - Type contract for document snapshot.
export type { DocumentSnapshot } from "./providers/db-firebase/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FirebaseClientConfig - Type contract for firebase client config.
export type { FirebaseClientConfig } from "./providers/db-firebase/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FirebaseSieveFieldConfig - Type contract for firebase sieve field config.
export type { FirebaseSieveFieldConfig } from "./providers/db-firebase/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FirebaseSieveFields - Type contract for firebase sieve fields.
export type { FirebaseSieveFields } from "./providers/db-firebase/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FirebaseSieveOptions - Type contract for firebase sieve options.
export type { FirebaseSieveOptions } from "./providers/db-firebase/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FirebaseSieveResult - Type contract for firebase sieve result.
export type { FirebaseSieveResult } from "./providers/db-firebase/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SieveFieldConfig - Type contract for sieve field config.
export type { SieveFieldConfig } from "./providers/db-firebase/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SieveFields - Type contract for sieve fields.
export type { SieveFields } from "./providers/db-firebase/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SieveModel - Type contract for sieve model.
export type { SieveModel } from "./providers/db-firebase/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SieveOptions - Type contract for sieve options.
export type { SieveOptions } from "./providers/db-firebase/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SieveResult - Type contract for sieve result.
export type { SieveResult } from "./providers/db-firebase/index";

// ./providers/email-resend/index
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createResendProvider - Helper for create resend provider.
export { createResendProvider } from "./providers/email-resend/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ResendProviderOptions - Type contract for resend provider options.
export type { ResendProviderOptions } from "./providers/email-resend/index";

// ./providers/firebase-client/index
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// FirebaseClientAuthProvider - Component for firebase client auth provider.
export { FirebaseClientAuthProvider } from "./providers/firebase-client/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// FirebaseClientRealtimeProvider - Component for firebase client realtime provider.
export { FirebaseClientRealtimeProvider } from "./providers/firebase-client/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FirebaseClientRealtimeConfig - Type contract for firebase client realtime config.
export type { FirebaseClientRealtimeConfig } from "./providers/firebase-client/index";

// ./providers/payment-razorpay/index
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// RazorpayProvider - Component for razorpay provider.
export { RazorpayProvider } from "./providers/payment-razorpay/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createRazorpayOrder - Helper for create razorpay order.
export { createRazorpayOrder } from "./providers/payment-razorpay/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createRazorpayRefund - Helper for create razorpay refund.
export { createRazorpayRefund } from "./providers/payment-razorpay/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// fetchRazorpayOrder - Shared export for fetch razorpay order.
export { fetchRazorpayOrder } from "./providers/payment-razorpay/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// paiseToRupees - Shared export for paise to rupees.
export { paiseToRupees } from "./providers/payment-razorpay/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// rupeesToPaise - Shared export for rupees to paise.
export { rupeesToPaise } from "./providers/payment-razorpay/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// verifyPaymentSignature - Shared export for verify payment signature.
export { verifyPaymentSignature } from "./providers/payment-razorpay/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// verifyPaymentSignatureWithKeys - Shared export for verify payment signature with keys.
export { verifyPaymentSignatureWithKeys } from "./providers/payment-razorpay/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// verifyWebhookSignature - Shared export for verify webhook signature.
export { verifyWebhookSignature } from "./providers/payment-razorpay/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RazorpayConfig - Type contract for razorpay config.
export type { RazorpayConfig } from "./providers/payment-razorpay/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RazorpayOrder - Type contract for razorpay order.
export type { RazorpayOrder } from "./providers/payment-razorpay/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RazorpayOrderOptions - Type contract for razorpay order options.
export type { RazorpayOrderOptions } from "./providers/payment-razorpay/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RazorpayPaymentResult - Type contract for razorpay payment result.
export type { RazorpayPaymentResult } from "./providers/payment-razorpay/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RazorpayRefundResult - Type contract for razorpay refund result.
export type { RazorpayRefundResult } from "./providers/payment-razorpay/index";

// ./providers/shipping-shiprocket/index
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// SHIPROCKET_TOKEN_TTL_MS - Constant used across modules.
export { SHIPROCKET_TOKEN_TTL_MS } from "./providers/shipping-shiprocket/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// ShiprocketProvider - Component for shiprocket provider.
export { ShiprocketProvider } from "./providers/shipping-shiprocket/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// isShiprocketTokenExpired - Shared export for is shiprocket token expired.
export { isShiprocketTokenExpired } from "./providers/shipping-shiprocket/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// shiprocketAddPickupLocation - Shared export for shiprocket add pickup location.
export { shiprocketAddPickupLocation } from "./providers/shipping-shiprocket/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// shiprocketAuthenticate - Shared export for shiprocket authenticate.
export { shiprocketAuthenticate } from "./providers/shipping-shiprocket/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// shiprocketCheckServiceability - Shared export for shiprocket check serviceability.
export { shiprocketCheckServiceability } from "./providers/shipping-shiprocket/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// shiprocketCreateOrder - Shared export for shiprocket create order.
export { shiprocketCreateOrder } from "./providers/shipping-shiprocket/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// shiprocketGenerateAWB - Shared export for shiprocket generate awb.
export { shiprocketGenerateAWB } from "./providers/shipping-shiprocket/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// shiprocketGeneratePickup - Shared export for shiprocket generate pickup.
export { shiprocketGeneratePickup } from "./providers/shipping-shiprocket/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// shiprocketGetPickupLocations - Shared export for shiprocket get pickup locations.
export { shiprocketGetPickupLocations } from "./providers/shipping-shiprocket/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// shiprocketTrackByAWB - Shared export for shiprocket track by awb.
export { shiprocketTrackByAWB } from "./providers/shipping-shiprocket/index";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// shiprocketVerifyPickupOTP - Shared export for shiprocket verify pickup otp.
export { shiprocketVerifyPickupOTP } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketAWBResponse - Type contract for shiprocket awbresponse.
export type { ShiprocketAWBResponse } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketAddPickupRequest - Type contract for shiprocket add pickup request.
export type { ShiprocketAddPickupRequest } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketAddPickupResponse - Type contract for shiprocket add pickup response.
export type { ShiprocketAddPickupResponse } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketAuthRequest - Type contract for shiprocket auth request.
export type { ShiprocketAuthRequest } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketAuthResponse - Type contract for shiprocket auth response.
export type { ShiprocketAuthResponse } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketCourierServiceabilityResponse - Type contract for shiprocket courier serviceability response.
export type { ShiprocketCourierServiceabilityResponse } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketCreateOrderRequest - Type contract for shiprocket create order request.
export type { ShiprocketCreateOrderRequest } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketCreateOrderResponse - Type contract for shiprocket create order response.
export type { ShiprocketCreateOrderResponse } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketGenerateAWBRequest - Type contract for shiprocket generate awbrequest.
export type { ShiprocketGenerateAWBRequest } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketGeneratePickupRequest - Type contract for shiprocket generate pickup request.
export type { ShiprocketGeneratePickupRequest } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketOrderItem - Type contract for shiprocket order item.
export type { ShiprocketOrderItem } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketPickupLocation - Type contract for shiprocket pickup location.
export type { ShiprocketPickupLocation } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketPickupLocationsResponse - Type contract for shiprocket pickup locations response.
export type { ShiprocketPickupLocationsResponse } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketPickupResponse - Type contract for shiprocket pickup response.
export type { ShiprocketPickupResponse } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketProviderConfig - Type contract for shiprocket provider config.
export type { ShiprocketProviderConfig } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketShipmentTrack - Type contract for shiprocket shipment track.
export type { ShiprocketShipmentTrack } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketTrackActivity - Type contract for shiprocket track activity.
export type { ShiprocketTrackActivity } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketTrackingResponse - Type contract for shiprocket tracking response.
export type { ShiprocketTrackingResponse } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketVerifyPickupOTPRequest - Type contract for shiprocket verify pickup otprequest.
export type { ShiprocketVerifyPickupOTPRequest } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketVerifyPickupOTPResponse - Type contract for shiprocket verify pickup otpresponse.
export type { ShiprocketVerifyPickupOTPResponse } from "./providers/shipping-shiprocket/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShiprocketWebhookPayload - Type contract for shiprocket webhook payload.
export type { ShiprocketWebhookPayload } from "./providers/shipping-shiprocket/index";

// ./providers/storage-firebase/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// firebaseStorageProvider - Component for firebase storage provider.
export { firebaseStorageProvider } from "./providers/storage-firebase/index";

// ./providers/storage-firebase/client
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// STORAGE_PATHS - Constant used across modules.
export { STORAGE_PATHS } from "./providers/storage-firebase/client";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createStorageHelpers - Helper for create storage helpers.
export { createStorageHelpers } from "./providers/storage-firebase/client";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// validateFileSize - Helper for validate file size.
export { validateFileSize } from "./providers/storage-firebase/client";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// validateFileType - Helper for validate file type.
export { validateFileType } from "./providers/storage-firebase/client";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// validateImage - Helper for validate image.
export { validateImage } from "./providers/storage-firebase/client";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StorageHelpers - Type contract for storage helpers.
export type { StorageHelpers } from "./providers/storage-firebase/client";

// ./features/about/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AboutView - Component for about view.
export { AboutView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FAQPageView - Component for faqpage view.
export { FAQPageView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FeesView - Component for fees view.
export { FeesView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HelpPageView - Component for help page view.
export { HelpPageView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HowAuctionsWorkView - Component for how auctions work view.
export { HowAuctionsWorkView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HowCheckoutWorksView - Component for how checkout works view.
export { HowCheckoutWorksView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HowOffersWorkView - Component for how offers work view.
export { HowOffersWorkView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HowOrdersWorkView - Component for how orders work view.
export { HowOrdersWorkView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HowPayoutsWorkView - Component for how payouts work view.
export { HowPayoutsWorkView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HowPreOrdersWorkView - Component for how pre orders work view.
export { HowPreOrdersWorkView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HowReviewsWorkView - Component for how reviews work view.
export { HowReviewsWorkView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PolicyPageView - Component for policy page view.
export { PolicyPageView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PublicProfileView - Component for public profile view.
export { PublicProfileView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SecurityPrivacyView - Component for security privacy view.
export { SecurityPrivacyView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ShippingPolicyView - Component for shipping policy view.
export { ShippingPolicyView } from "./features/about/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TrackOrderView - Component for track order view.
export { TrackOrderView } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AboutHowItem - Type contract for about how item.
export type { AboutHowItem } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AboutMilestone - Type contract for about milestone.
export type { AboutMilestone } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AboutValueItem - Type contract for about value item.
export type { AboutValueItem } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AboutViewProps - Type contract for about view props.
export type { AboutViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQPageViewProps - Type contract for faqpage view props.
export type { FAQPageViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FeesViewProps - Type contract for fees view props.
export type { FeesViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HelpPageViewProps - Type contract for help page view props.
export type { HelpPageViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HowAuctionsWorkViewProps - Type contract for how auctions work view props.
export type { HowAuctionsWorkViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HowCheckoutWorksViewProps - Type contract for how checkout works view props.
export type { HowCheckoutWorksViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HowOffersWorkViewProps - Type contract for how offers work view props.
export type { HowOffersWorkViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HowOrdersWorkViewProps - Type contract for how orders work view props.
export type { HowOrdersWorkViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HowPayoutsWorkViewProps - Type contract for how payouts work view props.
export type { HowPayoutsWorkViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HowPreOrdersWorkViewProps - Type contract for how pre orders work view props.
export type { HowPreOrdersWorkViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HowReviewsWorkViewProps - Type contract for how reviews work view props.
export type { HowReviewsWorkViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PolicyPageViewProps - Type contract for policy page view props.
export type { PolicyPageViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PublicProfileViewProps - Type contract for public profile view props.
export type { PublicProfileViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SecurityPrivacyViewProps - Type contract for security privacy view props.
export type { SecurityPrivacyViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShippingPolicyViewProps - Type contract for shipping policy view props.
export type { ShippingPolicyViewProps } from "./features/about/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TrackOrderViewProps - Type contract for track order view props.
export type { TrackOrderViewProps } from "./features/about/index";

// ./features/account/index
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// ADDRESS_FIELDS - Constant used across modules.
export { ADDRESS_FIELDS } from "./features/account/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// ADDRESS_INDEXED_FIELDS - Constant used across modules.
export { ADDRESS_INDEXED_FIELDS } from "./features/account/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// ADDRESS_PUBLIC_FIELDS - Constant used across modules.
export { ADDRESS_PUBLIC_FIELDS } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ADDRESS_SUBCOLLECTION - Constant used across modules.
export { ADDRESS_SUBCOLLECTION } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ADDRESS_TYPES - Constant used across modules.
export { ADDRESS_TYPES } from "./features/account/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// ADDRESS_UPDATABLE_FIELDS - Constant used across modules.
export { ADDRESS_UPDATABLE_FIELDS } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AddressBook - Shared export for address book.
export { AddressBook } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AddressCard - Component for address card.
export { AddressCard } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AddressForm - Component for address form.
export { AddressForm } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AddressSelectorCreate - Shared export for address selector create.
export { AddressSelectorCreate } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BecomeSellerView - Component for become seller view.
export { BecomeSellerView } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ChatList - Shared export for chat list.
export { ChatList } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ChatWindow - Shared export for chat window.
export { ChatWindow } from "./features/account/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_ADDRESS_DATA - Constant used across modules.
export { DEFAULT_ADDRESS_DATA } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// INDIAN_STATES - Constant used across modules.
export { INDIAN_STATES } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MessagesView - Component for messages view.
export { MessagesView } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// NotificationBell - Shared export for notification bell.
export { NotificationBell } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// OrderDetailView - Component for order detail view.
export { OrderDetailView } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ProfileView - Component for profile view.
export { ProfileView } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// UserAccountHubView - Component for user account hub view.
export { UserAccountHubView } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// UserAddressesView - Component for user addresses view.
export { UserAddressesView } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// UserNotificationsView - Component for user notifications view.
export { UserNotificationsView } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// UserOffersView - Component for user offers view.
export { UserOffersView } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// UserOrderTrackView - Component for user order track view.
export { UserOrderTrackView } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// UserOrdersView - Component for user orders view.
export { UserOrdersView } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// UserSettingsView - Component for user settings view.
export { UserSettingsView } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// accountAdminColumns - Shared export for account admin columns.
export { accountAdminColumns } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildAccountColumns - Helper for build account columns.
export { buildAccountColumns } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// manifest - Shared export for manifest.
export { manifest } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// notificationPreferencesSchema - Model for notification preferences schema.
export { notificationPreferencesSchema } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// updateProfileSchema - Helper for update profile schema.
export { updateProfileSchema } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useAddress - React hook for use address.
export { useAddress } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useAddressForm - React hook for use address form.
export { useAddressForm } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useAddressSelector - React hook for use address selector.
export { useAddressSelector } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useAddresses - React hook for use addresses.
export { useAddresses } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCreateAddress - React hook for use create address.
export { useCreateAddress } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useDeleteAddress - React hook for use delete address.
export { useDeleteAddress } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useNotifications - React hook for use notifications.
export { useNotifications } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useProfile - React hook for use profile.
export { useProfile } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useProfileStats - React hook for use profile stats.
export { useProfileStats } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// usePublicProfile - React hook for use public profile.
export { usePublicProfile } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useSetDefaultAddress - React hook for use set default address.
export { useSetDefaultAddress } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useUpdateAddress - React hook for use update address.
export { useUpdateAddress } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useUpdateProfile - React hook for use update profile.
export { useUpdateProfile } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// userAddressSchema - Model for user address schema.
export { userAddressSchema } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// userProfileSchema - Model for user profile schema.
export { userProfileSchema } from "./features/account/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// validateAddressForm - Helper for validate address form.
export { validateAddressForm } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// Address - Type contract for address.
export type { Address } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressCardAddress - Type contract for address card address.
export type { AddressCardAddress } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressCardProps - Type contract for address card props.
export type { AddressCardProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressCreateInput - Type contract for address create input.
export type { AddressCreateInput } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressDocument - Type contract for address document.
export type { AddressDocument } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressFormData - Type contract for address form data.
export type { AddressFormData } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressFormLabels - Type contract for address form labels.
export type { AddressFormLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressFormPlaceholders - Type contract for address form placeholders.
export type { AddressFormPlaceholders } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressFormStateData - Type contract for address form state data.
export type { AddressFormStateData } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressSelectorCreateLabels - Type contract for address selector create labels.
export type { AddressSelectorCreateLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressSelectorCreateProps - Type contract for address selector create props.
export type { AddressSelectorCreateProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressType - Type contract for address type.
export type { AddressType } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressUpdateInput - Type contract for address update input.
export type { AddressUpdateInput } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressValidationFormData - Type contract for address validation form data.
export type { AddressValidationFormData } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddressValidationOptions - Type contract for address validation options.
export type { AddressValidationOptions } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BaseNotificationItem - Type contract for base notification item.
export type { BaseNotificationItem } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BecomeSellerViewLabels - Type contract for become seller view labels.
export type { BecomeSellerViewLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BecomeSellerViewProps - Type contract for become seller view props.
export type { BecomeSellerViewProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ChatListLabels - Type contract for chat list labels.
export type { ChatListLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ChatListProps - Type contract for chat list props.
export type { ChatListProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ChatWindowLabels - Type contract for chat window labels.
export type { ChatWindowLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ChatWindowProps - Type contract for chat window props.
export type { ChatWindowProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IndianState - Type contract for indian state.
export type { IndianState } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MessagesViewLabels - Type contract for messages view labels.
export type { MessagesViewLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MessagesViewProps - Type contract for messages view props.
export type { MessagesViewProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NotificationBellLabels - Type contract for notification bell labels.
export type { NotificationBellLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NotificationBellProps - Type contract for notification bell props.
export type { NotificationBellProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NotificationBellRenderLinkProps - Type contract for notification bell render link props.
export type { NotificationBellRenderLinkProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NotificationPreferences - Type contract for notification preferences.
export type { NotificationPreferences } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NotificationsResponse - Type contract for notifications response.
export type { NotificationsResponse } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OfferStatus - Type contract for offer status.
export type { OfferStatus } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderDetailViewLabels - Type contract for order detail view labels.
export type { OrderDetailViewLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderDetailViewProps - Type contract for order detail view props.
export type { OrderDetailViewProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderItem - Type contract for order item.
export type { OrderItem } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderStatus - Type contract for order status.
export type { OrderStatus } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PaymentStatus - Type contract for payment status.
export type { PaymentStatus } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductsApiResponse - Type contract for products api response.
export type { ProductsApiResponse } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProfileStats - Type contract for profile stats.
export type { ProfileStats } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProfileViewLabels - Type contract for profile view labels.
export type { ProfileViewLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProfileViewProps - Type contract for profile view props.
export type { ProfileViewProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PublicProfileOptions - Type contract for public profile options.
export type { PublicProfileOptions } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PublicUserProfile - Type contract for public user profile.
export type { PublicUserProfile } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerReviewItem - Type contract for seller review item.
export type { SellerReviewItem } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerReviewsData - Type contract for seller reviews data.
export type { SellerReviewsData } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UpdateProfileInput - Type contract for update profile input.
export type { UpdateProfileInput } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseNotificationsOptions - Type contract for use notifications options.
export type { UseNotificationsOptions } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserAccountHubViewLabels - Type contract for user account hub view labels.
export type { UserAccountHubViewLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserAccountHubViewProps - Type contract for user account hub view props.
export type { UserAccountHubViewProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserAddress - Type contract for user address.
export type { UserAddress } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserAddressesViewLabels - Type contract for user addresses view labels.
export type { UserAddressesViewLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserAddressesViewProps - Type contract for user addresses view props.
export type { UserAddressesViewProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserNotification - Type contract for user notification.
export type { UserNotification } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserNotificationsViewLabels - Type contract for user notifications view labels.
export type { UserNotificationsViewLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserNotificationsViewProps - Type contract for user notifications view props.
export type { UserNotificationsViewProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserOffer - Type contract for user offer.
export type { UserOffer } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserOffersViewLabels - Type contract for user offers view labels.
export type { UserOffersViewLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserOffersViewProps - Type contract for user offers view props.
export type { UserOffersViewProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserOrder - Type contract for user order.
export type { UserOrder } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserOrderTrackViewLabels - Type contract for user order track view labels.
export type { UserOrderTrackViewLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserOrderTrackViewProps - Type contract for user order track view props.
export type { UserOrderTrackViewProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserOrdersViewLabels - Type contract for user orders view labels.
export type { UserOrdersViewLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserOrdersViewProps - Type contract for user orders view props.
export type { UserOrdersViewProps } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserProfile - Type contract for user profile.
export type { UserProfile } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserProfileStats - Type contract for user profile stats.
export type { UserProfileStats } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserSettingsViewLabels - Type contract for user settings view labels.
export type { UserSettingsViewLabels } from "./features/account/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserSettingsViewProps - Type contract for user settings view props.
export type { UserSettingsViewProps } from "./features/account/index";

// ./features/account/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// AccountRepository - Shared export for account repository.
export { AccountRepository } from "./features/account/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createAddressForUser - Helper for create address for user.
export { createAddressForUser } from "./features/account/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// deleteAddressForUser - Helper for delete address for user.
export { deleteAddressForUser } from "./features/account/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getAddressByIdForUser - Helper for get address by id for user.
export { getAddressByIdForUser } from "./features/account/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listAddressesForUser - Helper for list addresses for user.
export { listAddressesForUser } from "./features/account/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// setDefaultAddressForUser - Helper for set default address for user.
export { setDefaultAddressForUser } from "./features/account/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateAddressForUser - Helper for update address for user.
export { updateAddressForUser } from "./features/account/server";

// ./features/admin/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ADMIN_PAGE_PERMISSIONS - Constant used across modules.
export { ADMIN_PAGE_PERMISSIONS } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ADMIN_PRODUCT_STATUS_OPTIONS - Constant used across modules.
export { ADMIN_PRODUCT_STATUS_OPTIONS } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminAnalyticsView - Component for admin analytics view.
export { AdminAnalyticsView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminBidsView - Component for admin bids view.
export { AdminBidsView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminBlogView - Component for admin blog view.
export { AdminBlogView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminCarouselView - Component for admin carousel view.
export { AdminCarouselView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminCategoriesView - Component for admin categories view.
export { AdminCategoriesView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminCouponsView - Component for admin coupons view.
export { AdminCouponsView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminDashboardView - Component for admin dashboard view.
export { AdminDashboardView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminFaqsView - Component for admin faqs view.
export { AdminFaqsView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminFeatureFlagsView - Component for admin feature flags view.
export { AdminFeatureFlagsView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminFilterBar - Shared export for admin filter bar.
export { AdminFilterBar } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminMediaView - Component for admin media view.
export { AdminMediaView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminNavigationView - Component for admin navigation view.
export { AdminNavigationView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminOrdersChart - Shared export for admin orders chart.
export { AdminOrdersChart } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminOrdersView - Component for admin orders view.
export { AdminOrdersView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminPageHeader - Shared export for admin page header.
export { AdminPageHeader } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminPayoutsView - Component for admin payouts view.
export { AdminPayoutsView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminProductsView - Component for admin products view.
export { AdminProductsView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminRevenueChart - Shared export for admin revenue chart.
export { AdminRevenueChart } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminReviewsView - Component for admin reviews view.
export { AdminReviewsView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminSectionsView - Component for admin sections view.
export { AdminSectionsView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminSessionsManager - Shared export for admin sessions manager.
export { AdminSessionsManager } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminSidebar - Shared export for admin sidebar.
export { AdminSidebar } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminSiteView - Component for admin site view.
export { AdminSiteView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminStatCard - Component for admin stat card.
export { AdminStatCard } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminStoresView - Component for admin stores view.
export { AdminStoresView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminTopBar - Shared export for admin top bar.
export { AdminTopBar } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminTopProductsTable - Component for admin top products table.
export { AdminTopProductsTable } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminUsersView - Component for admin users view.
export { AdminUsersView } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CHAT_ROOM_COLLECTION - Constant used across modules.
export { CHAT_ROOM_COLLECTION } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CHAT_ROOM_FIELDS - Constant used across modules.
export { CHAT_ROOM_FIELDS } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CHAT_ROOM_INDEXED_FIELDS - Constant used across modules.
export { CHAT_ROOM_INDEXED_FIELDS } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_CHAT_ROOM_DATA - Constant used across modules.
export { DEFAULT_CHAT_ROOM_DATA } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_NOTIFICATION_DATA - Constant used across modules.
export { DEFAULT_NOTIFICATION_DATA } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_SITE_SETTINGS_DATA - Constant used across modules.
export { DEFAULT_SITE_SETTINGS_DATA } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_TRUST_BAR_ITEMS - Constant used across modules.
export { DEFAULT_TRUST_BAR_ITEMS } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DashboardStatsGrid - Component for dashboard stats grid.
export { DashboardStatsGrid } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DemoSeedView - Component for demo seed view.
export { DemoSeedView } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DrawerFormFooter - Shared export for drawer form footer.
export { DrawerFormFooter } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FEATURE_FLAG_META - Constant used across modules.
export { FEATURE_FLAG_META } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// NOTIFICATIONS_COLLECTION - Constant used across modules.
export { NOTIFICATIONS_COLLECTION } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// NOTIFICATIONS_INDEXED_FIELDS - Constant used across modules.
export { NOTIFICATIONS_INDEXED_FIELDS } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// NOTIFICATION_FIELDS - Constant used across modules.
export { NOTIFICATION_FIELDS } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// NOTIFICATION_PUBLIC_FIELDS - Constant used across modules.
export { NOTIFICATION_PUBLIC_FIELDS } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// QuickActionsPanel - Shared export for quick actions panel.
export { QuickActionsPanel } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// SITE_SETTINGS_COLLECTION - Constant used across modules.
export { SITE_SETTINGS_COLLECTION } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// SITE_SETTINGS_INDEXED_FIELDS - Constant used across modules.
export { SITE_SETTINGS_INDEXED_FIELDS } from "./features/admin/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// SITE_SETTINGS_PUBLIC_FIELDS - Constant used across modules.
export { SITE_SETTINGS_PUBLIC_FIELDS } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// chatRoomQueryHelpers - Shared export for chat room query helpers.
export { chatRoomQueryHelpers } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// notificationQueryHelpers - Shared export for notification query helpers.
export { notificationQueryHelpers } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useChat - React hook for use chat.
export { useChat } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useChatRooms - React hook for use chat rooms.
export { useChatRooms } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCreateChatRoom - React hook for use create chat room.
export { useCreateChatRoom } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useDashboardStats - React hook for use dashboard stats.
export { useDashboardStats } from "./features/admin/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useDeleteChatRoom - React hook for use delete chat room.
export { useDeleteChatRoom } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminAnalyticsData - Type contract for admin analytics data.
export type { AdminAnalyticsData } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminAnalyticsViewLabels - Type contract for admin analytics view labels.
export type { AdminAnalyticsViewLabels } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminAnalyticsViewProps - Type contract for admin analytics view props.
export type { AdminAnalyticsViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminBidsViewProps - Type contract for admin bids view props.
export type { AdminBidsViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminBlogViewProps - Type contract for admin blog view props.
export type { AdminBlogViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminCarouselViewProps - Type contract for admin carousel view props.
export type { AdminCarouselViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminCategoriesViewProps - Type contract for admin categories view props.
export type { AdminCategoriesViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminCouponsViewProps - Type contract for admin coupons view props.
export type { AdminCouponsViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminDashboardViewProps - Type contract for admin dashboard view props.
export type { AdminDashboardViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminFaqsViewProps - Type contract for admin faqs view props.
export type { AdminFaqsViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminFeatureFlagsViewProps - Type contract for admin feature flags view props.
export type { AdminFeatureFlagsViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminFilterBarProps - Type contract for admin filter bar props.
export type { AdminFilterBarProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminListParams - Type contract for admin list params.
export type { AdminListParams } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminListResponse - Type contract for admin list response.
export type { AdminListResponse } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminMediaViewProps - Type contract for admin media view props.
export type { AdminMediaViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminNavigationViewProps - Type contract for admin navigation view props.
export type { AdminNavigationViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminOrdersChartProps - Type contract for admin orders chart props.
export type { AdminOrdersChartProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminOrdersViewProps - Type contract for admin orders view props.
export type { AdminOrdersViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminPageHeaderProps - Type contract for admin page header props.
export type { AdminPageHeaderProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminPayoutsViewProps - Type contract for admin payouts view props.
export type { AdminPayoutsViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminProduct - Type contract for admin product.
export type { AdminProduct } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminProductDrawerMode - Type contract for admin product drawer mode.
export type { AdminProductDrawerMode } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminProductStatus - Type contract for admin product status.
export type { AdminProductStatus } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminProductsViewProps - Type contract for admin products view props.
export type { AdminProductsViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminRevenueChartProps - Type contract for admin revenue chart props.
export type { AdminRevenueChartProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminReviewsViewProps - Type contract for admin reviews view props.
export type { AdminReviewsViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminSectionsViewProps - Type contract for admin sections view props.
export type { AdminSectionsViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminSessionsManagerProps - Type contract for admin sessions manager props.
export type { AdminSessionsManagerProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminSidebarProps - Type contract for admin sidebar props.
export type { AdminSidebarProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminSiteViewProps - Type contract for admin site view props.
export type { AdminSiteViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminStatCardProps - Type contract for admin stat card props.
export type { AdminStatCardProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminStoresViewProps - Type contract for admin stores view props.
export type { AdminStoresViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminTableColumn - Type contract for admin table column.
export type { AdminTableColumn } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminTopBarProps - Type contract for admin top bar props.
export type { AdminTopBarProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminTopProductsTableLabels - Type contract for admin top products table labels.
export type { AdminTopProductsTableLabels } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminTopProductsTableProps - Type contract for admin top products table props.
export type { AdminTopProductsTableProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminUsersViewProps - Type contract for admin users view props.
export type { AdminUsersViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AnalyticsMonthEntry - Type contract for analytics month entry.
export type { AnalyticsMonthEntry } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AnalyticsSummary - Type contract for analytics summary.
export type { AnalyticsSummary } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AnalyticsTopProduct - Type contract for analytics top product.
export type { AnalyticsTopProduct } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ChatMessage - Type contract for chat message.
export type { ChatMessage } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ChatRoomCreateInput - Type contract for chat room create input.
export type { ChatRoomCreateInput } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ChatRoomDocument - Type contract for chat room document.
export type { ChatRoomDocument } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ChatRoomUpdateInput - Type contract for chat room update input.
export type { ChatRoomUpdateInput } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DashboardStats - Type contract for dashboard stats.
export type { DashboardStats } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DemoSeedViewProps - Type contract for demo seed view props.
export type { DemoSeedViewProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DrawerFormFooterProps - Type contract for drawer form footer props.
export type { DrawerFormFooterProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FeatureFlagKey - Type contract for feature flag key.
export type { FeatureFlagKey } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FeatureFlagMeta - Type contract for feature flag meta.
export type { FeatureFlagMeta } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FeaturedResult - Type contract for featured result.
export type { FeaturedResult } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NotificationCreateInput - Type contract for notification create input.
export type { NotificationCreateInput } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NotificationDocument - Type contract for notification document.
export type { NotificationDocument } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NotificationPriority - Type contract for notification priority.
export type { NotificationPriority } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NotificationUpdateInput - Type contract for notification update input.
export type { NotificationUpdateInput } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// QuickActionItem - Type contract for quick action item.
export type { QuickActionItem } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// QuickActionsPanelProps - Type contract for quick actions panel props.
export type { QuickActionsPanelProps } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SiteSettingsCredentials - Type contract for site settings credentials.
export type { SiteSettingsCredentials } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SiteSettingsCredentialsMasked - Type contract for site settings credentials masked.
export type { SiteSettingsCredentialsMasked } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SiteSettingsDocument - Type contract for site settings document.
export type { SiteSettingsDocument } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SiteSettingsUpdateInput - Type contract for site settings update input.
export type { SiteSettingsUpdateInput } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TrustBarItem - Type contract for trust bar item.
export type { TrustBarItem } from "./features/admin/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseChatReturn - Type contract for use chat return.
export type { UseChatReturn } from "./features/admin/index";

// ./features/admin/server
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminBidsGET - Shared export for admin bids get.
export { adminBidsGET } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminCouponsGET - Shared export for admin coupons get.
export { adminCouponsGET } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminCreateCoupon - Shared export for admin create coupon.
export { adminCreateCoupon } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminCreateProduct - Shared export for admin create product.
export { adminCreateProduct } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminDeleteCoupon - Shared export for admin delete coupon.
export { adminDeleteCoupon } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminDeleteProduct - Shared export for admin delete product.
export { adminDeleteProduct } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminDeleteUser - Shared export for admin delete user.
export { adminDeleteUser } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminProductsGET - Shared export for admin products get.
export { adminProductsGET } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminReviewsGET - Shared export for admin reviews get.
export { adminReviewsGET } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminUpdateCoupon - Shared export for admin update coupon.
export { adminUpdateCoupon } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminUpdateOrder - Shared export for admin update order.
export { adminUpdateOrder } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminUpdatePayout - Shared export for admin update payout.
export { adminUpdatePayout } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminUpdateProduct - Shared export for admin update product.
export { adminUpdateProduct } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminUpdateStoreStatus - Model for admin update store status.
export { adminUpdateStoreStatus } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminUpdateUser - Shared export for admin update user.
export { adminUpdateUser } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createOrGetChatRoom - Helper for create or get chat room.
export { createOrGetChatRoom } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// deleteChatRoom - Helper for delete chat room.
export { deleteChatRoom } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// deleteNotification - Helper for delete notification.
export { deleteNotification } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getAdminAnalytics - Helper for get admin analytics.
export { getAdminAnalytics } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getAdminDashboardStats - Helper for get admin dashboard stats.
export { getAdminDashboardStats } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getChatRooms - Helper for get chat rooms.
export { getChatRooms } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getSiteSettings - Helper for get site settings.
export { getSiteSettings } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getUnreadNotificationCount - Helper for get unread notification count.
export { getUnreadNotificationCount } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listAdminBids - Helper for list admin bids.
export { listAdminBids } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listAdminBlog - Helper for list admin blog.
export { listAdminBlog } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listAdminCoupons - Helper for list admin coupons.
export { listAdminCoupons } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listAdminOrders - Helper for list admin orders.
export { listAdminOrders } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listAdminPayouts - Helper for list admin payouts.
export { listAdminPayouts } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listAdminProducts - Helper for list admin products.
export { listAdminProducts } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listAdminSessions - Helper for list admin sessions.
export { listAdminSessions } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listAdminStores - Helper for list admin stores.
export { listAdminStores } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listAdminUsers - Helper for list admin users.
export { listAdminUsers } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listNotifications - Helper for list notifications.
export { listNotifications } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// markAllNotificationsRead - Shared export for mark all notifications read.
export { markAllNotificationsRead } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// markNotificationRead - Shared export for mark notification read.
export { markNotificationRead } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// revokeSession - Shared export for revoke session.
export { revokeSession } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// revokeUserSessions - Shared export for revoke user sessions.
export { revokeUserSessions } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sendChatMessage - Shared export for send chat message.
export { sendChatMessage } from "./features/admin/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateSiteSettings - Helper for update site settings.
export { updateSiteSettings } from "./features/admin/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminActor - Type contract for admin actor.
export type { AdminActor } from "./features/admin/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ChatRoomsResult - Type contract for chat rooms result.
export type { ChatRoomsResult } from "./features/admin/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateRoomInput - Type contract for create room input.
export type { CreateRoomInput } from "./features/admin/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateRoomResult - Type contract for create room result.
export type { CreateRoomResult } from "./features/admin/server";

// ./features/auctions/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AuctionCard - Component for auction card.
export { AuctionCard } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AuctionCountdown - Shared export for auction countdown.
export { AuctionCountdown } from "./features/auctions/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// BID_COLLECTION - Constant used across modules.
export { BID_COLLECTION } from "./features/auctions/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// BID_INDEXED_FIELDS - Constant used across modules.
export { BID_INDEXED_FIELDS } from "./features/auctions/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// BID_PUBLIC_FIELDS - Constant used across modules.
export { BID_PUBLIC_FIELDS } from "./features/auctions/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// BID_UPDATABLE_FIELDS - Constant used across modules.
export { BID_UPDATABLE_FIELDS } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BidStatusValues - Model for bid status values.
export { BidStatusValues } from "./features/auctions/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_BID_DATA - Constant used across modules.
export { DEFAULT_BID_DATA } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MarketplaceAuctionCard - Component for marketplace auction card.
export { MarketplaceAuctionCard } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MarketplaceAuctionGrid - Component for marketplace auction grid.
export { MarketplaceAuctionGrid } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// auctionAdminColumns - Shared export for auction admin columns.
export { auctionAdminColumns } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// auctionItemSchema - Model for auction item schema.
export { auctionItemSchema } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// auctionListParamsSchema - Model for auction list params schema.
export { auctionListParamsSchema } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// auctionStatusSchema - Model for auction status schema.
export { auctionStatusSchema } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// bidAdminColumns - Shared export for bid admin columns.
export { bidAdminColumns } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// bidQueryHelpers - Shared export for bid query helpers.
export { bidQueryHelpers } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// bidRecordSchema - Model for bid record schema.
export { bidRecordSchema } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildAuctionColumns - Helper for build auction columns.
export { buildAuctionColumns } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildBidColumns - Helper for build bid columns.
export { buildBidColumns } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createBidId - Helper for create bid id.
export { createBidId } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useAuction - React hook for use auction.
export { useAuction } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useAuctionBids - React hook for use auction bids.
export { useAuctionBids } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useAuctionDetail - React hook for use auction detail.
export { useAuctionDetail } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useAuctions - React hook for use auctions.
export { useAuctions } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// usePlaceBid - React hook for use place bid.
export { usePlaceBid } from "./features/auctions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useRealtimeBids - React hook for use realtime bids.
export { useRealtimeBids } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AuctionItem - Type contract for auction item.
export type { AuctionItem } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AuctionListParams - Type contract for auction list params.
export type { AuctionListParams } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AuctionListResponse - Type contract for auction list response.
export type { AuctionListResponse } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BidAdminUpdateInput - Type contract for bid admin update input.
export type { BidAdminUpdateInput } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BidCreateInput - Type contract for bid create input.
export type { BidCreateInput } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BidDocument - Type contract for bid document.
export type { BidDocument } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BidListResponse - Type contract for bid list response.
export type { BidListResponse } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BidRecord - Type contract for bid record.
export type { BidRecord } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BidResult - Type contract for bid result.
export type { BidResult } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BidStatus - Type contract for bid status.
export type { BidStatus } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BidUpdateInput - Type contract for bid update input.
export type { BidUpdateInput } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MarketplaceAuctionCardData - Type contract for marketplace auction card data.
export type { MarketplaceAuctionCardData } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MarketplaceAuctionCardLabels - Type contract for marketplace auction card labels.
export type { MarketplaceAuctionCardLabels } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MarketplaceAuctionCardProps - Type contract for marketplace auction card props.
export type { MarketplaceAuctionCardProps } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MarketplaceAuctionGridLabels - Type contract for marketplace auction grid labels.
export type { MarketplaceAuctionGridLabels } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MarketplaceAuctionGridProps - Type contract for marketplace auction grid props.
export type { MarketplaceAuctionGridProps } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PlaceBidPayload - Type contract for place bid payload.
export type { PlaceBidPayload } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PublicBid - Type contract for public bid.
export type { PublicBid } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RealtimeBidData - Type contract for realtime bid data.
export type { RealtimeBidData } from "./features/auctions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseRealtimeBidsReturn - Type contract for use realtime bids return.
export type { UseRealtimeBidsReturn } from "./features/auctions/index";

// ./features/auctions/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// AuctionsRepository - Shared export for auctions repository.
export { AuctionsRepository } from "./features/auctions/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// GET - Constant used across modules.
export { GET } from "./features/auctions/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// bidsGET - Shared export for bids get.
export { bidsGET } from "./features/auctions/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getBidById - Helper for get bid by id.
export { getBidById } from "./features/auctions/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listBidsByProduct - Helper for list bids by product.
export { listBidsByProduct } from "./features/auctions/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// placeBid - Shared export for place bid.
export { placeBid } from "./features/auctions/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PlaceBidInput - Type contract for place bid input.
export type { PlaceBidInput } from "./features/auctions/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PlaceBidResult - Type contract for place bid result.
export type { PlaceBidResult } from "./features/auctions/server";

// ./features/auth/server
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// CONSENT_OTP_COOLDOWN_MS - Constant used across modules.
export { CONSENT_OTP_COOLDOWN_MS } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// CONSENT_OTP_EXPIRY_MINUTES - Constant used across modules.
export { CONSENT_OTP_EXPIRY_MINUTES } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// CONSENT_OTP_EXPIRY_MS - Constant used across modules.
export { CONSENT_OTP_EXPIRY_MS } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// CONSENT_OTP_MAX_ATTEMPTS - Constant used across modules.
export { CONSENT_OTP_MAX_ATTEMPTS } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// CONSENT_OTP_MAX_BYPASS_CREDITS - Constant used across modules.
export { CONSENT_OTP_MAX_BYPASS_CREDITS } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// CONSENT_OTP_VERIFY_RATE_LIMIT - Constant used across modules.
export { CONSENT_OTP_VERIFY_RATE_LIMIT } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// authMeGET - Shared export for auth me get.
export { authMeGET } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// buildConsentOtpEmail - Helper for build consent otp email.
export { buildConsentOtpEmail } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// consentOtpRateLimitRef - Shared export for consent otp rate limit ref.
export { consentOtpRateLimitRef } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// consentOtpRef - Shared export for consent otp ref.
export { consentOtpRef } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createPasswordResetToken - Helper for create password reset token.
export { createPasswordResetToken } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createVerificationToken - Helper for create verification token.
export { createVerificationToken } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// deleteToken - Helper for delete token.
export { deleteToken } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// enforceConsentOtpRateLimit - Shared export for enforce consent otp rate limit.
export { enforceConsentOtpRateLimit } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// generateOtpCode - Helper for generate otp code.
export { generateOtpCode } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// generateToken - Helper for generate token.
export { generateToken } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getPublicUserProfile - Helper for get public user profile.
export { getPublicUserProfile } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getSellerProducts - Helper for get seller products.
export { getSellerProducts } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getSellerReviews - Helper for get seller reviews.
export { getSellerReviews } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getUserProfile - Helper for get user profile.
export { getUserProfile } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getUserSessions - Helper for get user sessions.
export { getUserSessions } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// hashOtp - Shared export for hash otp.
export { hashOtp } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// issueRealtimeToken - Shared export for issue realtime token.
export { issueRealtimeToken } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// markPasswordResetTokenAsUsed - Shared export for mark password reset token as used.
export { markPasswordResetTokenAsUsed } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// patchConsentOtp - Shared export for patch consent otp.
export { patchConsentOtp } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// readConsentOtp - Shared export for read consent otp.
export { readConsentOtp } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// saveConsentOtp - Shared export for save consent otp.
export { saveConsentOtp } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateUserProfile - Helper for update user profile.
export { updateUserProfile } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// verifyEmailToken - Shared export for verify email token.
export { verifyEmailToken } from "./features/auth/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// verifyPasswordResetToken - Shared export for verify password reset token.
export { verifyPasswordResetToken } from "./features/auth/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ConsentOtpDoc - Type contract for consent otp doc.
export type { ConsentOtpDoc } from "./features/auth/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PasswordResetTokenData - Type contract for password reset token data.
export type { PasswordResetTokenData } from "./features/auth/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RealtimeTokenResult - Type contract for realtime token result.
export type { RealtimeTokenResult } from "./features/auth/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TokenData - Type contract for token data.
export type { TokenData } from "./features/auth/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TokenErrorMessages - Type contract for token error messages.
export type { TokenErrorMessages } from "./features/auth/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TokenServiceConfig - Type contract for token service config.
export type { TokenServiceConfig } from "./features/auth/server";

// ./features/auth/schemas/index
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_EMAIL_VERIFICATION_TOKEN_DATA - Constant used across modules.
export { DEFAULT_EMAIL_VERIFICATION_TOKEN_DATA } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_PASSWORD_RESET_TOKEN_DATA - Constant used across modules.
export { DEFAULT_PASSWORD_RESET_TOKEN_DATA } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_SESSION_DATA - Constant used across modules.
export { DEFAULT_SESSION_DATA } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_USER_DATA - Constant used across modules.
export { DEFAULT_USER_DATA } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// EMAIL_VERIFICATION_COLLECTION - Constant used across modules.
export { EMAIL_VERIFICATION_COLLECTION } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// PASSWORD_RESET_COLLECTION - Constant used across modules.
export { PASSWORD_RESET_COLLECTION } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// SESSION_CLEANUP_AFTER_MS - Constant used across modules.
export { SESSION_CLEANUP_AFTER_MS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// SESSION_EXPIRATION_MS - Constant used across modules.
export { SESSION_EXPIRATION_MS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// SESSION_FIELDS - Constant used across modules.
export { SESSION_FIELDS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// SESSION_INDEXED_FIELDS - Constant used across modules.
export { SESSION_INDEXED_FIELDS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// SESSION_PUBLIC_FIELDS - Constant used across modules.
export { SESSION_PUBLIC_FIELDS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// SMS_COUNTERS_COLLECTION - Constant used across modules.
export { SMS_COUNTERS_COLLECTION } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// SMS_COUNTER_FIELDS - Constant used across modules.
export { SMS_COUNTER_FIELDS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// SMS_DAILY_LIMIT - Constant used across modules.
export { SMS_DAILY_LIMIT } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// TOKEN_FIELDS - Constant used across modules.
export { TOKEN_FIELDS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// TOKEN_INDEXED_FIELDS - Constant used across modules.
export { TOKEN_INDEXED_FIELDS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// TOKEN_PUBLIC_FIELDS - Constant used across modules.
export { TOKEN_PUBLIC_FIELDS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// TOKEN_UPDATABLE_FIELDS - Constant used across modules.
export { TOKEN_UPDATABLE_FIELDS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// USER_COLLECTION - Constant used across modules.
export { USER_COLLECTION } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// USER_FIELDS - Constant used across modules.
export { USER_FIELDS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// USER_INDEXED_FIELDS - Constant used across modules.
export { USER_INDEXED_FIELDS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// USER_PUBLIC_FIELDS - Constant used across modules.
export { USER_PUBLIC_FIELDS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// USER_UPDATABLE_FIELDS - Constant used across modules.
export { USER_UPDATABLE_FIELDS } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// UserSchemaDefaults - Shared export for user schema defaults.
export { UserSchemaDefaults } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// authUserSchema - Model for auth user schema.
export { authUserSchema } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// createUserId - Helper for create user id.
export { createUserId } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// forgotPasswordSchema - Model for forgot password schema.
export { forgotPasswordSchema } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// loginSchema - Model for login schema.
export { loginSchema } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// parseUserAgent - Helper for parse user agent.
export { parseUserAgent } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// registerSchema - Helper for register schema.
export { registerSchema } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// sessionQueryHelpers - Shared export for session query helpers.
export { sessionQueryHelpers } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// tokenQueryHelpers - Shared export for token query helpers.
export { tokenQueryHelpers } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// userQueryHelpers - Shared export for user query helpers.
export { userQueryHelpers } from "./features/auth/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// userRoleSchema - Model for user role schema.
export { userRoleSchema } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AvatarMetadata - Type contract for avatar metadata.
export type { AvatarMetadata } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EmailVerificationTokenCreateInput - Type contract for email verification token create input.
export type { EmailVerificationTokenCreateInput } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EmailVerificationTokenDocument - Type contract for email verification token document.
export type { EmailVerificationTokenDocument } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PasswordResetTokenCreateInput - Type contract for password reset token create input.
export type { PasswordResetTokenCreateInput } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PasswordResetTokenDocument - Type contract for password reset token document.
export type { PasswordResetTokenDocument } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerBankAccount - Type contract for seller bank account.
export type { SellerBankAccount } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerPayoutDetails - Type contract for seller payout details.
export type { SellerPayoutDetails } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerPayoutMethod - Type contract for seller payout method.
export type { SellerPayoutMethod } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerPickupAddress - Type contract for seller pickup address.
export type { SellerPickupAddress } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerShippingConfig - Type contract for seller shipping config.
export type { SellerShippingConfig } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerShippingMethod - Type contract for seller shipping method.
export type { SellerShippingMethod } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SessionCreateInput - Type contract for session create input.
export type { SessionCreateInput } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SessionDocument - Type contract for session document.
export type { SessionDocument } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SessionRevokeInput - Type contract for session revoke input.
export type { SessionRevokeInput } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SessionUpdateInput - Type contract for session update input.
export type { SessionUpdateInput } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SmsCounterDocument - Type contract for sms counter document.
export type { SmsCounterDocument } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserAdminUpdateInput - Type contract for user admin update input.
export type { UserAdminUpdateInput } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserCreateInput - Type contract for user create input.
export type { UserCreateInput } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserDocument - Type contract for user document.
export type { UserDocument } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserQueryFilter - Type contract for user query filter.
export type { UserQueryFilter } from "./features/auth/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserUpdateInput - Type contract for user update input.
export type { UserUpdateInput } from "./features/auth/schemas/index";

// ./features/before-after/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BeforeAfterGallery - Shared export for before after gallery.
export { BeforeAfterGallery } from "./features/before-after/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BeforeAfterSlider - Shared export for before after slider.
export { BeforeAfterSlider } from "./features/before-after/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// beforeAfterAdminColumns - Shared export for before after admin columns.
export { beforeAfterAdminColumns } from "./features/before-after/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// beforeAfterItemSchema - Model for before after item schema.
export { beforeAfterItemSchema } from "./features/before-after/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildBeforeAfterColumns - Helper for build before after columns.
export { buildBeforeAfterColumns } from "./features/before-after/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useBeforeAfter - React hook for use before after.
export { useBeforeAfter } from "./features/before-after/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BeforeAfterItem - Type contract for before after item.
export type { BeforeAfterItem } from "./features/before-after/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BeforeAfterListResponse - Type contract for before after list response.
export type { BeforeAfterListResponse } from "./features/before-after/index";

// ./features/before-after/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// BeforeAfterRepository - Shared export for before after repository.
export { BeforeAfterRepository } from "./features/before-after/server";

// ./features/blog/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BLOG_ADMIN_SORT_OPTIONS - Constant used across modules.
export { BLOG_ADMIN_SORT_OPTIONS } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BLOG_FILTER_KEYS - Constant used across modules.
export { BLOG_FILTER_KEYS } from "./features/blog/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// BLOG_POSTS_COLLECTION - Constant used across modules.
export { BLOG_POSTS_COLLECTION } from "./features/blog/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// BLOG_POSTS_INDEXED_FIELDS - Constant used across modules.
export { BLOG_POSTS_INDEXED_FIELDS } from "./features/blog/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// BLOG_POST_FIELDS - Constant used across modules.
export { BLOG_POST_FIELDS } from "./features/blog/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// BLOG_POST_PUBLIC_FIELDS - Constant used across modules.
export { BLOG_POST_PUBLIC_FIELDS } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BLOG_PUBLIC_SORT_OPTIONS - Constant used across modules.
export { BLOG_PUBLIC_SORT_OPTIONS } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BLOG_SELLER_SORT_OPTIONS - Constant used across modules.
export { BLOG_SELLER_SORT_OPTIONS } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BLOG_SORT_OPTIONS - Constant used across modules.
export { BLOG_SORT_OPTIONS } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BlogCard - Component for blog card.
export { BlogCard } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BlogCategoryTabs - Component for blog category tabs.
export { BlogCategoryTabs } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BlogFeaturedCard - Component for blog featured card.
export { BlogFeaturedCard } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BlogFilters - Shared export for blog filters.
export { BlogFilters } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BlogIndexPageView - Component for blog index page view.
export { BlogIndexPageView } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BlogListView - Component for blog list view.
export { BlogListView } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BlogPostForm - Component for blog post form.
export { BlogPostForm } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BlogPostStatusValues - Model for blog post status values.
export { BlogPostStatusValues } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BlogPostView - Component for blog post view.
export { BlogPostView } from "./features/blog/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_BLOG_POST_DATA - Constant used across modules.
export { DEFAULT_BLOG_POST_DATA } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// blogAdminColumns - Shared export for blog admin columns.
export { blogAdminColumns } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// blogListParamsSchema - Model for blog list params schema.
export { blogListParamsSchema } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// blogPostCategorySchema - Model for blog post category schema.
export { blogPostCategorySchema } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// blogPostQueryHelpers - Shared export for blog post query helpers.
export { blogPostQueryHelpers } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// blogPostSchema - Model for blog post schema.
export { blogPostSchema } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// blogPostStatusSchema - Model for blog post status schema.
export { blogPostStatusSchema } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildBlogColumns - Helper for build blog columns.
export { buildBlogColumns } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createBlogPostId - Helper for create blog post id.
export { createBlogPostId } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getBlogFilterKeys - Helper for get blog filter keys.
export { getBlogFilterKeys } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getBlogSortOptions - Helper for get blog sort options.
export { getBlogSortOptions } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useBlogPost - React hook for use blog post.
export { useBlogPost } from "./features/blog/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useBlogPosts - React hook for use blog posts.
export { useBlogPosts } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogFeaturedCardProps - Type contract for blog featured card props.
export type { BlogFeaturedCardProps } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogFilterVariant - Type contract for blog filter variant.
export type { BlogFilterVariant } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogFiltersProps - Type contract for blog filters props.
export type { BlogFiltersProps } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogListMeta - Type contract for blog list meta.
export type { BlogListMeta } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogListParams - Type contract for blog list params.
export type { BlogListParams } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogListResponse - Type contract for blog list response.
export type { BlogListResponse } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogPost - Type contract for blog post.
export type { BlogPost } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogPostCategory - Type contract for blog post category.
export type { BlogPostCategory } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogPostCreateInput - Type contract for blog post create input.
export type { BlogPostCreateInput } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogPostDocument - Type contract for blog post document.
export type { BlogPostDocument } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogPostFormLabels - Type contract for blog post form labels.
export type { BlogPostFormLabels } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogPostFormOption - Type contract for blog post form option.
export type { BlogPostFormOption } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogPostFormProps - Type contract for blog post form props.
export type { BlogPostFormProps } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogPostFormValue - Type contract for blog post form value.
export type { BlogPostFormValue } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogPostStatus - Type contract for blog post status.
export type { BlogPostStatus } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogPostUpdateInput - Type contract for blog post update input.
export type { BlogPostUpdateInput } from "./features/blog/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogPostViewProps - Type contract for blog post view props.
export type { BlogPostViewProps } from "./features/blog/index";

// ./features/blog/server
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// blogGET - Shared export for blog get.
export { blogGET } from "./features/blog/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// blogSlugGET - Shared export for blog slug get.
export { blogSlugGET } from "./features/blog/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createBlogPost - Helper for create blog post.
export { createBlogPost } from "./features/blog/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createBlogPostSchema - Helper for create blog post schema.
export { createBlogPostSchema } from "./features/blog/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// deleteBlogPost - Helper for delete blog post.
export { deleteBlogPost } from "./features/blog/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getBlogPostById - Helper for get blog post by id.
export { getBlogPostById } from "./features/blog/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getBlogPostBySlug - Helper for get blog post by slug.
export { getBlogPostBySlug } from "./features/blog/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getFeaturedBlogPosts - Helper for get featured blog posts.
export { getFeaturedBlogPosts } from "./features/blog/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getLatestBlogPosts - Helper for get latest blog posts.
export { getLatestBlogPosts } from "./features/blog/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listBlogPosts - Helper for list blog posts.
export { listBlogPosts } from "./features/blog/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateBlogPost - Helper for update blog post.
export { updateBlogPost } from "./features/blog/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateBlogPostSchema - Helper for update blog post schema.
export { updateBlogPostSchema } from "./features/blog/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogPostDetailResponse - Type contract for blog post detail response.
export type { BlogPostDetailResponse } from "./features/blog/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateBlogPostInput - Type contract for create blog post input.
export type { CreateBlogPostInput } from "./features/blog/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UpdateBlogPostInput - Type contract for update blog post input.
export type { UpdateBlogPostInput } from "./features/blog/server";

// ./features/cart/server
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// addItemToCart - Helper for add item to cart.
export { addItemToCart } from "./features/cart/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// clearCart - Shared export for clear cart.
export { clearCart } from "./features/cart/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getCart - Helper for get cart.
export { getCart } from "./features/cart/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// mergeGuestCart - Helper for merge guest cart.
export { mergeGuestCart } from "./features/cart/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// removeCartItem - Helper for remove cart item.
export { removeCartItem } from "./features/cart/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateCartItem - Helper for update cart item.
export { updateCartItem } from "./features/cart/server";

// ./features/cart/schemas/index
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CART_COLLECTION - Constant used across modules.
export { CART_COLLECTION } from "./features/cart/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CART_FIELDS - Constant used across modules.
export { CART_FIELDS } from "./features/cart/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CART_INDEXED_FIELDS - Constant used across modules.
export { CART_INDEXED_FIELDS } from "./features/cart/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_CART_DATA - Constant used across modules.
export { DEFAULT_CART_DATA } from "./features/cart/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// cartItemMetaSchema - Model for cart item meta schema.
export { cartItemMetaSchema } from "./features/cart/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// cartItemSchema - Model for cart item schema.
export { cartItemSchema } from "./features/cart/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// cartQueryHelpers - Shared export for cart query helpers.
export { cartQueryHelpers } from "./features/cart/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// cartSummarySchema - Model for cart summary schema.
export { cartSummarySchema } from "./features/cart/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AddToCartInput - Type contract for add to cart input.
export type { AddToCartInput } from "./features/cart/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CartCreateInput - Type contract for cart create input.
export type { CartCreateInput } from "./features/cart/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CartDocument - Type contract for cart document.
export type { CartDocument } from "./features/cart/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CartItemDocument - Type contract for cart item document.
export type { CartItemDocument } from "./features/cart/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UpdateCartItemInput - Type contract for update cart item input.
export type { UpdateCartItemInput } from "./features/cart/schemas/index";

// ./features/categories/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BreadcrumbTrail - Shared export for breadcrumb trail.
export { BreadcrumbTrail } from "./features/categories/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CATEGORIES_COLLECTION - Constant used across modules.
export { CATEGORIES_COLLECTION } from "./features/categories/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CATEGORIES_INDEXED_FIELDS - Constant used across modules.
export { CATEGORIES_INDEXED_FIELDS } from "./features/categories/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CATEGORIES_PUBLIC_FIELDS - Constant used across modules.
export { CATEGORIES_PUBLIC_FIELDS } from "./features/categories/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CATEGORIES_UPDATABLE_FIELDS - Constant used across modules.
export { CATEGORIES_UPDATABLE_FIELDS } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CATEGORY_ADMIN_SORT_OPTIONS - Constant used across modules.
export { CATEGORY_ADMIN_SORT_OPTIONS } from "./features/categories/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CATEGORY_FIELDS - Constant used across modules.
export { CATEGORY_FIELDS } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CATEGORY_FILTER_KEYS - Constant used across modules.
export { CATEGORY_FILTER_KEYS } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CATEGORY_PUBLIC_SORT_OPTIONS - Constant used across modules.
export { CATEGORY_PUBLIC_SORT_OPTIONS } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CATEGORY_SELLER_SORT_OPTIONS - Constant used across modules.
export { CATEGORY_SELLER_SORT_OPTIONS } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CategoriesListView - Component for categories list view.
export { CategoriesListView } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CategoryCard - Component for category card.
export { CategoryCard } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CategoryFilters - Shared export for category filters.
export { CategoryFilters } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CategoryForm - Component for category form.
export { CategoryForm } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CategoryGrid - Component for category grid.
export { CategoryGrid } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CategoryProductsView - Component for category products view.
export { CategoryProductsView } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CategorySelectorCreate - Shared export for category selector create.
export { CategorySelectorCreate } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CategorySortSelect - Component for category sort select.
export { CategorySortSelect } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CategoryTree - Shared export for category tree.
export { CategoryTree } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ConcernCard - Component for concern card.
export { ConcernCard } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ConcernGrid - Component for concern grid.
export { ConcernGrid } from "./features/categories/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_CATEGORY_DATA - Constant used across modules.
export { DEFAULT_CATEGORY_DATA } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MAX_FEATURED_CATEGORIES - Constant used across modules.
export { MAX_FEATURED_CATEGORIES } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MIN_ITEMS_FOR_FEATURED - Constant used across modules.
export { MIN_ITEMS_FOR_FEATURED } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildCategoryColumns - Helper for build category columns.
export { buildCategoryColumns } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildCategoryTree - Helper for build category tree.
export { buildCategoryTree } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// calculateCategoryFields - Helper for calculate category fields.
export { calculateCategoryFields } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// canBeFeatured - Shared export for can be featured.
export { canBeFeatured } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// categoryAdminColumns - Shared export for category admin columns.
export { categoryAdminColumns } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// categoryDisplaySchema - Model for category display schema.
export { categoryDisplaySchema } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// categoryItemSchema - Model for category item schema.
export { categoryItemSchema } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// categoryListParamsSchema - Model for category list params schema.
export { categoryListParamsSchema } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// categoryMetricsSchema - Model for category metrics schema.
export { categoryMetricsSchema } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// categoryQueryHelpers - Shared export for category query helpers.
export { categoryQueryHelpers } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// categorySeoSchema - Model for category seo schema.
export { categorySeoSchema } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// categoryTypeSchema - Model for category type schema.
export { categoryTypeSchema } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createCategoryId - Helper for create category id.
export { createCategoryId } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// flattenCategories - Shared export for flatten categories.
export { flattenCategories } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getCategoryFilterKeys - Helper for get category filter keys.
export { getCategoryFilterKeys } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getCategorySortOptions - Helper for get category sort options.
export { getCategorySortOptions } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getCategoryTableColumns - Helper for get category table columns.
export { getCategoryTableColumns } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isValidCategoryMove - Shared export for is valid category move.
export { isValidCategoryMove } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCategories - React hook for use categories.
export { useCategories } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCategoriesList - React hook for use categories list.
export { useCategoriesList } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCategoryDetail - React hook for use category detail.
export { useCategoryDetail } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCategorySelector - React hook for use category selector.
export { useCategorySelector } from "./features/categories/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCreateCategory - React hook for use create category.
export { useCreateCategory } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BreadcrumbTrailProps - Type contract for breadcrumb trail props.
export type { BreadcrumbTrailProps } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoriesListViewProps - Type contract for categories list view props.
export type { CategoriesListViewProps } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoriesResponse - Type contract for categories response.
export type { CategoriesResponse } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// Category - Type contract for category.
export type { Category } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryAncestor - Type contract for category ancestor.
export type { CategoryAncestor } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryCardProps - Type contract for category card props.
export type { CategoryCardProps } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryCreateInput - Type contract for category create input.
export type { CategoryCreateInput } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryDisplay - Type contract for category display.
export type { CategoryDisplay } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryDocument - Type contract for category document.
export type { CategoryDocument } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryDocumentDisplay - Type contract for category document display.
export type { CategoryDocumentDisplay } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryDocumentMetrics - Type contract for category document metrics.
export type { CategoryDocumentMetrics } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryDocumentSEO - Type contract for category document seo.
export type { CategoryDocumentSEO } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryDrawerMode - Type contract for category drawer mode.
export type { CategoryDrawerMode } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryFilterVariant - Type contract for category filter variant.
export type { CategoryFilterVariant } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryFiltersProps - Type contract for category filters props.
export type { CategoryFiltersProps } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryFormLabels - Type contract for category form labels.
export type { CategoryFormLabels } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryFormProps - Type contract for category form props.
export type { CategoryFormProps } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryGridProps - Type contract for category grid props.
export type { CategoryGridProps } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryItem - Type contract for category item.
export type { CategoryItem } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryMetrics - Type contract for category metrics.
export type { CategoryMetrics } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryMoveInput - Type contract for category move input.
export type { CategoryMoveInput } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryProductsViewProps - Type contract for category products view props.
export type { CategoryProductsViewProps } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategorySelectorCreateLabels - Type contract for category selector create labels.
export type { CategorySelectorCreateLabels } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategorySelectorCreateProps - Type contract for category selector create props.
export type { CategorySelectorCreateProps } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategorySeo - Type contract for category seo.
export type { CategorySeo } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategorySortSelectProps - Type contract for category sort select props.
export type { CategorySortSelectProps } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryTableColumnsLabels - Type contract for category table columns labels.
export type { CategoryTableColumnsLabels } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryTreeNode - Type contract for category tree node.
export type { CategoryTreeNode } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryTreeProps - Type contract for category tree props.
export type { CategoryTreeProps } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryType - Type contract for category type.
export type { CategoryType } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoryUpdateInput - Type contract for category update input.
export type { CategoryUpdateInput } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// Concern - Type contract for concern.
export type { Concern } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ConcernListResponse - Type contract for concern list response.
export type { ConcernListResponse } from "./features/categories/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseCategorySelectorOptions - Type contract for use category selector options.
export type { UseCategorySelectorOptions } from "./features/categories/index";

// ./features/categories/server
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// POST - Constant used across modules.
export { POST } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// categoriesGET - Shared export for categories get.
export { categoriesGET } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// categoryItemDELETE - Shared export for category item delete.
export { categoryItemDELETE } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// categoryItemGET - Shared export for category item get.
export { categoryItemGET } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// categoryItemPATCH - Shared export for category item patch.
export { categoryItemPATCH } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createCategory - Helper for create category.
export { createCategory } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// deleteCategory - Helper for delete category.
export { deleteCategory } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// fetchCategoryTree - Shared export for fetch category tree.
export { fetchCategoryTree } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getCategoryById - Helper for get category by id.
export { getCategoryById } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getCategoryBySlug - Helper for get category by slug.
export { getCategoryBySlug } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getCategoryChildren - Helper for get category children.
export { getCategoryChildren } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listBrandCategories - Helper for list brand categories.
export { listBrandCategories } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listCategories - Helper for list categories.
export { listCategories } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listTopLevelCategories - Helper for list top level categories.
export { listTopLevelCategories } from "./features/categories/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateCategory - Helper for update category.
export { updateCategory } from "./features/categories/server";

// ./features/checkout/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// FailedCheckoutRepository - Shared export for failed checkout repository.
export { FailedCheckoutRepository } from "./features/checkout/server";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// failedCheckoutRepository - Shared export for failed checkout repository.
export { failedCheckoutRepository } from "./features/checkout/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// grantCheckoutConsentViaSms - Shared export for grant checkout consent via sms.
export { grantCheckoutConsentViaSms } from "./features/checkout/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sendCheckoutConsentOtp - Shared export for send checkout consent otp.
export { sendCheckoutConsentOtp } from "./features/checkout/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// verifyCheckoutConsentOtp - Shared export for verify checkout consent otp.
export { verifyCheckoutConsentOtp } from "./features/checkout/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FailedCheckoutMeta - Type contract for failed checkout meta.
export type { FailedCheckoutMeta } from "./features/checkout/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FailedPaymentMeta - Type contract for failed payment meta.
export type { FailedPaymentMeta } from "./features/checkout/server";

// ./features/checkout/schemas/index
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// FAILED_CHECKOUTS_COLLECTION - Constant used across modules.
export { FAILED_CHECKOUTS_COLLECTION } from "./features/checkout/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// FAILED_PAYMENTS_COLLECTION - Constant used across modules.
export { FAILED_PAYMENTS_COLLECTION } from "./features/checkout/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FailedCheckoutDocument - Type contract for failed checkout document.
export type { FailedCheckoutDocument } from "./features/checkout/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FailedCheckoutReason - Type contract for failed checkout reason.
export type { FailedCheckoutReason } from "./features/checkout/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FailedPaymentDocument - Type contract for failed payment document.
export type { FailedPaymentDocument } from "./features/checkout/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FailedPaymentReason - Type contract for failed payment reason.
export type { FailedPaymentReason } from "./features/checkout/schemas/index";

// ./features/cms/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CMS_FEATURE - Constant used across modules.
export { CMS_FEATURE } from "./features/cms/index";

// ./features/collections/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CollectionCard - Component for collection card.
export { CollectionCard } from "./features/collections/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CollectionGrid - Component for collection grid.
export { CollectionGrid } from "./features/collections/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildCollectionColumns - Helper for build collection columns.
export { buildCollectionColumns } from "./features/collections/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// collectionAdminColumns - Shared export for collection admin columns.
export { collectionAdminColumns } from "./features/collections/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// collectionItemSchema - Model for collection item schema.
export { collectionItemSchema } from "./features/collections/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// collectionListItemSchema - Model for collection list item schema.
export { collectionListItemSchema } from "./features/collections/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCollection - React hook for use collection.
export { useCollection } from "./features/collections/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCollections - React hook for use collections.
export { useCollections } from "./features/collections/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CollectionItem - Type contract for collection item.
export type { CollectionItem } from "./features/collections/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CollectionListItem - Type contract for collection list item.
export type { CollectionListItem } from "./features/collections/index";

// ./features/collections/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// CollectionsRepository - Shared export for collections repository.
export { CollectionsRepository } from "./features/collections/server";

// ./features/consultation/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ConsultationForm - Component for consultation form.
export { ConsultationForm } from "./features/consultation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// bookConsultationSchema - Model for book consultation schema.
export { bookConsultationSchema } from "./features/consultation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildConsultationColumns - Helper for build consultation columns.
export { buildConsultationColumns } from "./features/consultation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// consultationAdminColumns - Shared export for consultation admin columns.
export { consultationAdminColumns } from "./features/consultation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// consultationBookingSchema - Model for consultation booking schema.
export { consultationBookingSchema } from "./features/consultation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// consultationModeSchema - Model for consultation mode schema.
export { consultationModeSchema } from "./features/consultation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// consultationStatusSchema - Model for consultation status schema.
export { consultationStatusSchema } from "./features/consultation/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useBookConsultation - React hook for use book consultation.
export { useBookConsultation } from "./features/consultation/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BookConsultationInput - Type contract for book consultation input.
export type { BookConsultationInput } from "./features/consultation/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ConsultationBooking - Type contract for consultation booking.
export type { ConsultationBooking } from "./features/consultation/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ConsultationListResponse - Type contract for consultation list response.
export type { ConsultationListResponse } from "./features/consultation/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ConsultationMode - Type contract for consultation mode.
export type { ConsultationMode } from "./features/consultation/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ConsultationStatus - Type contract for consultation status.
export type { ConsultationStatus } from "./features/consultation/index";

// ./features/consultation/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// ConsultationsRepository - Shared export for consultations repository.
export { ConsultationsRepository } from "./features/consultation/server";

// ./features/contact/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ContactForm - Component for contact form.
export { ContactForm } from "./features/contact/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ContactInfoSidebar - Shared export for contact info sidebar.
export { ContactInfoSidebar } from "./features/contact/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useContactSubmit - React hook for use contact submit.
export { useContactSubmit } from "./features/contact/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ContactFormProps - Type contract for contact form props.
export type { ContactFormProps } from "./features/contact/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ContactInfoItem - Type contract for contact info item.
export type { ContactInfoItem } from "./features/contact/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ContactInfoSidebarProps - Type contract for contact info sidebar props.
export type { ContactInfoSidebarProps } from "./features/contact/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseContactSubmitOptions - Type contract for use contact submit options.
export type { UseContactSubmitOptions } from "./features/contact/index";

// ./features/contact/server
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sendContactEmail - Shared export for send contact email.
export { sendContactEmail } from "./features/contact/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sendEmail - Shared export for send email.
export { sendEmail } from "./features/contact/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sendOrderConfirmationEmail - Shared export for send order confirmation email.
export { sendOrderConfirmationEmail } from "./features/contact/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sendPasswordResetEmailWithLink - Shared export for send password reset email with link.
export { sendPasswordResetEmailWithLink } from "./features/contact/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sendSiteSettingsChangedEmail - Shared export for send site settings changed email.
export { sendSiteSettingsChangedEmail } from "./features/contact/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sendVerificationEmailWithLink - Shared export for send verification email with link.
export { sendVerificationEmailWithLink } from "./features/contact/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderConfirmationEmailParams - Type contract for order confirmation email params.
export type { OrderConfirmationEmailParams } from "./features/contact/server";

// ./features/copilot/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminCopilotView - Component for admin copilot view.
export { AdminCopilotView } from "./features/copilot/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCopilotChat - React hook for use copilot chat.
export { useCopilotChat } from "./features/copilot/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCopilotFeedback - React hook for use copilot feedback.
export { useCopilotFeedback } from "./features/copilot/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminCopilotViewProps - Type contract for admin copilot view props.
export type { AdminCopilotViewProps } from "./features/copilot/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CopilotChatResponse - Type contract for copilot chat response.
export type { CopilotChatResponse } from "./features/copilot/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CopilotMessage - Type contract for copilot message.
export type { CopilotMessage } from "./features/copilot/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseCopilotFeedbackOptions - Type contract for use copilot feedback options.
export type { UseCopilotFeedbackOptions } from "./features/copilot/index";

// ./features/corporate/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CorporateInquiryForm - Component for corporate inquiry form.
export { CorporateInquiryForm } from "./features/corporate/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildCorporateColumns - Helper for build corporate columns.
export { buildCorporateColumns } from "./features/corporate/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// corporateAdminColumns - Shared export for corporate admin columns.
export { corporateAdminColumns } from "./features/corporate/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// corporateInquirySchema - Model for corporate inquiry schema.
export { corporateInquirySchema } from "./features/corporate/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// corporateInquiryStatusSchema - Model for corporate inquiry status schema.
export { corporateInquiryStatusSchema } from "./features/corporate/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// submitCorporateInquirySchema - Model for submit corporate inquiry schema.
export { submitCorporateInquirySchema } from "./features/corporate/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useSubmitCorporateInquiry - React hook for use submit corporate inquiry.
export { useSubmitCorporateInquiry } from "./features/corporate/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CorporateInquiry - Type contract for corporate inquiry.
export type { CorporateInquiry } from "./features/corporate/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CorporateInquiryListResponse - Type contract for corporate inquiry list response.
export type { CorporateInquiryListResponse } from "./features/corporate/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CorporateInquiryStatus - Type contract for corporate inquiry status.
export type { CorporateInquiryStatus } from "./features/corporate/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SubmitCorporateInquiryInput - Type contract for submit corporate inquiry input.
export type { SubmitCorporateInquiryInput } from "./features/corporate/index";

// ./features/corporate/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// CorporateRepository - Shared export for corporate repository.
export { CorporateRepository } from "./features/corporate/server";

// ./features/cron/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// _resetCronRegistry - Shared export for reset cron registry.
export { _resetCronRegistry } from "./features/cron/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createAuctionExpiryJob - Helper for create auction expiry job.
export { createAuctionExpiryJob } from "./features/cron/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createCronJob - Helper for create cron job.
export { createCronJob } from "./features/cron/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createPreOrderReminderJob - Helper for create pre order reminder job.
export { createPreOrderReminderJob } from "./features/cron/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// findCronJob - Shared export for find cron job.
export { findCronJob } from "./features/cron/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getCronRegistry - Helper for get cron registry.
export { getCronRegistry } from "./features/cron/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getCronRegistrySummary - Helper for get cron registry summary.
export { getCronRegistrySummary } from "./features/cron/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// runJob - Helper for run job.
export { runJob } from "./features/cron/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// wrapPubSub - Shared export for wrap pub sub.
export { wrapPubSub } from "./features/cron/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// wrapScheduled - Shared export for wrap scheduled.
export { wrapScheduled } from "./features/cron/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateCronJobOptions - Type contract for create cron job options.
export type { CreateCronJobOptions } from "./features/cron/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CronJobDefinition - Type contract for cron job definition.
export type { CronJobDefinition } from "./features/cron/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CronRegistry - Type contract for cron registry.
export type { CronRegistry } from "./features/cron/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CronRegistrySummary - Type contract for cron registry summary.
export type { CronRegistrySummary } from "./features/cron/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// JobContext - Type contract for job context.
export type { JobContext } from "./features/cron/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// JobResult - Type contract for job result.
export type { JobResult } from "./features/cron/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PubSubJobFn - Type contract for pub sub job fn.
export type { PubSubJobFn } from "./features/cron/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ScheduledJobFn - Type contract for scheduled job fn.
export type { ScheduledJobFn } from "./features/cron/index";

// ./features/events/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminEventEntriesView - Component for admin event entries view.
export { AdminEventEntriesView } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdminEventsView - Component for admin events view.
export { AdminEventsView } from "./features/events/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// EVENTS_COLLECTION - Constant used across modules.
export { EVENTS_COLLECTION } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EVENT_ADMIN_SORT_OPTIONS - Constant used across modules.
export { EVENT_ADMIN_SORT_OPTIONS } from "./features/events/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// EVENT_ENTRIES_COLLECTION - Constant used across modules.
export { EVENT_ENTRIES_COLLECTION } from "./features/events/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// EVENT_ENTRY_FIELDS - Constant used across modules.
export { EVENT_ENTRY_FIELDS } from "./features/events/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// EVENT_ENTRY_INDEXED_FIELDS - Constant used across modules.
export { EVENT_ENTRY_INDEXED_FIELDS } from "./features/events/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// EVENT_FIELDS - Constant used across modules.
export { EVENT_FIELDS } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EVENT_FILTER_KEYS - Constant used across modules.
export { EVENT_FILTER_KEYS } from "./features/events/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// EVENT_INDEXED_FIELDS - Constant used across modules.
export { EVENT_INDEXED_FIELDS } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EVENT_PUBLIC_SORT_OPTIONS - Constant used across modules.
export { EVENT_PUBLIC_SORT_OPTIONS } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EVENT_SELLER_SORT_OPTIONS - Constant used across modules.
export { EVENT_SELLER_SORT_OPTIONS } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EVENT_SORT_OPTIONS - Constant used across modules.
export { EVENT_SORT_OPTIONS } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EventBanner - Shared export for event banner.
export { EventBanner } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EventCard - Component for event card.
export { EventCard } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EventDetailView - Component for event detail view.
export { EventDetailView } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EventFilters - Shared export for event filters.
export { EventFilters } from "./features/events/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// EventFormDrawer - Component for event form drawer.
export { EventFormDrawer } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EventLeaderboard - Shared export for event leaderboard.
export { EventLeaderboard } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EventParticipateView - Component for event participate view.
export { EventParticipateView } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EventStatusBadge - Component for event status badge.
export { EventStatusBadge } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// EventsListView - Component for events list view.
export { EventsListView } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildEventColumns - Helper for build event columns.
export { buildEventColumns } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// eventAdminColumns - Shared export for event admin columns.
export { eventAdminColumns } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// eventItemSchema - Model for event item schema.
export { eventItemSchema } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// eventListParamsSchema - Model for event list params schema.
export { eventListParamsSchema } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// eventQueryHelpers - Shared export for event query helpers.
export { eventQueryHelpers } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// eventStatusSchema - Model for event status schema.
export { eventStatusSchema } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// eventTypeSchema - Model for event type schema.
export { eventTypeSchema } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getEventFilterKeys - Helper for get event filter keys.
export { getEventFilterKeys } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getEventSortOptions - Helper for get event sort options.
export { getEventSortOptions } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// offerConfigSchema - Model for offer config schema.
export { offerConfigSchema } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// pollConfigSchema - Model for poll config schema.
export { pollConfigSchema } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// saleConfigSchema - Model for sale config schema.
export { saleConfigSchema } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useBulkEvent - React hook for use bulk event.
export { useBulkEvent } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useEvent - React hook for use event.
export { useEvent } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useEventEntries - React hook for use event entries.
export { useEventEntries } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useEventLeaderboard - React hook for use event leaderboard.
export { useEventLeaderboard } from "./features/events/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useEvents - React hook for use events.
export { useEvents } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminEventEntriesViewProps - Type contract for admin event entries view props.
export type { AdminEventEntriesViewProps } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdminEventsViewProps - Type contract for admin events view props.
export type { AdminEventsViewProps } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BulkEventStatus - Type contract for bulk event status.
export type { BulkEventStatus } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateEventEntryInput - Type contract for create event entry input.
export type { CreateEventEntryInput } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EntryReviewStatus - Type contract for entry review status.
export type { EntryReviewStatus } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventBannerProps - Type contract for event banner props.
export type { EventBannerProps } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventCreateInput - Type contract for event create input.
export type { EventCreateInput } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventDetailViewProps - Type contract for event detail view props.
export type { EventDetailViewProps } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventDocument - Type contract for event document.
export type { EventDocument } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventEntryCreateInput - Type contract for event entry create input.
export type { EventEntryCreateInput } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventEntryDocument - Type contract for event entry document.
export type { EventEntryDocument } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventEntryItem - Type contract for event entry item.
export type { EventEntryItem } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventEntryListResponse - Type contract for event entry list response.
export type { EventEntryListResponse } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventFilterVariant - Type contract for event filter variant.
export type { EventFilterVariant } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventFiltersProps - Type contract for event filters props.
export type { EventFiltersProps } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventFormDrawerProps - Type contract for event form drawer props.
export type { EventFormDrawerProps } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventItem - Type contract for event item.
export type { EventItem } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventLeaderboardProps - Type contract for event leaderboard props.
export type { EventLeaderboardProps } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventListParams - Type contract for event list params.
export type { EventListParams } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventListResponse - Type contract for event list response.
export type { EventListResponse } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventParticipateViewProps - Type contract for event participate view props.
export type { EventParticipateViewProps } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventStatus - Type contract for event status.
export type { EventStatus } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventType - Type contract for event type.
export type { EventType } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventUpdateInput - Type contract for event update input.
export type { EventUpdateInput } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FeedbackConfig - Type contract for feedback config.
export type { FeedbackConfig } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FormFieldType - Type contract for form field type.
export type { FormFieldType } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LeaderboardEntry - Type contract for leaderboard entry.
export type { LeaderboardEntry } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OfferConfig - Type contract for offer config.
export type { OfferConfig } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PollConfig - Type contract for poll config.
export type { PollConfig } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PollResultsVisibility - Type contract for poll results visibility.
export type { PollResultsVisibility } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SaleConfig - Type contract for sale config.
export type { SaleConfig } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SurveyConfig - Type contract for survey config.
export type { SurveyConfig } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SurveyFormField - Type contract for survey form field.
export type { SurveyFormField } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseBulkEventOptions - Type contract for use bulk event options.
export type { UseBulkEventOptions } from "./features/events/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseBulkEventReturn - Type contract for use bulk event return.
export type { UseBulkEventReturn } from "./features/events/index";

// ./features/events/server
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminGetEventById - Shared export for admin get event by id.
export { adminGetEventById } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminGetEventEntries - Shared export for admin get event entries.
export { adminGetEventEntries } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminGetEventStats - Shared export for admin get event stats.
export { adminGetEventStats } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminListEvents - Shared export for admin list events.
export { adminListEvents } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminUpdateEventEntry - Model for admin update event entry.
export { adminUpdateEventEntry } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// changeEventStatus - Model for change event status.
export { changeEventStatus } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createEvent - Helper for create event.
export { createEvent } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// deleteEvent - Helper for delete event.
export { deleteEvent } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// enterEvent - Model for enter event.
export { enterEvent } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// eventIdGET - Shared export for event id get.
export { eventIdGET } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// eventsGET - Shared export for events get.
export { eventsGET } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getEventLeaderboard - Helper for get event leaderboard.
export { getEventLeaderboard } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getPublicEventById - Helper for get public event by id.
export { getPublicEventById } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listPublicEvents - Helper for list public events.
export { listPublicEvents } from "./features/events/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateEvent - Helper for update event.
export { updateEvent } from "./features/events/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateEventInput - Type contract for create event input.
export type { CreateEventInput } from "./features/events/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EnterEventInput - Type contract for enter event input.
export type { EnterEventInput } from "./features/events/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UpdateEventInput - Type contract for update event input.
export type { UpdateEventInput } from "./features/events/server";

// ./features/faq/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ContactCTA - Shared export for contact cta.
export { ContactCTA } from "./features/faq/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_FAQ_DATA - Constant used across modules.
export { DEFAULT_FAQ_DATA } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FAQAccordion - Shared export for faqaccordion.
export { FAQAccordion } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FAQCategorySidebar - Shared export for faqcategory sidebar.
export { FAQCategorySidebar } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FAQCategoryTabs - Component for faqcategory tabs.
export { FAQCategoryTabs } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FAQHelpfulButtons - Shared export for faqhelpful buttons.
export { FAQHelpfulButtons } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FAQPageContent - Shared export for faqpage content.
export { FAQPageContent } from "./features/faq/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// FAQS_COLLECTION - Constant used across modules.
export { FAQS_COLLECTION } from "./features/faq/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// FAQS_INDEXED_FIELDS - Constant used across modules.
export { FAQS_INDEXED_FIELDS } from "./features/faq/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// FAQS_PUBLIC_FIELDS - Constant used across modules.
export { FAQS_PUBLIC_FIELDS } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FAQSortDropdown - Shared export for faqsort dropdown.
export { FAQSortDropdown } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FAQ_CATEGORY_LABELS - Constant used across modules.
export { FAQ_CATEGORY_LABELS } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// RelatedFAQs - Shared export for related faqs.
export { RelatedFAQs } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildFaqColumns - Helper for build faq columns.
export { buildFaqColumns } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createFAQId - Helper for create faqid.
export { createFAQId } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// extractVariablePlaceholders - Shared export for extract variable placeholders.
export { extractVariablePlaceholders } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// faqAdminColumns - Shared export for faq admin columns.
export { faqAdminColumns } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// faqAnswerFormatSchema - Model for faq answer format schema.
export { faqAnswerFormatSchema } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// faqAnswerSchema - Model for faq answer schema.
export { faqAnswerSchema } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// faqCategorySchema - Model for faq category schema.
export { faqCategorySchema } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// faqListParamsSchema - Model for faq list params schema.
export { faqListParamsSchema } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// faqQueryHelpers - Shared export for faq query helpers.
export { faqQueryHelpers } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// faqSchema - Model for faq schema.
export { faqSchema } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// faqStatsSchema - Model for faq stats schema.
export { faqStatsSchema } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getHelpfulnessPercentage - Helper for get helpfulness percentage.
export { getHelpfulnessPercentage } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isPopularFAQ - Shared export for is popular faq.
export { isPopularFAQ } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// slugifyQuestion - Shared export for slugify question.
export { slugifyQuestion } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useFAQ - React hook for use faq.
export { useFAQ } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useFAQs - React hook for use faqs.
export { useFAQs } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useFaqList - React hook for use faq list.
export { useFaqList } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useFaqVote - React hook for use faq vote.
export { useFaqVote } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// usesVariableInterpolation - Shared export for uses variable interpolation.
export { usesVariableInterpolation } from "./features/faq/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// validateFAQVariables - Helper for validate faqvariables.
export { validateFAQVariables } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQ - Type contract for faq.
export type { FAQ } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQAnswer - Type contract for faqanswer.
export type { FAQAnswer } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQAnswerFormat - Type contract for faqanswer format.
export type { FAQAnswerFormat } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQCategory - Type contract for faqcategory.
export type { FAQCategory } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQCategoryItem - Type contract for faqcategory item.
export type { FAQCategoryItem } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQCreateInput - Type contract for faqcreate input.
export type { FAQCreateInput } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQDocument - Type contract for faqdocument.
export type { FAQDocument } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQListParams - Type contract for faqlist params.
export type { FAQListParams } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQListResponse - Type contract for faqlist response.
export type { FAQListResponse } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQSEO - Type contract for faqseo.
export type { FAQSEO } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQSortOption - Type contract for faqsort option.
export type { FAQSortOption } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQStats - Type contract for faqstats.
export type { FAQStats } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQUpdateInput - Type contract for faqupdate input.
export type { FAQUpdateInput } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQWithInterpolatedAnswer - Type contract for faqwith interpolated answer.
export type { FAQWithInterpolatedAnswer } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseFaqVoteOptions - Type contract for use faq vote options.
export type { UseFaqVoteOptions } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// VoteFaqInput - Type contract for vote faq input.
export type { VoteFaqInput } from "./features/faq/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// VoteFaqResult - Type contract for vote faq result.
export type { VoteFaqResult } from "./features/faq/index";

// ./features/faq/server
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createFaq - Helper for create faq.
export { createFaq } from "./features/faq/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// deleteFaq - Helper for delete faq.
export { deleteFaq } from "./features/faq/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// faqCreateSchema - Model for faq create schema.
export { faqCreateSchema } from "./features/faq/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// faqGET - Shared export for faq get.
export { faqGET } from "./features/faq/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// faqUpdateSchema - Model for faq update schema.
export { faqUpdateSchema } from "./features/faq/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getFaqById - Helper for get faq by id.
export { getFaqById } from "./features/faq/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listFaqs - Helper for list faqs.
export { listFaqs } from "./features/faq/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listPublicFaqs - Helper for list public faqs.
export { listPublicFaqs } from "./features/faq/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateFaq - Helper for update faq.
export { updateFaq } from "./features/faq/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// voteFaq - Shared export for vote faq.
export { voteFaq } from "./features/faq/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FaqCreateInput - Type contract for faq create input.
export type { FaqCreateInput } from "./features/faq/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FaqUpdateInput - Type contract for faq update input.
export type { FaqUpdateInput } from "./features/faq/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// VoteFaqActionInput - Type contract for vote faq action input.
export type { VoteFaqActionInput } from "./features/faq/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// VoteFaqActionResult - Type contract for vote faq action result.
export type { VoteFaqActionResult } from "./features/faq/server";

// ./features/filters/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FilterFacetSection - Shared export for filter facet section.
export { FilterFacetSection } from "./features/filters/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FilterPanel - Shared export for filter panel.
export { FilterPanel } from "./features/filters/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// RangeFilter - Shared export for range filter.
export { RangeFilter } from "./features/filters/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SwitchFilter - Shared export for switch filter.
export { SwitchFilter } from "./features/filters/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getFilterLabel - Helper for get filter label.
export { getFilterLabel } from "./features/filters/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getFilterValue - Helper for get filter value.
export { getFilterValue } from "./features/filters/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FacetMultiConfig - Type contract for facet multi config.
export type { FacetMultiConfig } from "./features/filters/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FacetOption - Type contract for facet option.
export type { FacetOption } from "./features/filters/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FacetSingleConfig - Type contract for facet single config.
export type { FacetSingleConfig } from "./features/filters/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FilterConfig - Type contract for filter config.
export type { FilterConfig } from "./features/filters/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FilterFacetSectionProps - Type contract for filter facet section props.
export type { FilterFacetSectionProps } from "./features/filters/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FilterPanelProps - Type contract for filter panel props.
export type { FilterPanelProps } from "./features/filters/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RangeDateConfig - Type contract for range date config.
export type { RangeDateConfig } from "./features/filters/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RangeFilterProps - Type contract for range filter props.
export type { RangeFilterProps } from "./features/filters/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RangeNumberConfig - Type contract for range number config.
export type { RangeNumberConfig } from "./features/filters/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SwitchConfig - Type contract for switch config.
export type { SwitchConfig } from "./features/filters/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SwitchFilterProps - Type contract for switch filter props.
export type { SwitchFilterProps } from "./features/filters/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UrlTable - Type contract for url table.
export type { UrlTable } from "./features/filters/index";

// ./features/forms/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// INPUT_BASE - Constant used across modules.
export { INPUT_BASE } from "./features/forms/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// INPUT_DISABLED - Constant used across modules.
export { INPUT_DISABLED } from "./features/forms/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// INPUT_ERROR - Constant used across modules.
export { INPUT_ERROR } from "./features/forms/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// INPUT_SUCCESS - Constant used across modules.
export { INPUT_SUCCESS } from "./features/forms/index";

// ./features/homepage/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AdvertisementBanner - Shared export for advertisement banner.
export { AdvertisementBanner } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BeforeAfterCard - Component for before after card.
export { BeforeAfterCard } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CharacterHotspot - Shared export for character hotspot.
export { CharacterHotspot } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CharacterHotspotForm - Component for character hotspot form.
export { CharacterHotspotForm } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CustomerReviewsSection - Shared export for customer reviews section.
export { CustomerReviewsSection } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FAQSection - Shared export for faqsection.
export { FAQSection } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FeaturedResultsSection - Shared export for featured results section.
export { FeaturedResultsSection } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HeroBanner - Shared export for hero banner.
export { HeroBanner } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HeroCarousel - Shared export for hero carousel.
export { HeroCarousel } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HeroSection - Shared export for hero section.
export { HeroSection } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HomepageSkeleton - Shared export for homepage skeleton.
export { HomepageSkeleton } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HomepageView - Component for homepage view.
export { HomepageView } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HowItWorksInfoView - Component for how it works info view.
export { HowItWorksInfoView } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// HowItWorksSection - Shared export for how it works section.
export { HowItWorksSection } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MarketplaceHomepageView - Component for marketplace homepage view.
export { MarketplaceHomepageView } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// NewsletterBanner - Shared export for newsletter banner.
export { NewsletterBanner } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// NewsletterSection - Shared export for newsletter section.
export { NewsletterSection } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PromoGrid - Component for promo grid.
export { PromoGrid } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SectionCarousel - Shared export for section carousel.
export { SectionCarousel } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SecurityHighlightsSection - Shared export for security highlights section.
export { SecurityHighlightsSection } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SiteFeaturesSection - Shared export for site features section.
export { SiteFeaturesSection } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StatsCounterSection - Shared export for stats counter section.
export { StatsCounterSection } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TestimonialsCarousel - Shared export for testimonials carousel.
export { TestimonialsCarousel } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TrustBadges - Shared export for trust badges.
export { TrustBadges } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TrustFeaturesSection - Shared export for trust features section.
export { TrustFeaturesSection } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TrustIndicatorsSection - Shared export for trust indicators section.
export { TrustIndicatorsSection } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// WelcomeSection - Shared export for welcome section.
export { WelcomeSection } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// WhatsAppCommunitySection - Shared export for whats app community section.
export { WhatsAppCommunitySection } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useBlogArticles - React hook for use blog articles.
export { useBlogArticles } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useFeaturedAuctions - React hook for use featured auctions.
export { useFeaturedAuctions } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useFeaturedPreOrders - React hook for use featured pre orders.
export { useFeaturedPreOrders } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useFeaturedProducts - React hook for use featured products.
export { useFeaturedProducts } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useHeroCarousel - React hook for use hero carousel.
export { useHeroCarousel } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useHomepage - React hook for use homepage.
export { useHomepage } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useHomepageReviews - React hook for use homepage reviews.
export { useHomepageReviews } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useHomepageSections - React hook for use homepage sections.
export { useHomepageSections } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useNewsletter - React hook for use newsletter.
export { useNewsletter } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useTopBrands - React hook for use top brands.
export { useTopBrands } from "./features/homepage/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useTopCategories - React hook for use top categories.
export { useTopCategories } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AdvertisementBannerProps - Type contract for advertisement banner props.
export type { AdvertisementBannerProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// Banner - Type contract for banner.
export type { Banner } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BeforeAfterCardProps - Type contract for before after card props.
export type { BeforeAfterCardProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogListResult - Type contract for blog list result.
export type { BlogListResult } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CarouselSlide - Type contract for carousel slide.
export type { CarouselSlide } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CarouselSlideCard - Type contract for carousel slide card.
export type { CarouselSlideCard } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CharacterHotspotConfig - Type contract for character hotspot config.
export type { CharacterHotspotConfig } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CharacterHotspotFormProps - Type contract for character hotspot form props.
export type { CharacterHotspotFormProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CharacterHotspotProps - Type contract for character hotspot props.
export type { CharacterHotspotProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CustomerReviewsSectionProps - Type contract for customer reviews section props.
export type { CustomerReviewsSectionProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQItem - Type contract for faqitem.
export type { FAQItem } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQSectionProps - Type contract for faqsection props.
export type { FAQSectionProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQTab - Type contract for faqtab.
export type { FAQTab } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FeaturedResultItem - Type contract for featured result item.
export type { FeaturedResultItem } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FeaturedResultsSectionProps - Type contract for featured results section props.
export type { FeaturedResultsSectionProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HeroBannerProps - Type contract for hero banner props.
export type { HeroBannerProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HeroCarouselProps - Type contract for hero carousel props.
export type { HeroCarouselProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HomepageData - Type contract for homepage data.
export type { HomepageData } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HomepageSection - Type contract for homepage section.
export type { HomepageSection } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HomepageSectionContent - Type contract for homepage section content.
export type { HomepageSectionContent } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HomepageViewProps - Type contract for homepage view props.
export type { HomepageViewProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HotspotPin - Type contract for hotspot pin.
export type { HotspotPin } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HowItWorksInfoDetail - Type contract for how it works info detail.
export type { HowItWorksInfoDetail } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HowItWorksInfoStep - Type contract for how it works info step.
export type { HowItWorksInfoStep } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HowItWorksInfoViewProps - Type contract for how it works info view props.
export type { HowItWorksInfoViewProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HowItWorksSectionProps - Type contract for how it works section props.
export type { HowItWorksSectionProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HowItWorksStep - Type contract for how it works step.
export type { HowItWorksStep } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NewsletterBannerProps - Type contract for newsletter banner props.
export type { NewsletterBannerProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NewsletterSectionProps - Type contract for newsletter section props.
export type { NewsletterSectionProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PromoBanner - Type contract for promo banner.
export type { PromoBanner } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PromoGridProps - Type contract for promo grid props.
export type { PromoGridProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SectionCarouselProps - Type contract for section carousel props.
export type { SectionCarouselProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SecurityHighlightItem - Type contract for security highlight item.
export type { SecurityHighlightItem } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SecurityHighlightsSectionProps - Type contract for security highlights section props.
export type { SecurityHighlightsSectionProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SiteFeatureItem - Type contract for site feature item.
export type { SiteFeatureItem } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SiteFeaturesSectionProps - Type contract for site features section props.
export type { SiteFeaturesSectionProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StatsCounterSectionProps - Type contract for stats counter section props.
export type { StatsCounterSectionProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SubscribeNewsletterInput - Type contract for subscribe newsletter input.
export type { SubscribeNewsletterInput } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SubscribeNewsletterResult - Type contract for subscribe newsletter result.
export type { SubscribeNewsletterResult } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// Testimonial - Type contract for testimonial.
export type { Testimonial } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TestimonialsCarouselProps - Type contract for testimonials carousel props.
export type { TestimonialsCarouselProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TrustBadge - Type contract for trust badge.
export type { TrustBadge } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TrustBadgeIconKey - Type contract for trust badge icon key.
export type { TrustBadgeIconKey } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TrustBadgesProps - Type contract for trust badges props.
export type { TrustBadgesProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TrustFeatureItem - Type contract for trust feature item.
export type { TrustFeatureItem } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TrustFeaturesSectionProps - Type contract for trust features section props.
export type { TrustFeaturesSectionProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TrustIndicatorItem - Type contract for trust indicator item.
export type { TrustIndicatorItem } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TrustIndicatorsSectionProps - Type contract for trust indicators section props.
export type { TrustIndicatorsSectionProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseHomepageReviewsOptions - Type contract for use homepage reviews options.
export type { UseHomepageReviewsOptions } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseNewsletterOptions - Type contract for use newsletter options.
export type { UseNewsletterOptions } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// WelcomeSectionChip - Type contract for welcome section chip.
export type { WelcomeSectionChip } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// WelcomeSectionProps - Type contract for welcome section props.
export type { WelcomeSectionProps } from "./features/homepage/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// WhatsAppCommunitySectionProps - Type contract for whats app community section props.
export type { WhatsAppCommunitySectionProps } from "./features/homepage/index";

// ./features/homepage/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// HomepageSectionsRepository - Shared export for homepage sections repository.
export { HomepageSectionsRepository } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// carouselGET - Shared export for carousel get.
export { carouselGET } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// carouselItemDELETE - Shared export for carousel item delete.
export { carouselItemDELETE } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// carouselItemGET - Shared export for carousel item get.
export { carouselItemGET } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// carouselItemPATCH - Shared export for carousel item patch.
export { carouselItemPATCH } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// carouselPOST - Shared export for carousel post.
export { carouselPOST } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createCarouselSlide - Helper for create carousel slide.
export { createCarouselSlide } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createHomepageSection - Helper for create homepage section.
export { createHomepageSection } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createSectionSchema - Helper for create section schema.
export { createSectionSchema } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// deleteCarouselSlide - Helper for delete carousel slide.
export { deleteCarouselSlide } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// deleteHomepageSection - Helper for delete homepage section.
export { deleteHomepageSection } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getCarouselSlideById - Helper for get carousel slide by id.
export { getCarouselSlideById } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getHomepageSectionById - Helper for get homepage section by id.
export { getHomepageSectionById } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// homepageGET - Shared export for homepage get.
export { homepageGET } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// homepageSectionItemDELETE - Shared export for homepage section item delete.
export { homepageSectionItemDELETE } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// homepageSectionItemGET - Shared export for homepage section item get.
export { homepageSectionItemGET } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// homepageSectionItemPATCH - Shared export for homepage section item patch.
export { homepageSectionItemPATCH } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listActiveCarouselSlides - Helper for list active carousel slides.
export { listActiveCarouselSlides } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listAllCarouselSlides - Helper for list all carousel slides.
export { listAllCarouselSlides } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listEnabledHomepageSections - Helper for list enabled homepage sections.
export { listEnabledHomepageSections } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listHomepageSections - Helper for list homepage sections.
export { listHomepageSections } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// reorderCarouselSlides - Shared export for reorder carousel slides.
export { reorderCarouselSlides } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// reorderHomepageSections - Shared export for reorder homepage sections.
export { reorderHomepageSections } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateCarouselSlide - Helper for update carousel slide.
export { updateCarouselSlide } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateHomepageSection - Helper for update homepage section.
export { updateHomepageSection } from "./features/homepage/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateSectionSchema - Helper for update section schema.
export { updateSectionSchema } from "./features/homepage/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CarouselSlideInput - Type contract for carousel slide input.
export type { CarouselSlideInput } from "./features/homepage/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CarouselSlideUpdateInput - Type contract for carousel slide update input.
export type { CarouselSlideUpdateInput } from "./features/homepage/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateHomepageSectionInput - Type contract for create homepage section input.
export type { CreateHomepageSectionInput } from "./features/homepage/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UpdateHomepageSectionInput - Type contract for update homepage section input.
export type { UpdateHomepageSectionInput } from "./features/homepage/server";

// ./features/layout/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AppLayoutShell - Component for app layout shell.
export { AppLayoutShell } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AutoBreadcrumbs - Shared export for auto breadcrumbs.
export { AutoBreadcrumbs } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BackToTop - Shared export for back to top.
export { BackToTop } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BottomActions - Shared export for bottom actions.
export { BottomActions } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BottomActionsProvider - Component for bottom actions provider.
export { BottomActionsProvider } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BottomNavItem - Model for bottom nav item.
export { BottomNavItem } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BottomNavLayout - Component for bottom nav layout.
export { BottomNavLayout } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BottomNavbar - Shared export for bottom navbar.
export { BottomNavbar } from "./features/layout/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// BottomSheet - Shared export for bottom sheet.
export { BottomSheet } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Breadcrumbs - Shared export for breadcrumbs.
export { Breadcrumbs } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// DashboardNavProvider - Component for dashboard nav provider.
export { DashboardNavProvider } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// FooterLayout - Component for footer layout.
export { FooterLayout } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// LayoutClient - Shared export for layout client.
export { LayoutClient } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// LocaleSwitcher - Shared export for locale switcher.
export { LocaleSwitcher } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MainNavbar - Shared export for main navbar.
export { MainNavbar } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// NavbarLayout - Component for navbar layout.
export { NavbarLayout } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SidebarLayout - Component for sidebar layout.
export { SidebarLayout } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TitleBar - Shared export for title bar.
export { TitleBar } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// TitleBarLayout - Component for title bar layout.
export { TitleBarLayout } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useBottomActions - React hook for use bottom actions.
export { useBottomActions } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useBottomActionsContext - React hook for use bottom actions context.
export { useBottomActionsContext } from "./features/layout/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useDashboardNav - React hook for use dashboard nav.
export { useDashboardNav } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AppLayoutShellProps - Type contract for app layout shell props.
export type { AppLayoutShellProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AutoBreadcrumbsProps - Type contract for auto breadcrumbs props.
export type { AutoBreadcrumbsProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BackToTopProps - Type contract for back to top props.
export type { BackToTopProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BottomAction - Type contract for bottom action.
export type { BottomAction } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BottomActionsState - Type contract for bottom actions state.
export type { BottomActionsState } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BottomBulkConfig - Type contract for bottom bulk config.
export type { BottomBulkConfig } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BottomNavItemProps - Type contract for bottom nav item props.
export type { BottomNavItemProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BottomNavLayoutProps - Type contract for bottom nav layout props.
export type { BottomNavLayoutProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BottomNavbarProps - Type contract for bottom navbar props.
export type { BottomNavbarProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BottomSheetProps - Type contract for bottom sheet props.
export type { BottomSheetProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BreadcrumbItemProps - Type contract for breadcrumb item props.
export type { BreadcrumbItemProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BreadcrumbsProps - Type contract for breadcrumbs props.
export type { BreadcrumbsProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DashboardNavState - Type contract for dashboard nav state.
export type { DashboardNavState } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FooterLayoutProps - Type contract for footer layout props.
export type { FooterLayoutProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FooterLinkGroup - Type contract for footer link group.
export type { FooterLinkGroup } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FooterSocialLink - Type contract for footer social link.
export type { FooterSocialLink } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LayoutClientProps - Type contract for layout client props.
export type { LayoutClientProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LayoutProvider - Type contract for layout provider.
export type { LayoutProvider } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LocaleSwitcherOption - Type contract for locale switcher option.
export type { LocaleSwitcherOption } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LocaleSwitcherProps - Type contract for locale switcher props.
export type { LocaleSwitcherProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MainNavbarItem - Type contract for main navbar item.
export type { MainNavbarItem } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MainNavbarProps - Type contract for main navbar props.
export type { MainNavbarProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NavItemProps - Type contract for nav item props.
export type { NavItemProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NavbarLayoutItem - Type contract for navbar layout item.
export type { NavbarLayoutItem } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NavbarLayoutProps - Type contract for navbar layout props.
export type { NavbarLayoutProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SidebarLayoutProps - Type contract for sidebar layout props.
export type { SidebarLayoutProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TitleBarLayoutProps - Type contract for title bar layout props.
export type { TitleBarLayoutProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TitleBarProps - Type contract for title bar props.
export type { TitleBarProps } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TitleBarUser - Type contract for title bar user.
export type { TitleBarUser } from "./features/layout/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseBottomActionsOptions - Type contract for use bottom actions options.
export type { UseBottomActionsOptions } from "./features/layout/index";

// ./features/loyalty/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CoinsBadge - Component for coins badge.
export { CoinsBadge } from "./features/loyalty/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CoinsDisplay - Shared export for coins display.
export { CoinsDisplay } from "./features/loyalty/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// applyCoinsToOrder - Helper for apply coins to order.
export { applyCoinsToOrder } from "./features/loyalty/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildLoyaltyColumns - Helper for build loyalty columns.
export { buildLoyaltyColumns } from "./features/loyalty/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// calculateCoinsEarned - Helper for calculate coins earned.
export { calculateCoinsEarned } from "./features/loyalty/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// calculateMaxRedeemable - Helper for calculate max redeemable.
export { calculateMaxRedeemable } from "./features/loyalty/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// coinHistoryEntrySchema - Model for coin history entry schema.
export { coinHistoryEntrySchema } from "./features/loyalty/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// coinReasonSchema - Model for coin reason schema.
export { coinReasonSchema } from "./features/loyalty/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// coinsToRupees - Shared export for coins to rupees.
export { coinsToRupees } from "./features/loyalty/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// loyaltyAdminColumns - Shared export for loyalty admin columns.
export { loyaltyAdminColumns } from "./features/loyalty/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// loyaltyBalanceSchema - Model for loyalty balance schema.
export { loyaltyBalanceSchema } from "./features/loyalty/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// loyaltyConfigSchema - Model for loyalty config schema.
export { loyaltyConfigSchema } from "./features/loyalty/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useLoyaltyBalance - React hook for use loyalty balance.
export { useLoyaltyBalance } from "./features/loyalty/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CoinHistoryEntry - Type contract for coin history entry.
export type { CoinHistoryEntry } from "./features/loyalty/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CoinReason - Type contract for coin reason.
export type { CoinReason } from "./features/loyalty/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EarnCoinsInput - Type contract for earn coins input.
export type { EarnCoinsInput } from "./features/loyalty/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LoyaltyBalance - Type contract for loyalty balance.
export type { LoyaltyBalance } from "./features/loyalty/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LoyaltyConfig - Type contract for loyalty config.
export type { LoyaltyConfig } from "./features/loyalty/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RedeemCoinsInput - Type contract for redeem coins input.
export type { RedeemCoinsInput } from "./features/loyalty/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RedeemCoinsResult - Type contract for redeem coins result.
export type { RedeemCoinsResult } from "./features/loyalty/index";

// ./features/loyalty/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// LoyaltyRepository - Shared export for loyalty repository.
export { LoyaltyRepository } from "./features/loyalty/server";

// ./features/media/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AvatarUpload - Shared export for avatar upload.
export { AvatarUpload } from "./features/media/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CameraCapture - Shared export for camera capture.
export { CameraCapture } from "./features/media/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// ImageCropModal - Component for image crop modal.
export { ImageCropModal } from "./features/media/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ImageUpload - Shared export for image upload.
export { ImageUpload } from "./features/media/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MediaAvatar - Shared export for media avatar.
export { MediaAvatar } from "./features/media/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MediaImage - Shared export for media image.
export { MediaImage } from "./features/media/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MediaLightbox - Shared export for media lightbox.
export { MediaLightbox } from "./features/media/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MediaSlider - Shared export for media slider.
export { MediaSlider } from "./features/media/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MediaUploadField - Shared export for media upload field.
export { MediaUploadField } from "./features/media/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MediaUploadList - Shared export for media upload list.
export { MediaUploadList } from "./features/media/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MediaVideo - Shared export for media video.
export { MediaVideo } from "./features/media/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// VideoThumbnailSelector - Shared export for video thumbnail selector.
export { VideoThumbnailSelector } from "./features/media/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// VideoTrimModal - Component for video trim modal.
export { VideoTrimModal } from "./features/media/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useMediaAbort - React hook for use media abort.
export { useMediaAbort } from "./features/media/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useMediaCleanup - React hook for use media cleanup.
export { useMediaCleanup } from "./features/media/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useMediaCrop - React hook for use media crop.
export { useMediaCrop } from "./features/media/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useMediaTrim - React hook for use media trim.
export { useMediaTrim } from "./features/media/index";
// [CLIENT-ONLY]-Cannot run in SSR mode — uses browser-only APIs (window, navigator, localStorage, matchMedia, DOM events) that do not exist in Node.js.
// useMediaUpload - React hook for use media upload.
export { useMediaUpload } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AvatarUploadProps - Type contract for avatar upload props.
export type { AvatarUploadProps } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CameraCaptureProps - Type contract for camera capture props.
export type { CameraCaptureProps } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ImageCropData - Type contract for image crop data.
export type { ImageCropData } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ImageCropModalProps - Type contract for image crop modal props.
export type { ImageCropModalProps } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ImageUploadProps - Type contract for image upload props.
export type { ImageUploadProps } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LightboxItem - Type contract for lightbox item.
export type { LightboxItem } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// LightboxLabels - Type contract for lightbox labels.
export type { LightboxLabels } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaAvatarProps - Type contract for media avatar props.
export type { MediaAvatarProps } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaCropInput - Type contract for media crop input.
export type { MediaCropInput } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaImageProps - Type contract for media image props.
export type { MediaImageProps } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaImageSize - Type contract for media image size.
export type { MediaImageSize } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaLightboxProps - Type contract for media lightbox props.
export type { MediaLightboxProps } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaSliderProps - Type contract for media slider props.
export type { MediaSliderProps } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaTrimInput - Type contract for media trim input.
export type { MediaTrimInput } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaUploadFieldProps - Type contract for media upload field props.
export type { MediaUploadFieldProps } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaUploadListProps - Type contract for media upload list props.
export type { MediaUploadListProps } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaUploadResult - Type contract for media upload result.
export type { MediaUploadResult } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MediaVideoProps - Type contract for media video props.
export type { MediaVideoProps } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// VideoThumbnailSelectorProps - Type contract for video thumbnail selector props.
export type { VideoThumbnailSelectorProps } from "./features/media/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// VideoTrimModalProps - Type contract for video trim modal props.
export type { VideoTrimModalProps } from "./features/media/index";

// ./features/media/server
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// extractStoragePathFromUrl - Shared export for extract storage path from url.
export { extractStoragePathFromUrl } from "./features/media/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// finalizeStagedMediaArray - Shared export for finalize staged media array.
export { finalizeStagedMediaArray } from "./features/media/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// finalizeStagedMediaField - Shared export for finalize staged media field.
export { finalizeStagedMediaField } from "./features/media/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// finalizeStagedMediaObject - Shared export for finalize staged media object.
export { finalizeStagedMediaObject } from "./features/media/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// finalizeStagedMediaObjectArray - Shared export for finalize staged media object array.
export { finalizeStagedMediaObjectArray } from "./features/media/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// finalizeStagedMediaUrl - Shared export for finalize staged media url.
export { finalizeStagedMediaUrl } from "./features/media/server";

// ./features/orders/index
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_ORDER_DATA - Constant used across modules.
export { DEFAULT_ORDER_DATA } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MarketplaceOrderCard - Component for marketplace order card.
export { MarketplaceOrderCard } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ORDER_ADMIN_SORT_OPTIONS - Constant used across modules.
export { ORDER_ADMIN_SORT_OPTIONS } from "./features/orders/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// ORDER_COLLECTION - Constant used across modules.
export { ORDER_COLLECTION } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ORDER_FILTER_KEYS - Constant used across modules.
export { ORDER_FILTER_KEYS } from "./features/orders/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// ORDER_INDEXED_FIELDS - Constant used across modules.
export { ORDER_INDEXED_FIELDS } from "./features/orders/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// ORDER_PUBLIC_FIELDS - Constant used across modules.
export { ORDER_PUBLIC_FIELDS } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ORDER_SELLER_SORT_OPTIONS - Constant used across modules.
export { ORDER_SELLER_SORT_OPTIONS } from "./features/orders/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// ORDER_UPDATABLE_FIELDS - Constant used across modules.
export { ORDER_UPDATABLE_FIELDS } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ORDER_USER_SORT_OPTIONS - Constant used across modules.
export { ORDER_USER_SORT_OPTIONS } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// OrderCard - Component for order card.
export { OrderCard } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// OrderFilters - Shared export for order filters.
export { OrderFilters } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// OrderStatusValues - Model for order status values.
export { OrderStatusValues } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// OrdersList - Shared export for orders list.
export { OrdersList } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PaymentGatewayValues - Model for payment gateway values.
export { PaymentGatewayValues } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PaymentMethodValues - Model for payment method values.
export { PaymentMethodValues } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PaymentStatusValues - Model for payment status values.
export { PaymentStatusValues } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// RefundStatusValues - Model for refund status values.
export { RefundStatusValues } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ShippingMethodValues - Model for shipping method values.
export { ShippingMethodValues } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildOrderColumns - Helper for build order columns.
export { buildOrderColumns } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createOrderId - Helper for create order id.
export { createOrderId } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getOrderFilterKeys - Helper for get order filter keys.
export { getOrderFilterKeys } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getOrderSortOptions - Helper for get order sort options.
export { getOrderSortOptions } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// orderAdminColumns - Shared export for order admin columns.
export { orderAdminColumns } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// orderItemSchema - Model for order item schema.
export { orderItemSchema } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// orderListParamsSchema - Model for order list params schema.
export { orderListParamsSchema } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// orderQueryHelpers - Shared export for order query helpers.
export { orderQueryHelpers } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// orderSchema - Model for order schema.
export { orderSchema } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// orderStatusSchema - Model for order status schema.
export { orderStatusSchema } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// orderTimelineSchema - Model for order timeline schema.
export { orderTimelineSchema } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useOrder - React hook for use order.
export { useOrder } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useOrders - React hook for use orders.
export { useOrders } from "./features/orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useTrackOrder - React hook for use track order.
export { useTrackOrder } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MarketplaceOrderCardLabels - Type contract for marketplace order card labels.
export type { MarketplaceOrderCardLabels } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MarketplaceOrderCardLinks - Type contract for marketplace order card links.
export type { MarketplaceOrderCardLinks } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MarketplaceOrderCardOrder - Type contract for marketplace order card order.
export type { MarketplaceOrderCardOrder } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MarketplaceOrderCardProps - Type contract for marketplace order card props.
export type { MarketplaceOrderCardProps } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// Order - Type contract for order.
export type { Order } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderAdminUpdateInput - Type contract for order admin update input.
export type { OrderAdminUpdateInput } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderCreateInput - Type contract for order create input.
export type { OrderCreateInput } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderDocument - Type contract for order document.
export type { OrderDocument } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderDocumentItem - Type contract for order document item.
export type { OrderDocumentItem } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderFilterVariant - Type contract for order filter variant.
export type { OrderFilterVariant } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderFiltersProps - Type contract for order filters props.
export type { OrderFiltersProps } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderListParams - Type contract for order list params.
export type { OrderListParams } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderListResponse - Type contract for order list response.
export type { OrderListResponse } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderPayoutStatus - Type contract for order payout status.
export type { OrderPayoutStatus } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderTimeline - Type contract for order timeline.
export type { OrderTimeline } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderUpdateInput - Type contract for order update input.
export type { OrderUpdateInput } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PaymentGateway - Type contract for payment gateway.
export type { PaymentGateway } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RefundStatus - Type contract for refund status.
export type { RefundStatus } from "./features/orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ShippingMethod - Type contract for shipping method.
export type { ShippingMethod } from "./features/orders/index";

// ./features/orders/server
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// cancelOrderForUser - Shared export for cancel order for user.
export { cancelOrderForUser } from "./features/orders/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getOrderByIdForUser - Helper for get order by id for user.
export { getOrderByIdForUser } from "./features/orders/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getOrderTrackingHandler - Helper for get order tracking handler.
export { getOrderTrackingHandler } from "./features/orders/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getTrackingInfo - Helper for get tracking info.
export { getTrackingInfo } from "./features/orders/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// issuePartialRefund - Shared export for issue partial refund.
export { issuePartialRefund } from "./features/orders/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listOrdersForUser - Helper for list orders for user.
export { listOrdersForUser } from "./features/orders/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// previewCancellationRefund - Shared export for preview cancellation refund.
export { previewCancellationRefund } from "./features/orders/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PartialRefundResult - Type contract for partial refund result.
export type { PartialRefundResult } from "./features/orders/server";

// ./features/payments/index
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_PAYOUT_DATA - Constant used across modules.
export { DEFAULT_PAYOUT_DATA } from "./features/payments/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_PLATFORM_FEE_RATE - Constant used across modules.
export { DEFAULT_PLATFORM_FEE_RATE } from "./features/payments/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// PAYOUT_ADMIN_UPDATEABLE_FIELDS - Constant used across modules.
export { PAYOUT_ADMIN_UPDATEABLE_FIELDS } from "./features/payments/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// PAYOUT_COLLECTION - Constant used across modules.
export { PAYOUT_COLLECTION } from "./features/payments/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// PAYOUT_FIELDS - Constant used across modules.
export { PAYOUT_FIELDS } from "./features/payments/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// PAYOUT_INDEXED_FIELDS - Constant used across modules.
export { PAYOUT_INDEXED_FIELDS } from "./features/payments/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// PAYOUT_PUBLIC_FIELDS - Constant used across modules.
export { PAYOUT_PUBLIC_FIELDS } from "./features/payments/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PayoutStatusValues - Model for payout status values.
export { PayoutStatusValues } from "./features/payments/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildPaymentColumns - Helper for build payment columns.
export { buildPaymentColumns } from "./features/payments/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createPayoutId - Helper for create payout id.
export { createPayoutId } from "./features/payments/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// paymentAdminColumns - Shared export for payment admin columns.
export { paymentAdminColumns } from "./features/payments/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// paymentGatewayConfigSchema - Model for payment gateway config schema.
export { paymentGatewayConfigSchema } from "./features/payments/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// paymentGatewaySchema - Model for payment gateway schema.
export { paymentGatewaySchema } from "./features/payments/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// paymentRecordSchema - Model for payment record schema.
export { paymentRecordSchema } from "./features/payments/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// paymentStatusSchema - Model for payment status schema.
export { paymentStatusSchema } from "./features/payments/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// payoutQueryHelpers - Shared export for payout query helpers.
export { payoutQueryHelpers } from "./features/payments/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// payoutStatusSchema - Model for payout status schema.
export { payoutStatusSchema } from "./features/payments/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// usePaymentEvent - React hook for use payment event.
export { usePaymentEvent } from "./features/payments/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// usePaymentSettings - React hook for use payment settings.
export { usePaymentSettings } from "./features/payments/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PaymentEventStatus - Type contract for payment event status.
export type { PaymentEventStatus } from "./features/payments/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PaymentGatewayConfig - Type contract for payment gateway config.
export type { PaymentGatewayConfig } from "./features/payments/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PaymentRecord - Type contract for payment record.
export type { PaymentRecord } from "./features/payments/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PaymentSettings - Type contract for payment settings.
export type { PaymentSettings } from "./features/payments/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PayoutBankAccount - Type contract for payout bank account.
export type { PayoutBankAccount } from "./features/payments/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PayoutCreateInput - Type contract for payout create input.
export type { PayoutCreateInput } from "./features/payments/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PayoutDocument - Type contract for payout document.
export type { PayoutDocument } from "./features/payments/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PayoutPaymentMethod - Type contract for payout payment method.
export type { PayoutPaymentMethod } from "./features/payments/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PayoutUpdateInput - Type contract for payout update input.
export type { PayoutUpdateInput } from "./features/payments/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UsePaymentEventOptions - Type contract for use payment event options.
export type { UsePaymentEventOptions } from "./features/payments/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UsePaymentEventReturn - Type contract for use payment event return.
export type { UsePaymentEventReturn } from "./features/payments/index";

// ./features/payments/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// PaymentsRepository - Shared export for payments repository.
export { PaymentsRepository } from "./features/payments/server";

// ./features/pre-orders/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MarketplacePreorderCard - Component for marketplace preorder card.
export { MarketplacePreorderCard } from "./features/pre-orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PreorderBadge - Component for preorder badge.
export { PreorderBadge } from "./features/pre-orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PreorderCard - Component for preorder card.
export { PreorderCard } from "./features/pre-orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildPreorderColumns - Helper for build preorder columns.
export { buildPreorderColumns } from "./features/pre-orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getPreorderStatus - Helper for get preorder status.
export { getPreorderStatus } from "./features/pre-orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// preorderAdminColumns - Shared export for preorder admin columns.
export { preorderAdminColumns } from "./features/pre-orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// preorderItemSchema - Model for preorder item schema.
export { preorderItemSchema } from "./features/pre-orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// usePreorder - React hook for use preorder.
export { usePreorder } from "./features/pre-orders/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// usePreorders - React hook for use preorders.
export { usePreorders } from "./features/pre-orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MarketplacePreorderCardData - Type contract for marketplace preorder card data.
export type { MarketplacePreorderCardData } from "./features/pre-orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MarketplacePreorderCardLabels - Type contract for marketplace preorder card labels.
export type { MarketplacePreorderCardLabels } from "./features/pre-orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MarketplacePreorderCardProps - Type contract for marketplace preorder card props.
export type { MarketplacePreorderCardProps } from "./features/pre-orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PreOrderItem - Type contract for pre order item.
export type { PreOrderItem } from "./features/pre-orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PreOrderStatus - Type contract for pre order status.
export type { PreOrderStatus } from "./features/pre-orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PreorderItem - Type contract for preorder item.
export type { PreorderItem } from "./features/pre-orders/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PreorderStatus - Type contract for preorder status.
export type { PreorderStatus } from "./features/pre-orders/index";

// ./features/pre-orders/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// PreordersRepository - Shared export for preorders repository.
export { PreordersRepository } from "./features/pre-orders/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// preOrdersGET - Shared export for pre orders get.
export { preOrdersGET } from "./features/pre-orders/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// preOrdersPOST - Shared export for pre orders post.
export { preOrdersPOST } from "./features/pre-orders/server";

// ./features/products/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AuctionDetailView - Component for auction detail view.
export { AuctionDetailView } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// AuctionsView - Component for auctions view.
export { AuctionsView } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BidHistory - Shared export for bid history.
export { BidHistory } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// BuyBar - Shared export for buy bar.
export { BuyBar } from "./features/products/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_PRODUCT_DATA - Constant used across modules.
export { DEFAULT_PRODUCT_DATA } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// InteractiveProductCard - Component for interactive product card.
export { InteractiveProductCard } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// MakeOfferForm - Component for make offer form.
export { MakeOfferForm } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PRODUCT_ADMIN_SORT_OPTIONS - Constant used across modules.
export { PRODUCT_ADMIN_SORT_OPTIONS } from "./features/products/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// PRODUCT_COLLECTION - Constant used across modules.
export { PRODUCT_COLLECTION } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PRODUCT_FILTER_KEYS - Constant used across modules.
export { PRODUCT_FILTER_KEYS } from "./features/products/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// PRODUCT_INDEXED_FIELDS - Constant used across modules.
export { PRODUCT_INDEXED_FIELDS } from "./features/products/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// PRODUCT_PUBLIC_FIELDS - Constant used across modules.
export { PRODUCT_PUBLIC_FIELDS } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PRODUCT_PUBLIC_SORT_OPTIONS - Constant used across modules.
export { PRODUCT_PUBLIC_SORT_OPTIONS } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PRODUCT_SELLER_SORT_OPTIONS - Constant used across modules.
export { PRODUCT_SELLER_SORT_OPTIONS } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PRODUCT_SORT_VALUES - Constant used across modules.
export { PRODUCT_SORT_VALUES } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PRODUCT_STATUS_OPTIONS - Constant used across modules.
export { PRODUCT_STATUS_OPTIONS } from "./features/products/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// PRODUCT_UPDATABLE_FIELDS - Constant used across modules.
export { PRODUCT_UPDATABLE_FIELDS } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PlaceBidForm - Component for place bid form.
export { PlaceBidForm } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PreOrderDetailView - Component for pre order detail view.
export { PreOrderDetailView } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PreOrdersView - Component for pre orders view.
export { PreOrdersView } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ProductCard - Component for product card.
export { ProductCard } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ProductDetailView - Component for product detail view.
export { ProductDetailView } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ProductFeatureBadges - Shared export for product feature badges.
export { ProductFeatureBadges } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ProductFilters - Shared export for product filters.
export { ProductFilters } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ProductForm - Component for product form.
export { ProductForm } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ProductGrid - Component for product grid.
export { ProductGrid } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ProductInfo - Shared export for product info.
export { ProductInfo } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ProductStatusValues - Model for product status values.
export { ProductStatusValues } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ProductTabs - Component for product tabs.
export { ProductTabs } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ProductsIndexPageView - Component for products index page view.
export { ProductsIndexPageView } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ProductsView - Component for products view.
export { ProductsView } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// RelatedProducts - Shared export for related products.
export { RelatedProducts } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildProductColumns - Helper for build product columns.
export { buildProductColumns } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createAuctionId - Helper for create auction id.
export { createAuctionId } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createPreOrderId - Helper for create pre order id.
export { createPreOrderId } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createProductId - Helper for create product id.
export { createProductId } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getProductFilterKeys - Helper for get product filter keys.
export { getProductFilterKeys } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getProductSortOptions - Helper for get product sort options.
export { getProductSortOptions } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getProductTableColumns - Helper for get product table columns.
export { getProductTableColumns } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// normalizeListingType - Helper for normalize listing type.
export { normalizeListingType } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// productAdminColumns - Shared export for product admin columns.
export { productAdminColumns } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// productImageSchema - Model for product image schema.
export { productImageSchema } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// productItemSchema - Model for product item schema.
export { productItemSchema } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// productListParamsSchema - Model for product list params schema.
export { productListParamsSchema } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// productQueryHelpers - Shared export for product query helpers.
export { productQueryHelpers } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// productSeoSchema - Model for product seo schema.
export { productSeoSchema } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useBrands - React hook for use brands.
export { useBrands } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useProduct - React hook for use product.
export { useProduct } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useProductDetail - React hook for use product detail.
export { useProductDetail } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useProducts - React hook for use products.
export { useProducts } from "./features/products/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useRelatedProducts - React hook for use related products.
export { useRelatedProducts } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AuctionDetailViewProps - Type contract for auction detail view props.
export type { AuctionDetailViewProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AuctionsViewProps - Type contract for auctions view props.
export type { AuctionsViewProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BidHistoryEntry - Type contract for bid history entry.
export type { BidHistoryEntry } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BidHistoryProps - Type contract for bid history props.
export type { BidHistoryProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BuyBarProps - Type contract for buy bar props.
export type { BuyBarProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// InteractiveProductCardProps - Type contract for interactive product card props.
export type { InteractiveProductCardProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ListingType - Type contract for listing type.
export type { ListingType } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MakeOfferFormProps - Type contract for make offer form props.
export type { MakeOfferFormProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PlaceBidFormProps - Type contract for place bid form props.
export type { PlaceBidFormProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PreOrderDetailViewProps - Type contract for pre order detail view props.
export type { PreOrderDetailViewProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PreOrdersViewProps - Type contract for pre orders view props.
export type { PreOrdersViewProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductAdminUpdateInput - Type contract for product admin update input.
export type { ProductAdminUpdateInput } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductCondition - Type contract for product condition.
export type { ProductCondition } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductCreateInput - Type contract for product create input.
export type { ProductCreateInput } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductDetailViewProps - Type contract for product detail view props.
export type { ProductDetailViewProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductDocument - Type contract for product document.
export type { ProductDocument } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductFilterVariant - Type contract for product filter variant.
export type { ProductFilterVariant } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductFiltersProps - Type contract for product filters props.
export type { ProductFiltersProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductFormProps - Type contract for product form props.
export type { ProductFormProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductFormValue - Type contract for product form value.
export type { ProductFormValue } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductImage - Type contract for product image.
export type { ProductImage } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductInfoProps - Type contract for product info props.
export type { ProductInfoProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductItem - Type contract for product item.
export type { ProductItem } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductListParams - Type contract for product list params.
export type { ProductListParams } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductListResponse - Type contract for product list response.
export type { ProductListResponse } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductSeo - Type contract for product seo.
export type { ProductSeo } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductSortValue - Type contract for product sort value.
export type { ProductSortValue } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductSpecification - Type contract for product specification.
export type { ProductSpecification } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductStatus - Type contract for product status.
export type { ProductStatus } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductTab - Type contract for product tab.
export type { ProductTab } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductTableColumnLabels - Type contract for product table column labels.
export type { ProductTableColumnLabels } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductTableColumnsConfig - Type contract for product table columns config.
export type { ProductTableColumnsConfig } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductTabsProps - Type contract for product tabs props.
export type { ProductTabsProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductUpdateInput - Type contract for product update input.
export type { ProductUpdateInput } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductVideoField - Type contract for product video field.
export type { ProductVideoField } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductsViewProps - Type contract for products view props.
export type { ProductsViewProps } from "./features/products/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RelatedProductsProps - Type contract for related products props.
export type { RelatedProductsProps } from "./features/products/index";

// ./features/products/server
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getFeaturedAuctions - Helper for get featured auctions.
export { getFeaturedAuctions } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getFeaturedPreOrders - Helper for get featured pre orders.
export { getFeaturedPreOrders } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getFeaturedProducts - Helper for get featured products.
export { getFeaturedProducts } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getLatestAuctions - Helper for get latest auctions.
export { getLatestAuctions } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getLatestPreOrders - Helper for get latest pre orders.
export { getLatestPreOrders } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getLatestProducts - Helper for get latest products.
export { getLatestProducts } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getProductById - Helper for get product by id.
export { getProductById } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getRelatedProducts - Helper for get related products.
export { getRelatedProducts } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getSellerStorefrontProducts - Helper for get seller storefront products.
export { getSellerStorefrontProducts } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listAuctions - Helper for list auctions.
export { listAuctions } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listPreOrders - Helper for list pre orders.
export { listPreOrders } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listProducts - Helper for list products.
export { listProducts } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// productItemDELETE - Shared export for product item delete.
export { productItemDELETE } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// productItemGET - Shared export for product item get.
export { productItemGET } from "./features/products/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// productItemPATCH - Shared export for product item patch.
export { productItemPATCH } from "./features/products/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductListActionParams - Type contract for product list action params.
export type { ProductListActionParams } from "./features/products/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductListResult - Type contract for product list result.
export type { ProductListResult } from "./features/products/server";

// ./features/promotions/index
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// COUPONS_COLLECTION - Constant used across modules.
export { COUPONS_COLLECTION } from "./features/promotions/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// COUPONS_INDEXED_FIELDS - Constant used across modules.
export { COUPONS_INDEXED_FIELDS } from "./features/promotions/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// COUPONS_PUBLIC_FIELDS - Constant used across modules.
export { COUPONS_PUBLIC_FIELDS } from "./features/promotions/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// COUPONS_UPDATABLE_FIELDS - Constant used across modules.
export { COUPONS_UPDATABLE_FIELDS } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// COUPON_TYPE_LABELS - Constant used across modules.
export { COUPON_TYPE_LABELS } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// COUPON_USAGE_SUBCOLLECTION - Constant used across modules.
export { COUPON_USAGE_SUBCOLLECTION } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// CouponCard - Component for coupon card.
export { CouponCard } from "./features/promotions/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_COUPON_DATA - Constant used across modules.
export { DEFAULT_COUPON_DATA } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PromotionsView - Component for promotions view.
export { PromotionsView } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// PromotionsViewProductSection - Shared export for promotions view product section.
export { PromotionsViewProductSection } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildCouponColumns - Helper for build coupon columns.
export { buildCouponColumns } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildSellerCouponCode - Helper for build seller coupon code.
export { buildSellerCouponCode } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// calculateDiscount - Helper for calculate discount.
export { calculateDiscount } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// canUserUseCoupon - Shared export for can user use coupon.
export { canUserUseCoupon } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// couponAdminColumns - Shared export for coupon admin columns.
export { couponAdminColumns } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// couponItemSchema - Model for coupon item schema.
export { couponItemSchema } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// couponQueryHelpers - Shared export for coupon query helpers.
export { couponQueryHelpers } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// couponScopeSchema - Model for coupon scope schema.
export { couponScopeSchema } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// couponTypeSchema - Model for coupon type schema.
export { couponTypeSchema } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createCouponId - Helper for create coupon id.
export { createCouponId } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getTieredDiscount - Helper for get tiered discount.
export { getTieredDiscount } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isCouponValid - Shared export for is coupon valid.
export { isCouponValid } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// isValidCouponCode - Shared export for is valid coupon code.
export { isValidCouponCode } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// promotionsListParamsSchema - Model for promotions list params schema.
export { promotionsListParamsSchema } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// sellerCouponCodePrefix - Shared export for seller coupon code prefix.
export { sellerCouponCodePrefix } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCoupon - React hook for use coupon.
export { useCoupon } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCouponValidate - React hook for use coupon validate.
export { useCouponValidate } from "./features/promotions/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// usePromotions - React hook for use promotions.
export { usePromotions } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ApplyCouponResult - Type contract for apply coupon result.
export type { ApplyCouponResult } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BXGYConfig - Type contract for bxgyconfig.
export type { BXGYConfig } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CouponCreateInput - Type contract for coupon create input.
export type { CouponCreateInput } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CouponDocument - Type contract for coupon document.
export type { CouponDocument } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CouponItem - Type contract for coupon item.
export type { CouponItem } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CouponScope - Type contract for coupon scope.
export type { CouponScope } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CouponStats - Type contract for coupon stats.
export type { CouponStats } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CouponType - Type contract for coupon type.
export type { CouponType } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CouponUpdateInput - Type contract for coupon update input.
export type { CouponUpdateInput } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CouponUsageDocument - Type contract for coupon usage document.
export type { CouponUsageDocument } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CouponValidationResult - Type contract for coupon validation result.
export type { CouponValidationResult } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// DiscountConfig - Type contract for discount config.
export type { DiscountConfig } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PromotionProductItem - Type contract for promotion product item.
export type { PromotionProductItem } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PromotionsListParams - Type contract for promotions list params.
export type { PromotionsListParams } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PromotionsListResponse - Type contract for promotions list response.
export type { PromotionsListResponse } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PromotionsViewProductSectionProps - Type contract for promotions view product section props.
export type { PromotionsViewProductSectionProps } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PromotionsViewProps - Type contract for promotions view props.
export type { PromotionsViewProps } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RestrictionsConfig - Type contract for restrictions config.
export type { RestrictionsConfig } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TieredDiscount - Type contract for tiered discount.
export type { TieredDiscount } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UsageConfig - Type contract for usage config.
export type { UsageConfig } from "./features/promotions/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ValidityConfig - Type contract for validity config.
export type { ValidityConfig } from "./features/promotions/index";

// ./features/promotions/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// PromotionsRepository - Shared export for promotions repository.
export { PromotionsRepository } from "./features/promotions/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getPromotions - Helper for get promotions.
export { getPromotions } from "./features/promotions/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// promotionsGET - Shared export for promotions get.
export { promotionsGET } from "./features/promotions/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sellerCreateCoupon - Shared export for seller create coupon.
export { sellerCreateCoupon } from "./features/promotions/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sellerDeleteCoupon - Shared export for seller delete coupon.
export { sellerDeleteCoupon } from "./features/promotions/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sellerUpdateCoupon - Shared export for seller update coupon.
export { sellerUpdateCoupon } from "./features/promotions/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// validateCoupon - Helper for validate coupon.
export { validateCoupon } from "./features/promotions/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// validateCouponForCart - Helper for validate coupon for cart.
export { validateCouponForCart } from "./features/promotions/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CouponCartItem - Type contract for coupon cart item.
export type { CouponCartItem } from "./features/promotions/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CouponCartValidationResult - Type contract for coupon cart validation result.
export type { CouponCartValidationResult } from "./features/promotions/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PromotionsResult - Type contract for promotions result.
export type { PromotionsResult } from "./features/promotions/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerCreateCouponInput - Type contract for seller create coupon input.
export type { SellerCreateCouponInput } from "./features/promotions/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerUpdateCouponInput - Type contract for seller update coupon input.
export type { SellerUpdateCouponInput } from "./features/promotions/server";

// ./features/reviews/index
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_REVIEW_DATA - Constant used across modules.
export { DEFAULT_REVIEW_DATA } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// REVIEW_ADMIN_SORT_OPTIONS - Constant used across modules.
export { REVIEW_ADMIN_SORT_OPTIONS } from "./features/reviews/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// REVIEW_COLLECTION - Constant used across modules.
export { REVIEW_COLLECTION } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// REVIEW_FILTER_KEYS - Constant used across modules.
export { REVIEW_FILTER_KEYS } from "./features/reviews/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// REVIEW_INDEXED_FIELDS - Constant used across modules.
export { REVIEW_INDEXED_FIELDS } from "./features/reviews/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// REVIEW_PUBLIC_FIELDS - Constant used across modules.
export { REVIEW_PUBLIC_FIELDS } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// REVIEW_PUBLIC_SORT_OPTIONS - Constant used across modules.
export { REVIEW_PUBLIC_SORT_OPTIONS } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// REVIEW_SELLER_SORT_OPTIONS - Constant used across modules.
export { REVIEW_SELLER_SORT_OPTIONS } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// REVIEW_SORT_OPTIONS - Constant used across modules.
export { REVIEW_SORT_OPTIONS } from "./features/reviews/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// REVIEW_UPDATABLE_FIELDS - Constant used across modules.
export { REVIEW_UPDATABLE_FIELDS } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ReviewCard - Component for review card.
export { ReviewCard } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ReviewFilters - Shared export for review filters.
export { ReviewFilters } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ReviewStatusValues - Model for review status values.
export { ReviewStatusValues } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ReviewSummary - Shared export for review summary.
export { ReviewSummary } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ReviewsList - Shared export for reviews list.
export { ReviewsList } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ReviewsListView - Component for reviews list view.
export { ReviewsListView } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// ViewReviewModal - Component for view review modal.
export { ViewReviewModal } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildReviewColumns - Helper for build review columns.
export { buildReviewColumns } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createReviewId - Helper for create review id.
export { createReviewId } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getReviewFilterKeys - Helper for get review filter keys.
export { getReviewFilterKeys } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// getReviewSortOptions - Helper for get review sort options.
export { getReviewSortOptions } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// reviewAdminColumns - Shared export for review admin columns.
export { reviewAdminColumns } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// reviewImageSchema - Model for review image schema.
export { reviewImageSchema } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// reviewListParamsSchema - Model for review list params schema.
export { reviewListParamsSchema } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// reviewQueryHelpers - Shared export for review query helpers.
export { reviewQueryHelpers } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// reviewSchema - Model for review schema.
export { reviewSchema } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// reviewStatusSchema - Model for review status schema.
export { reviewStatusSchema } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// reviewVideoSchema - Model for review video schema.
export { reviewVideoSchema } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useCreateReview - React hook for use create review.
export { useCreateReview } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useProductReviews - React hook for use product reviews.
export { useProductReviews } from "./features/reviews/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useReviews - React hook for use reviews.
export { useReviews } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateReviewHookInput - Type contract for create review hook input.
export type { CreateReviewHookInput } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateReviewInput - Type contract for create review input.
export type { CreateReviewInput } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// Review - Type contract for review.
export type { Review } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewCardProps - Type contract for review card props.
export type { ReviewCardProps } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewCreateInput - Type contract for review create input.
export type { ReviewCreateInput } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewDocument - Type contract for review document.
export type { ReviewDocument } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewFilterVariant - Type contract for review filter variant.
export type { ReviewFilterVariant } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewFiltersProps - Type contract for review filters props.
export type { ReviewFiltersProps } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewImage - Type contract for review image.
export type { ReviewImage } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewListParams - Type contract for review list params.
export type { ReviewListParams } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewListResponse - Type contract for review list response.
export type { ReviewListResponse } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewModerationInput - Type contract for review moderation input.
export type { ReviewModerationInput } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewStatus - Type contract for review status.
export type { ReviewStatus } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewSummaryProps - Type contract for review summary props.
export type { ReviewSummaryProps } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewUpdateInput - Type contract for review update input.
export type { ReviewUpdateInput } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewVideo - Type contract for review video.
export type { ReviewVideo } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewVideoField - Type contract for review video field.
export type { ReviewVideoField } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewsListProps - Type contract for reviews list props.
export type { ReviewsListProps } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewsListViewProps - Type contract for reviews list view props.
export type { ReviewsListViewProps } from "./features/reviews/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ViewReviewModalProps - Type contract for view review modal props.
export type { ViewReviewModalProps } from "./features/reviews/index";

// ./features/reviews/server
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminDeleteReview - Shared export for admin delete review.
export { adminDeleteReview } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// adminUpdateReview - Shared export for admin update review.
export { adminUpdateReview } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createReview - Helper for create review.
export { createReview } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// deleteReview - Helper for delete review.
export { deleteReview } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getHomepageReviews - Helper for get homepage reviews.
export { getHomepageReviews } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getReviewById - Helper for get review by id.
export { getReviewById } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listAdminReviews - Helper for list admin reviews.
export { listAdminReviews } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listReviewsByProduct - Helper for list reviews by product.
export { listReviewsByProduct } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listReviewsBySeller - Helper for list reviews by seller.
export { listReviewsBySeller } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// reviewItemDELETE - Shared export for review item delete.
export { reviewItemDELETE } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// reviewItemGET - Shared export for review item get.
export { reviewItemGET } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// reviewItemPATCH - Shared export for review item patch.
export { reviewItemPATCH } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// reviewsGET - Shared export for reviews get.
export { reviewsGET } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateReview - Helper for update review.
export { updateReview } from "./features/reviews/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// voteReviewHelpful - Shared export for vote review helpful.
export { voteReviewHelpful } from "./features/reviews/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateReviewActionInput - Type contract for create review action input.
export type { CreateReviewActionInput } from "./features/reviews/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UpdateReviewActionInput - Type contract for update review action input.
export type { UpdateReviewActionInput } from "./features/reviews/server";

// ./features/search/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// Search - Shared export for search.
export { Search } from "./features/search/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SearchFiltersRow - Shared export for search filters row.
export { SearchFiltersRow } from "./features/search/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SearchResultsSection - Shared export for search results section.
export { SearchResultsSection } from "./features/search/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SearchView - Component for search view.
export { SearchView } from "./features/search/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildSearchResultColumns - Helper for build search result columns.
export { buildSearchResultColumns } from "./features/search/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// searchProductItemSchema - Model for search product item schema.
export { searchProductItemSchema } from "./features/search/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// searchQuerySchema - Model for search query schema.
export { searchQuerySchema } from "./features/search/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// searchResultAdminColumns - Shared export for search result admin columns.
export { searchResultAdminColumns } from "./features/search/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useNavSuggestions - React hook for use nav suggestions.
export { useNavSuggestions } from "./features/search/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useSearch - React hook for use search.
export { useSearch } from "./features/search/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NavSuggestionRecord - Type contract for nav suggestion record.
export type { NavSuggestionRecord } from "./features/search/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchCategoryOption - Type contract for search category option.
export type { SearchCategoryOption } from "./features/search/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchLabels - Type contract for search labels.
export type { SearchLabels } from "./features/search/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchProductItem - Type contract for search product item.
export type { SearchProductItem } from "./features/search/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchProps - Type contract for search props.
export type { SearchProps } from "./features/search/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchQuery - Type contract for search query.
export type { SearchQuery } from "./features/search/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchQuickLink - Type contract for search quick link.
export type { SearchQuickLink } from "./features/search/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchResponse - Type contract for search response.
export type { SearchResponse } from "./features/search/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchResultsSectionProps - Type contract for search results section props.
export type { SearchResultsSectionProps } from "./features/search/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchRouterAdapter - Type contract for search router adapter.
export type { SearchRouterAdapter } from "./features/search/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchViewProps - Type contract for search view props.
export type { SearchViewProps } from "./features/search/index";

// ./features/search/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// SearchRepository - Shared export for search repository.
export { SearchRepository } from "./features/search/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// searchGET - Shared export for search get.
export { searchGET } from "./features/search/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// searchProducts - Shared export for search products.
export { searchProducts } from "./features/search/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SearchProductsResult - Type contract for search products result.
export type { SearchProductsResult } from "./features/search/server";

// ./features/seller/index
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// OFFER_COLLECTION - Constant used across modules.
export { OFFER_COLLECTION } from "./features/seller/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// OFFER_FIELDS - Constant used across modules.
export { OFFER_FIELDS } from "./features/seller/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// OFFER_INDEXED_FIELDS - Constant used across modules.
export { OFFER_INDEXED_FIELDS } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// OfferStatusValues - Model for offer status values.
export { OfferStatusValues } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SELLER_PAGE_PERMISSIONS - Constant used across modules.
export { SELLER_PAGE_PERMISSIONS } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerAddressesView - Component for seller addresses view.
export { SellerAddressesView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerAnalyticsStats - Shared export for seller analytics stats.
export { SellerAnalyticsStats } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerAnalyticsView - Component for seller analytics view.
export { SellerAnalyticsView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerAuctionsView - Component for seller auctions view.
export { SellerAuctionsView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerCouponsView - Component for seller coupons view.
export { SellerCouponsView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerCreateProductView - Component for seller create product view.
export { SellerCreateProductView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerDashboardView - Component for seller dashboard view.
export { SellerDashboardView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerEditProductView - Component for seller edit product view.
export { SellerEditProductView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerGuideView - Component for seller guide view.
export { SellerGuideView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerOffersView - Component for seller offers view.
export { SellerOffersView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerOrdersView - Component for seller orders view.
export { SellerOrdersView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerPayoutHistoryTable - Component for seller payout history table.
export { SellerPayoutHistoryTable } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerPayoutSettingsView - Component for seller payout settings view.
export { SellerPayoutSettingsView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerPayoutStats - Shared export for seller payout stats.
export { SellerPayoutStats } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerPayoutsView - Component for seller payouts view.
export { SellerPayoutsView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerProductsView - Component for seller products view.
export { SellerProductsView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerRevenueChart - Shared export for seller revenue chart.
export { SellerRevenueChart } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerShippingView - Component for seller shipping view.
export { SellerShippingView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerSidebar - Shared export for seller sidebar.
export { SellerSidebar } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerStatCard - Component for seller stat card.
export { SellerStatCard } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerStoreSetupView - Component for seller store setup view.
export { SellerStoreSetupView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerStoreView - Component for seller store view.
export { SellerStoreView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerStorefrontView - Component for seller storefront view.
export { SellerStorefrontView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellerTopProducts - Shared export for seller top products.
export { SellerTopProducts } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// SellersListView - Component for sellers list view.
export { SellersListView } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildPayoutColumns - Helper for build payout columns.
export { buildPayoutColumns } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildSellerColumns - Helper for build seller columns.
export { buildSellerColumns } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// createOfferId - Helper for create offer id.
export { createOfferId } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// offerQueryHelpers - Shared export for offer query helpers.
export { offerQueryHelpers } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// payoutAdminColumns - Shared export for payout admin columns.
export { payoutAdminColumns } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// payoutListParamsSchema - Model for payout list params schema.
export { payoutListParamsSchema } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// payoutPaymentMethodSchema - Model for payout payment method schema.
export { payoutPaymentMethodSchema } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// payoutRecordSchema - Model for payout record schema.
export { payoutRecordSchema } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// sellerAdminColumns - Shared export for seller admin columns.
export { sellerAdminColumns } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// sellerListParamsSchema - Model for seller list params schema.
export { sellerListParamsSchema } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// sellerStoreSchema - Model for seller store schema.
export { sellerStoreSchema } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// socialLinksSchema - Model for social links schema.
export { socialLinksSchema } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// storeStatusSchema - Model for store status schema.
export { storeStatusSchema } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useBecomeSeller - React hook for use become seller.
export { useBecomeSeller } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useSellerAnalytics - React hook for use seller analytics.
export { useSellerAnalytics } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useSellerDashboard - React hook for use seller dashboard.
export { useSellerDashboard } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useSellerPayoutSettings - React hook for use seller payout settings.
export { useSellerPayoutSettings } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useSellerPayouts - React hook for use seller payouts.
export { useSellerPayouts } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useSellerStore - React hook for use seller store.
export { useSellerStore } from "./features/seller/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useSellerStorefront - React hook for use seller storefront.
export { useSellerStorefront } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BecomeSellerResult - Type contract for become seller result.
export type { BecomeSellerResult } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OfferCreateInput - Type contract for offer create input.
export type { OfferCreateInput } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OfferDocument - Type contract for offer document.
export type { OfferDocument } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OfferUpdateInput - Type contract for offer update input.
export type { OfferUpdateInput } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PayoutListResponse - Type contract for payout list response.
export type { PayoutListResponse } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// PayoutRecord - Type contract for payout record.
export type { PayoutRecord } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerAddressesViewProps - Type contract for seller addresses view props.
export type { SellerAddressesViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerAnalytics - Type contract for seller analytics.
export type { SellerAnalytics } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerAnalyticsMonthEntry - Type contract for seller analytics month entry.
export type { SellerAnalyticsMonthEntry } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerAnalyticsStatsLabels - Type contract for seller analytics stats labels.
export type { SellerAnalyticsStatsLabels } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerAnalyticsStatsProps - Type contract for seller analytics stats props.
export type { SellerAnalyticsStatsProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerAnalyticsSummary - Type contract for seller analytics summary.
export type { SellerAnalyticsSummary } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerAnalyticsTopProduct - Type contract for seller analytics top product.
export type { SellerAnalyticsTopProduct } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerAnalyticsViewProps - Type contract for seller analytics view props.
export type { SellerAnalyticsViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerAuctionsViewProps - Type contract for seller auctions view props.
export type { SellerAuctionsViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerCouponsViewProps - Type contract for seller coupons view props.
export type { SellerCouponsViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerCreateProductViewProps - Type contract for seller create product view props.
export type { SellerCreateProductViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerDashboardStats - Type contract for seller dashboard stats.
export type { SellerDashboardStats } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerDashboardViewProps - Type contract for seller dashboard view props.
export type { SellerDashboardViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerEditProductViewProps - Type contract for seller edit product view props.
export type { SellerEditProductViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerGuideViewProps - Type contract for seller guide view props.
export type { SellerGuideViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerListResponse - Type contract for seller list response.
export type { SellerListResponse } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerNavItem - Type contract for seller nav item.
export type { SellerNavItem } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerOffersViewProps - Type contract for seller offers view props.
export type { SellerOffersViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerOrdersViewProps - Type contract for seller orders view props.
export type { SellerOrdersViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerPayoutHistoryTableLabels - Type contract for seller payout history table labels.
export type { SellerPayoutHistoryTableLabels } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerPayoutHistoryTableProps - Type contract for seller payout history table props.
export type { SellerPayoutHistoryTableProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerPayoutSettings - Type contract for seller payout settings.
export type { SellerPayoutSettings } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerPayoutSettingsViewProps - Type contract for seller payout settings view props.
export type { SellerPayoutSettingsViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerPayoutStatsProps - Type contract for seller payout stats props.
export type { SellerPayoutStatsProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerPayoutsViewProps - Type contract for seller payouts view props.
export type { SellerPayoutsViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerProductsViewProps - Type contract for seller products view props.
export type { SellerProductsViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerRevenueChartLabels - Type contract for seller revenue chart labels.
export type { SellerRevenueChartLabels } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerRevenueChartProps - Type contract for seller revenue chart props.
export type { SellerRevenueChartProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerRevenueDataPoint - Type contract for seller revenue data point.
export type { SellerRevenueDataPoint } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerShippingViewProps - Type contract for seller shipping view props.
export type { SellerShippingViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerStore - Type contract for seller store.
export type { SellerStore } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerStoreSetupViewProps - Type contract for seller store setup view props.
export type { SellerStoreSetupViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerStoreViewProps - Type contract for seller store view props.
export type { SellerStoreViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerStorefrontViewProps - Type contract for seller storefront view props.
export type { SellerStorefrontViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerTopProductsLabels - Type contract for seller top products labels.
export type { SellerTopProductsLabels } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerTopProductsProps - Type contract for seller top products props.
export type { SellerTopProductsProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellersListViewProps - Type contract for sellers list view props.
export type { SellersListViewProps } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreStatus - Type contract for store status.
export type { StoreStatus } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UpdateSellerStoreInput - Type contract for update seller store input.
export type { UpdateSellerStoreInput } from "./features/seller/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseBecomeSellerOptions - Type contract for use become seller options.
export type { UseBecomeSellerOptions } from "./features/seller/index";

// ./features/seller/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// PayoutsRepository - Shared export for payouts repository.
export { PayoutsRepository } from "./features/seller/server";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// SellerRepository - Shared export for seller repository.
export { SellerRepository } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// acceptCounterOffer - Shared export for accept counter offer.
export { acceptCounterOffer } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// becomeSeller - Shared export for become seller.
export { becomeSeller } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// bulkSellerOrder - Shared export for bulk seller order.
export { bulkSellerOrder } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// checkoutOffer - Shared export for checkout offer.
export { checkoutOffer } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// counterOfferByBuyer - Shared export for counter offer by buyer.
export { counterOfferByBuyer } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createSellerProduct - Helper for create seller product.
export { createSellerProduct } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createStore - Helper for create store.
export { createStore } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// customShipOrder - Shared export for custom ship order.
export { customShipOrder } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getSellerAnalytics - Helper for get seller analytics.
export { getSellerAnalytics } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getSellerPayoutSettings - Helper for get seller payout settings.
export { getSellerPayoutSettings } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getSellerShipping - Helper for get seller shipping.
export { getSellerShipping } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getSellerStore - Helper for get seller store.
export { getSellerStore } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listBuyerOffers - Helper for list buyer offers.
export { listBuyerOffers } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listSellerCoupons - Helper for list seller coupons.
export { listSellerCoupons } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listSellerMyProducts - Helper for list seller my products.
export { listSellerMyProducts } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listSellerOffers - Helper for list seller offers.
export { listSellerOffers } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listSellerOrders - Helper for list seller orders.
export { listSellerOrders } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listSellerPayouts - Helper for list seller payouts.
export { listSellerPayouts } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// makeOffer - Shared export for make offer.
export { makeOffer } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// requestPayout - Shared export for request payout.
export { requestPayout } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// respondToOffer - Shared export for respond to offer.
export { respondToOffer } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sellerCouponsGET - Shared export for seller coupons get.
export { sellerCouponsGET } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sellerDeleteProduct - Shared export for seller delete product.
export { sellerDeleteProduct } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sellerOffersGET - Shared export for seller offers get.
export { sellerOffersGET } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sellerProductsGET - Shared export for seller products get.
export { sellerProductsGET } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sellerStoreGET - Shared export for seller store get.
export { sellerStoreGET } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sellerUpdateProduct - Shared export for seller update product.
export { sellerUpdateProduct } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updatePayoutSettings - Helper for update payout settings.
export { updatePayoutSettings } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateStore - Helper for update store.
export { updateStore } from "./features/seller/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// withdrawOffer - Shared export for withdraw offer.
export { withdrawOffer } from "./features/seller/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BulkSellerOrderResult - Type contract for bulk seller order result.
export type { BulkSellerOrderResult } from "./features/seller/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BuyerCounterInput - Type contract for buyer counter input.
export type { BuyerCounterInput } from "./features/seller/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CreateStoreInput - Type contract for create store input.
export type { CreateStoreInput } from "./features/seller/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CustomShipOrderInput - Type contract for custom ship order input.
export type { CustomShipOrderInput } from "./features/seller/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// MakeOfferInput - Type contract for make offer input.
export type { MakeOfferInput } from "./features/seller/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RequestPayoutInput - Type contract for request payout input.
export type { RequestPayoutInput } from "./features/seller/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// RespondToOfferInput - Type contract for respond to offer input.
export type { RespondToOfferInput } from "./features/seller/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SellerListParams - Type contract for seller list params.
export type { SellerListParams } from "./features/seller/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UpdatePayoutSettingsInput - Type contract for update payout settings input.
export type { UpdatePayoutSettingsInput } from "./features/seller/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UpdateStoreInput - Type contract for update store input.
export type { UpdateStoreInput } from "./features/seller/server";

// ./features/stores/index
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_STORE_ADDRESS_DATA - Constant used across modules.
export { DEFAULT_STORE_ADDRESS_DATA } from "./features/stores/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_STORE_DATA - Constant used across modules.
export { DEFAULT_STORE_DATA } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// InteractiveStoreCard - Component for interactive store card.
export { InteractiveStoreCard } from "./features/stores/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// STORE_ADDRESS_FIELDS - Constant used across modules.
export { STORE_ADDRESS_FIELDS } from "./features/stores/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// STORE_ADDRESS_INDEXED_FIELDS - Constant used across modules.
export { STORE_ADDRESS_INDEXED_FIELDS } from "./features/stores/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// STORE_ADDRESS_PUBLIC_FIELDS - Constant used across modules.
export { STORE_ADDRESS_PUBLIC_FIELDS } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// STORE_ADDRESS_SUBCOLLECTION - Constant used across modules.
export { STORE_ADDRESS_SUBCOLLECTION } from "./features/stores/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// STORE_ADDRESS_UPDATABLE_FIELDS - Constant used across modules.
export { STORE_ADDRESS_UPDATABLE_FIELDS } from "./features/stores/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// STORE_COLLECTION - Constant used across modules.
export { STORE_COLLECTION } from "./features/stores/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// STORE_FIELDS - Constant used across modules.
export { STORE_FIELDS } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StoreAboutView - Component for store about view.
export { StoreAboutView } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StoreAddressSelectorCreate - Shared export for store address selector create.
export { StoreAddressSelectorCreate } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StoreAuctionsView - Component for store auctions view.
export { StoreAuctionsView } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StoreHeader - Shared export for store header.
export { StoreHeader } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StoreNavTabs - Component for store nav tabs.
export { StoreNavTabs } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StoreProductsView - Component for store products view.
export { StoreProductsView } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StoreReviewsView - Component for store reviews view.
export { StoreReviewsView } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StoreStatusValues - Model for store status values.
export { StoreStatusValues } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// StoresListView - Component for stores list view.
export { StoresListView } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildStoreColumns - Helper for build store columns.
export { buildStoreColumns } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// generateStoreSlug - Helper for generate store slug.
export { generateStoreSlug } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// storeAdminColumns - Shared export for store admin columns.
export { storeAdminColumns } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// storeListItemSchema - Model for store list item schema.
export { storeListItemSchema } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// storeListParamsSchema - Model for store list params schema.
export { storeListParamsSchema } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useStoreAddressSelector - React hook for use store address selector.
export { useStoreAddressSelector } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useStoreAuctions - React hook for use store auctions.
export { useStoreAuctions } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useStoreBySlug - React hook for use store by slug.
export { useStoreBySlug } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useStoreProducts - React hook for use store products.
export { useStoreProducts } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useStoreReviews - React hook for use store reviews.
export { useStoreReviews } from "./features/stores/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useStores - React hook for use stores.
export { useStores } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// InteractiveStoreCardProps - Type contract for interactive store card props.
export type { InteractiveStoreCardProps } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreAboutViewProps - Type contract for store about view props.
export type { StoreAboutViewProps } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreAddressCreateInput - Type contract for store address create input.
export type { StoreAddressCreateInput } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreAddressDocument - Type contract for store address document.
export type { StoreAddressDocument } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreAddressSelectorCreateLabels - Type contract for store address selector create labels.
export type { StoreAddressSelectorCreateLabels } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreAddressSelectorCreateProps - Type contract for store address selector create props.
export type { StoreAddressSelectorCreateProps } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreAddressUpdateInput - Type contract for store address update input.
export type { StoreAddressUpdateInput } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreAuctionItem - Type contract for store auction item.
export type { StoreAuctionItem } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreAuctionsResponse - Type contract for store auctions response.
export type { StoreAuctionsResponse } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreAuctionsViewProps - Type contract for store auctions view props.
export type { StoreAuctionsViewProps } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreDetail - Type contract for store detail.
export type { StoreDetail } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreDocument - Type contract for store document.
export type { StoreDocument } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreListItem - Type contract for store list item.
export type { StoreListItem } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreListParams - Type contract for store list params.
export type { StoreListParams } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreListResponse - Type contract for store list response.
export type { StoreListResponse } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreNavTabsProps - Type contract for store nav tabs props.
export type { StoreNavTabsProps } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreProductItem - Type contract for store product item.
export type { StoreProductItem } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreProductsResponse - Type contract for store products response.
export type { StoreProductsResponse } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreProductsViewProps - Type contract for store products view props.
export type { StoreProductsViewProps } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreReview - Type contract for store review.
export type { StoreReview } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreReviewsData - Type contract for store reviews data.
export type { StoreReviewsData } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreReviewsViewProps - Type contract for store reviews view props.
export type { StoreReviewsViewProps } from "./features/stores/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreTab - Type contract for store tab.
export type { StoreTab } from "./features/stores/index";

// ./features/stores/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// StoresRepository - Shared export for stores repository.
export { StoresRepository } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// createStoreAddressForSeller - Helper for create store address for seller.
export { createStoreAddressForSeller } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// deleteStoreAddressForSeller - Helper for delete store address for seller.
export { deleteStoreAddressForSeller } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getStoreAuctions - Helper for get store auctions.
export { getStoreAuctions } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getStoreBySlug - Helper for get store by slug.
export { getStoreBySlug } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getStoreProducts - Helper for get store products.
export { getStoreProducts } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getStoreReviews - Helper for get store reviews.
export { getStoreReviews } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listStoreAddressesForSeller - Helper for list store addresses for seller.
export { listStoreAddressesForSeller } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// listStores - Helper for list stores.
export { listStores } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// storeAuctionsGET - Shared export for store auctions get.
export { storeAuctionsGET } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// storeProductsGET - Shared export for store products get.
export { storeProductsGET } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// storeReviewsGET - Shared export for store reviews get.
export { storeReviewsGET } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// storeSlugGET - Shared export for store slug get.
export { storeSlugGET } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// storesGET - Shared export for stores get.
export { storesGET } from "./features/stores/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// updateStoreAddressForSeller - Helper for update store address for seller.
export { updateStoreAddressForSeller } from "./features/stores/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreContentParams - Type contract for store content params.
export type { StoreContentParams } from "./features/stores/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreQueryListParams - Type contract for store query list params.
export type { StoreQueryListParams } from "./features/stores/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoreReviewsResult - Type contract for store reviews result.
export type { StoreReviewsResult } from "./features/stores/server";

// ./features/whatsapp-bot/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// WhatsAppChatButton - Component for whats app chat button.
export { WhatsAppChatButton } from "./features/whatsapp-bot/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CheckoutMessageInput - Type contract for checkout message input.
export type { CheckoutMessageInput } from "./features/whatsapp-bot/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// IncomingWebhookPayload - Type contract for incoming webhook payload.
export type { IncomingWebhookPayload } from "./features/whatsapp-bot/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// OrderStatusKey - Type contract for order status key.
export type { OrderStatusKey } from "./features/whatsapp-bot/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SendWhatsAppInput - Type contract for send whats app input.
export type { SendWhatsAppInput } from "./features/whatsapp-bot/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StatusMessageInput - Type contract for status message input.
export type { StatusMessageInput } from "./features/whatsapp-bot/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StatusNotificationInput - Type contract for status notification input.
export type { StatusNotificationInput } from "./features/whatsapp-bot/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// WebhookVerifyInput - Type contract for webhook verify input.
export type { WebhookVerifyInput } from "./features/whatsapp-bot/index";

// ./features/whatsapp-bot/server
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// buildCheckoutMessageURL - Helper for build checkout message url.
export { buildCheckoutMessageURL } from "./features/whatsapp-bot/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// buildStatusMessage - Helper for build status message.
export { buildStatusMessage } from "./features/whatsapp-bot/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// buildStatusNotificationURL - Helper for build status notification url.
export { buildStatusNotificationURL } from "./features/whatsapp-bot/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// isAdminNumber - Shared export for is admin number.
export { isAdminNumber } from "./features/whatsapp-bot/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// parseIncomingWebhookPayload - Helper for parse incoming webhook payload.
export { parseIncomingWebhookPayload } from "./features/whatsapp-bot/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// sendWhatsAppMessage - Shared export for send whats app message.
export { sendWhatsAppMessage } from "./features/whatsapp-bot/server";

// ./features/wishlist/index
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// WishlistCard - Component for wishlist card.
export { WishlistCard } from "./features/wishlist/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// WishlistPage - Component for wishlist page.
export { WishlistPage } from "./features/wishlist/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// WishlistToggleButton - Component for wishlist toggle button.
export { WishlistToggleButton } from "./features/wishlist/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// WishlistView - Component for wishlist view.
export { WishlistView } from "./features/wishlist/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// buildWishlistColumns - Helper for build wishlist columns.
export { buildWishlistColumns } from "./features/wishlist/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useUserWishlist - React hook for use user wishlist.
export { useUserWishlist } from "./features/wishlist/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useWishlist - React hook for use wishlist.
export { useWishlist } from "./features/wishlist/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// useWishlistToggle - React hook for use wishlist toggle.
export { useWishlistToggle } from "./features/wishlist/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// wishlistAdminColumns - Shared export for wishlist admin columns.
export { wishlistAdminColumns } from "./features/wishlist/index";
// [CLIENT-SSR]-Runs in both SSR and browser — React component or hook that does not depend on browser-only APIs.
// wishlistItemSchema - Model for wishlist item schema.
export { wishlistItemSchema } from "./features/wishlist/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UseWishlistToggleReturn - Type contract for use wishlist toggle return.
export type { UseWishlistToggleReturn } from "./features/wishlist/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserWishlistItem - Type contract for user wishlist item.
export type { UserWishlistItem } from "./features/wishlist/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// UserWishlistResponse - Type contract for user wishlist response.
export type { UserWishlistResponse } from "./features/wishlist/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// WishlistResponse - Type contract for wishlist response.
export type { WishlistResponse } from "./features/wishlist/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// WishlistTab - Type contract for wishlist tab.
export type { WishlistTab } from "./features/wishlist/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// WishlistToggleActions - Type contract for wishlist toggle actions.
export type { WishlistToggleActions } from "./features/wishlist/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// WishlistViewProps - Type contract for wishlist view props.
export type { WishlistViewProps } from "./features/wishlist/index";

// ./features/wishlist/server
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// UserWishlistRepository - Shared export for user wishlist repository.
export { UserWishlistRepository } from "./features/wishlist/server";
// [DB]-Database layer — uses firebase-admin or another server-side DB SDK; can only run in a trusted server environment.
// WishlistRepository - Shared export for wishlist repository.
export { WishlistRepository } from "./features/wishlist/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// addToWishlist - Helper for add to wishlist.
export { addToWishlist } from "./features/wishlist/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// getWishlistForUser - Helper for get wishlist for user.
export { getWishlistForUser } from "./features/wishlist/server";
// [SERVER-ONLY]-Server-only — uses Node.js, Next.js server internals, or third-party server SDKs (auth, email, payment, shipping).
// removeFromWishlist - Helper for remove from wishlist.
export { removeFromWishlist } from "./features/wishlist/server";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EnrichedWishlistItem - Type contract for enriched wishlist item.
export type { EnrichedWishlistItem } from "./features/wishlist/server";

// ./features/homepage/schemas/index
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// BANNER_HEIGHTS - Constant used across modules.
export { BANNER_HEIGHTS } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CAROUSEL_SLIDES_COLLECTION - Constant used across modules.
export { CAROUSEL_SLIDES_COLLECTION } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CAROUSEL_SLIDES_INDEXED_FIELDS - Constant used across modules.
export { CAROUSEL_SLIDES_INDEXED_FIELDS } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// CAROUSEL_SLIDES_PUBLIC_FIELDS - Constant used across modules.
export { CAROUSEL_SLIDES_PUBLIC_FIELDS } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_CAROUSEL_SLIDE_DATA - Constant used across modules.
export { DEFAULT_CAROUSEL_SLIDE_DATA } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// DEFAULT_SECTION_ORDER - Constant used across modules.
export { DEFAULT_SECTION_ORDER } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// GRID_CONFIG - Constant used across modules.
export { GRID_CONFIG } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// HOMEPAGE_SECTIONS_COLLECTION - Constant used across modules.
export { HOMEPAGE_SECTIONS_COLLECTION } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// HOMEPAGE_SECTIONS_INDEXED_FIELDS - Constant used across modules.
export { HOMEPAGE_SECTIONS_INDEXED_FIELDS } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// MAX_ACTIVE_SLIDES - Constant used across modules.
export { MAX_ACTIVE_SLIDES } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// canActivateSlide - Shared export for can activate slide.
export { canActivateSlide } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// carouselSlideQueryHelpers - Shared export for carousel slide query helpers.
export { carouselSlideQueryHelpers } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// createCarouselId - Helper for create carousel id.
export { createCarouselId } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// createHomepageSectionId - Helper for create homepage section id.
export { createHomepageSectionId } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// homepageSectionQueryHelpers - Shared export for homepage section query helpers.
export { homepageSectionQueryHelpers } from "./features/homepage/schemas/index";
// [SCHEMA]-Schema / data-shape constant — Zod validator, default-value object, or Firestore collection/field name constant.
// isValidGridPosition - Shared export for is valid grid position.
export { isValidGridPosition } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// AuctionsSectionConfig - Type contract for auctions section config.
export type { AuctionsSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BannerSectionConfig - Type contract for banner section config.
export type { BannerSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BlogArticlesSectionConfig - Type contract for blog articles section config.
export type { BlogArticlesSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// BrandsSectionConfig - Type contract for brands section config.
export type { BrandsSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CarouselSlideCreateInput - Type contract for carousel slide create input.
export type { CarouselSlideCreateInput } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CarouselSlideDocument - Type contract for carousel slide document.
export type { CarouselSlideDocument } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// CategoriesSectionConfig - Type contract for categories section config.
export type { CategoriesSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// EventsSectionConfig - Type contract for events section config.
export type { EventsSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FAQSectionConfig - Type contract for faqsection config.
export type { FAQSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// FeaturesSectionConfig - Type contract for features section config.
export type { FeaturesSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GridCard - Type contract for grid card.
export type { GridCard } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// GridCardCreateInput - Type contract for grid card create input.
export type { GridCardCreateInput } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HomepageSectionCreateInput - Type contract for homepage section create input.
export type { HomepageSectionCreateInput } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HomepageSectionDocument - Type contract for homepage section document.
export type { HomepageSectionDocument } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// HomepageSectionUpdateInput - Type contract for homepage section update input.
export type { HomepageSectionUpdateInput } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// NewsletterSectionConfig - Type contract for newsletter section config.
export type { NewsletterSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ProductsSectionConfig - Type contract for products section config.
export type { ProductsSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// ReviewsSectionConfig - Type contract for reviews section config.
export type { ReviewsSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SectionConfig - Type contract for section config.
export type { SectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// SectionType - Type contract for section type.
export type { SectionType } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// StoresSectionConfig - Type contract for stores section config.
export type { StoresSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// TrustIndicatorsSectionConfig - Type contract for trust indicators section config.
export type { TrustIndicatorsSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// WelcomeSectionConfig - Type contract for welcome section config.
export type { WelcomeSectionConfig } from "./features/homepage/schemas/index";
// [TYPE]-TypeScript type-only export — erased at compile time, zero runtime cost.
// WhatsAppCommunitySectionConfig - Type contract for whats app community section config.
export type { WhatsAppCommunitySectionConfig } from "./features/homepage/schemas/index";
