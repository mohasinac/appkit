// SB-UNI X2 — placeholder for classified-listing CTA list.
//
// Still empty, and now for a settled reason rather than a pending one: the
// classified CTA is `ACTIONS.PRODUCT["make-offer"]`, rendered by
// `MakeOfferButton` through the detail view's `renderOfferAction` slot. The old
// note here promised a "Contact Seller" → conversations flow; that feature was
// deleted 2026-08-31 and the offer is now the type's only purchase path.
export const ctas: readonly string[] = [];
