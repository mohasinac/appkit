// Shared constants — feature-specific constants added here as features migrate to _internal/
// Each feature adds: _internal/shared/features/<f>/config.ts with its own page-size, limits, etc.
export {};
